<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[Details you need to know about Apache Parquet]]></title>
    <url>%2F2020%2F05%2F31%2Fdetails-you-need-to-know-about-Apache-Parquet%2F</url>
    <content type="text"><![CDATA[Parquet is a columnar file format that supports nested data. Lots of data systems support this data format because of it’s great advantage of performance. File format First we should known is that Apache Parquet is a binary encoding like Apache Thrift and Protocol Buffers which are not human-redable, it’s very different from some texual format like JSON, XML and CSV. In order to identify the beginning and ending of the Parquet file, it use a Magic Number(4 special bytes) as separator. Following the first magic number, there are several Row Groups and then Footer. FileMetaData is placed in Footer, because metadata is written after the data is written. Row Groups are about datas. There are three types of metadata: file metadata, column (chunk) metadata and page header metadata. All thrift structures are serialized using the TCompactProtocol. ThriftCompactProtocol is another binary encoding from Apache Thrift project. Some important conceptions listed below. Block (hdfs block): This means a block in hdfs and the meaning is unchanged for describing this file format. The file format is designed to work well on top of hdfs. File: A hdfs file that must include the metadata for the file. It does not need to actually contain the data. Row group: A logical horizontal partitioning of the data into rows. There is no physical structure that is guaranteed for a row group. A row group consists of a column chunk for each column in the dataset. Column chunk: A chunk of the data for a particular column. These live in a particular row group and is guaranteed to be contiguous in the file. Page: Column chunks are divided up into pages. A page is conceptually an indivisible unit (in terms of compression and encoding). There can be multiple page types which is interleaved in a column chunk. Hierarchically, a file consists of one or more row groups. A row group contains exactly one column chunk per column. Column chunks contain one or more pages. MetadataLet’s deep into the metadata. The definition file: Parquet thrift definition FileMetaDataHere is the definition: /** * Description for file metadata */struct FileMetaData &#123; /** Version of this file **/ 1: required i32 version /** Parquet schema for this file. This schema contains metadata for all the columns. * The schema is represented as a tree with a single root. The nodes of the tree * are flattened to a list by doing a depth-first traversal. * The column metadata contains the path in the schema for that column which can be * used to map columns to nodes in the schema. * The first element is the root **/ 2: required list&lt;SchemaElement&gt; schema; /** Number of rows in this file **/ 3: required i64 num_rows /** Row groups in this file **/ 4: required list&lt;RowGroup&gt; row_groups /** Optional key/value metadata **/ 5: optional list&lt;KeyValue&gt; key_value_metadata /** String for application that wrote this file. This should be in the format * &lt;Application&gt; version &lt;App Version&gt; (build &lt;App Build Hash&gt;). * e.g. impala version 1.0 (build 6cf94d29b2b7115df4de2c06e2ab4326d721eb55) **/ 6: optional string created_by /** * Sort order used for the min_value and max_value fields of each column in * this file. Each sort order corresponds to one column, determined by its * position in the list, matching the position of the column in the schema. * * Without column_orders, the meaning of the min_value and max_value fields is * undefined. To ensure well-defined behaviour, if min_value and max_value are * written to a Parquet file, column_orders must be written as well. * * The obsolete min and max fields are always sorted by signed comparison * regardless of column_orders. */ 7: optional list&lt;ColumnOrder&gt; column_orders;&#125; The field ‘num_rows’ is very useful when data reader wanna to count the data, for instance, when SparkSQL count on some paritioned table, Spark just sum all the ‘num_rows’ of each parquet file belong to those filtered partitions. The ‘schema’ is the most important part of this metadata, it is defined by a list of SchemaElement, that’s means each field is represented by a SchemaElement. SchemaElementHere is the structure of SchemaElement. /** * Represents a element inside a schema definition. * - if it is a group (inner node) then type is undefined and num_children is defined * - if it is a primitive type (leaf) then type is defined and num_children is undefined * the nodes are listed in depth first traversal order. */struct SchemaElement &#123; /** Data type for this field. Not set if the current element is a non-leaf node */ 1: optional Type type; /** If type is FIXED_LEN_BYTE_ARRAY, this is the byte length of the vales. * Otherwise, if specified, this is the maximum bit length to store any of the values. * (e.g. a low cardinality INT col could have this set to 3). Note that this is * in the schema, and therefore fixed for the entire file. */ 2: optional i32 type_length; /** repetition of the field. The root of the schema does not have a repetition_type. * All other nodes must have one */ 3: optional FieldRepetitionType repetition_type; /** Name of the field in the schema */ 4: required string name; /** Nested fields. Since thrift does not support nested fields, * the nesting is flattened to a single list by a depth-first traversal. * The children count is used to construct the nested relationship. * This field is not set when the element is a primitive type */ 5: optional i32 num_children; /** When the schema is the result of a conversion from another model * Used to record the original type to help with cross conversion. */ 6: optional ConvertedType converted_type; /** Used when this column contains decimal data. * See the DECIMAL converted type for more details. */ 7: optional i32 scale 8: optional i32 precision /** When the original schema supports field ids, this will save the * original field id in the parquet schema */ 9: optional i32 field_id; /** * The logical type of this SchemaElement * * LogicalType replaces ConvertedType, but ConvertedType is still required * for some logical types to ensure forward-compatibility in format v1. */ 10: optional LogicalType logicalType&#125; The data type of each data column is determined by both ‘type’ and ‘logicalType’ field. The ‘type’ field support several primitive types: BOOLEAN: 1 bit booleanINT32: 32 bit signed intsINT64: 64 bit signed intsINT96: 96 bit signed intsFLOAT: IEEE 32-bit floating point valuesDOUBLE: IEEE 64-bit floating point valuesBYTE_ARRAY: arbitrarily long byte arrays Logical types are some type based on primitive type, the field ‘logicalType’ tells data reader to which LogicalType should the primitive type data be transfered. Logical types are used to extend the types that parquet can be used to store, by specifying how the primitive types should be interpreted. This keeps the set of primitive types to a minimum and reuses parquet’s efficient encodings. For example, strings are stored as byte arrays (binary) with a UTF8 annotation. Many types supported by ‘logicalType’. Such as: STRING,ENUM,UUID,DATE,TIME,TIMESTAMP,INTERVALINT,DECIMALJSON,BSONLIST,MAPNULL INT annotation can be used to specify the maximum number of bits in the stored value. The annotation has two parameter: bit width and sign, such as:INT(8, true), INT(16, true), INT(32, true), INT(64, true). For more details about LogicalType see Parquet Logical Type Definitions ColumnMetaData/** * Description for column metadata */struct ColumnMetaData &#123; /** Type of this column **/ 1: required Type type /** Set of all encodings used for this column. The purpose is to validate * whether we can decode those pages. **/ 2: required list&lt;Encoding&gt; encodings /** Path in schema **/ 3: required list&lt;string&gt; path_in_schema /** Compression codec **/ 4: required CompressionCodec codec /** Number of values in this column **/ 5: required i64 num_values /** total byte size of all uncompressed pages in this column chunk (including the headers) **/ 6: required i64 total_uncompressed_size /** total byte size of all compressed pages in this column chunk (including the headers) **/ 7: required i64 total_compressed_size /** Optional key/value metadata **/ 8: optional list&lt;KeyValue&gt; key_value_metadata /** Byte offset from beginning of file to first data page **/ 9: required i64 data_page_offset /** Byte offset from beginning of file to root index page **/ 10: optional i64 index_page_offset /** Byte offset from the beginning of file to first (only) dictionary page **/ 11: optional i64 dictionary_page_offset /** optional statistics for this column chunk */ 12: optional Statistics statistics; /** Set of all encodings used for pages in this column chunk. * This information can be used to determine if all data pages are * dictionary encoded for example **/ 13: optional list&lt;PageEncodingStats&gt; encoding_stats;&#125; Each ColumnChunk has only one ColumnMetaData, so one ColumnMetaData defined how to store one column’s data in one row group. Some fields should be noticed: encodings: just for validation, each column’s encoding is defined in DataPageHeader(see below); codec: the compression algorithm used, such as SNAPPY, GZIP, LZO and so on; statistics: statistics information for the column of the row group. Some useful field are showed below, they are very useful for distinct counting or filtering. With the ‘max_value’ and ‘min_value’, SparkSQL can do filter push-down by skipping some row groups. struct Statistics &#123; ... /** count of null value in the column */ 3: optional i64 null_count; /** count of distinct values occurring */ 4: optional i64 distinct_count; /** * Min and max values for the column, determined by its ColumnOrder. * * Values are encoded using PLAIN encoding, except that variable-length byte * arrays do not include a length prefix. */ 5: optional binary max_value; 6: optional binary min_value;&#125; PageHeaderPageHeader is kind of like parent class of DataPageHeader, IndexPageHeader and DictionaryPageHeader, contains some common fields. Each data page has a DataPageHeader, let’s look into it below. DataPageHeader/** * New page format allowing reading levels without decompressing the data * Repetition and definition levels are uncompressed * The remaining section containing the data is compressed if is_compressed is true **/struct DataPageHeaderV2 &#123; /** Number of values, including NULLs, in this data page. **/ 1: required i32 num_values /** Number of NULL values, in this data page. Number of non-null = num_values - num_nulls which is also the number of values in the data section **/ 2: required i32 num_nulls /** Number of rows in this data page. which means pages change on record boundaries (r = 0) **/ 3: required i32 num_rows /** Encoding used for data in this page **/ 4: required Encoding encoding // repetition levels and definition levels are always using RLE (without size in it) /** length of the definition levels */ 5: required i32 definition_levels_byte_length; /** length of the repetition levels */ 6: required i32 repetition_levels_byte_length; /** whether the values are compressed. Which means the section of the page between definition_levels_byte_length + repetition_levels_byte_length + 1 and compressed_page_size (included) is compressed with the compression_codec. If missing it is considered compressed */ 7: optional bool is_compressed = 1; /** optional statistics for this column chunk */ 8: optional Statistics statistics;&#125; encoding: This field is about how to store primitive type data with bytes, for example, a PLAIN encoding means to use 4 bytes, PLAIN_DICTIONARY means to use a dictionary.For more details see Parquet encoding definitions statistics: statistics information for this page ps: The comment of statistics is wrong, it should be ‘optional statistics for the data in this page’, I have fixed that in my PR. How encoding work with compression ? First, the data be encoded , second, encoded output is then compressed with a generic compression algorithm specified in ColumnMetaData like Snappy. There is another description. Encoding: It’s more at application level where data representation is changed. The encoding can also minimize space usage which can give us a kind of compression.Compression : In general it’s the Technic to reduce storage for given data in bytes irrespective of underline data is already encoded or not. Parquet-toolsParquet-tools is a convenient offical tool to play with Parquet file. If you use MacOS and homebrew, just install it by brew install parquet-tools . To show metadata: parquet-tools meta yourfile.parquet Or use hadoop distribution: hadoop jar ./parquet-tools-&lt;VERSION&gt;.jar &lt;command&gt; You’ll get something like this: creator: parquet-mr version 1.10.1 (build a89df8f9932b6ef6633d06069e50c9b7970bebd1)extra: org.apache.spark.sql.parquet.row.metadata = &#123;&quot;type&quot;:&quot;struct&quot;,&quot;fields&quot;:[&#123;&quot;name&quot;:&quot;v&quot;,&quot;type&quot;:&quot;i [more]...file schema: spark_schema-------------------------------------------------------------------------------------------------------------------v: REQUIRED INT32 R:0 D:0sq: REQUIRED INT32 R:0 D:0str: OPTIONAL BINARY O:UTF8 R:0 D:1row group 1: RC:5000 TS:94040-------------------------------------------------------------------------------------------------------------------v: INT32 SNAPPY DO:0 FPO:4 SZ:20056/20050/1.00 VC:5000 ENC:PLAIN,BIT_PACKEDsq: INT32 SNAPPY DO:0 FPO:20060 SZ:20030/20050/1.00 VC:5000 ENC:PLAIN,BIT_PACKEDstr: BINARY SNAPPY DO:0 FPO:40090 SZ:21960/53940/2.46 VC:5000 ENC:RLE,PLAIN,BIT_PACKED More details: Parquet Tools Use Parquet with SparkYou can find some guide here: Spark SQL Guide &gt; Parquet Files. With Parquet file, spark can do some optimizations. Column projection The idea behind this feature is simple: just read the data for columns that the query needs to process and skip the rest of the data. Column-oriented data formats like Parquet can implement this feature quite naturally. Predicate push down Predicate push down is another feature of Spark and Parquet that can improve query performance by reducing the amount of data read from Parquet files. Predicate push down works by evaluating filtering predicates in the query against metadata stored in the Parquet files. Does predicate push down available for nest field ? Yes. If the field schema is user(id, name), data will be stored in two columns as user.id and user.name, then both id and name have thier own statistic info in metadata. Use Parquet with HDFSWhen using Parquet with HDFS, you should care about the row group size. Row group size: Larger row groups allow for larger column chunks which makes it possible to do larger sequential IO. Larger groups also require more buffering in the write path (or a two pass write). We recommend large row groups (512MB - 1GB). Since an entire row group might need to be read, we want it to completely fit on one HDFS block. Therefore, HDFS block sizes should also be set to be larger. An optimized read setup would be: 1GB row groups, 1GB HDFS block size, 1 HDFS block per HDFS file. The row group size is controlled by config named parquet.block.size, the default value is 128MB, You can set it in Spark as below:sc.hadoopConfiguration.setInt(&quot;parquet.block.size&quot;,256*1024*1024)ordf.write.option(&quot;parquet.block.size&quot;,256*1024*1024) ReferenceApache Parquet Diving into Spark and Parquet Workloads, by Example]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>Apache Parquet</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[基于Redis实现分布式锁和分布式限流器]]></title>
    <url>%2F2020%2F03%2F29%2Fredis-lock-rate-limiter%2F</url>
    <content type="text"><![CDATA[分布式锁和分布式限流器应该是算是比较常见的需求了，而Redis现在几乎是应用的标配了，于是很多人会倾向于选择基于Redis来实现，因为不需要引入额外的依赖。 分布式锁和分布式限流器在Java领域比较成熟和常用的开源实现是Redisson(中文官方介绍)，下面从它的极小部分源码，分析下分布式锁和分布式限流器的实现逻辑。 分布式锁的实现加锁Redisson中加锁实现的核心源码如下，为了实现操作的原子性，不得不使用eval命令加Lua脚本的方式。 &lt;T&gt; RFuture&lt;T&gt; tryLockInnerAsync(long leaseTime, TimeUnit unit, long threadId, RedisStrictCommand&lt;T&gt; command) &#123; internalLockLeaseTime = unit.toMillis(leaseTime); return commandExecutor.evalWriteAsync(getName(), LongCodec.INSTANCE, command, "if (redis.call('exists', KEYS[1]) == 0) then " + "redis.call('hincrby', KEYS[1], ARGV[2], 1); " + "redis.call('pexpire', KEYS[1], ARGV[1]); " + "return nil; " + "end; " + "if (redis.call('hexists', KEYS[1], ARGV[2]) == 1) then " + "redis.call('hincrby', KEYS[1], ARGV[2], 1); " + "redis.call('pexpire', KEYS[1], ARGV[1]); " + "return nil; " + "end; " + "return redis.call('pttl', KEYS[1]);", Collections.&lt;Object&gt;singletonList(getName()), internalLockLeaseTime, getLockName(threadId));&#125; 把上面的Lua代码的逻辑翻译成redis命令组成的伪代码： //获取锁if exists $lockName == 0 hset $lockName $threadUid 1 pexpire $lockName $expireTime return nil//重入锁if hexists $lockName $threadUid == 1 hincrby $lockName $threadUid 1 pexpire $lockName $expireTime return nil//获取失败return pttl $lockName 现在逻辑很清晰了吧，注意这里的锁使用了hash结构而不是string，因为实现的是可重入锁，需要记录加锁的线程标识，并维持一个计数器。 有的需求是要线程一直等待直到获取到锁的，Redisson中通过subscribe命令订阅锁相关的一个channel，收到通知后会再次尝试获取锁。 关于可重入锁,我在Scala并发编程实战 2：Lock 锁中有解释过： 所谓可重入锁，也就是说一个线程可以在持有该锁的时候，再次获取该锁。可重入锁通常与一个计数器关联，第一次获取锁的时候，计数器从0变为1，再次获取锁，变为2，以此类推。释放锁的时候，计数器每次减1，直至减为0，该锁才真正释放给其他线程。 另外需要注意的是，这里实现的锁是有过期时间的，如果需要一直持有锁，redisson中的实现是，会用一个定时任务不断去刷新过期时间。(可以想想，为什么不用一个永不过期的key来实现呢？) 解锁下面看解锁的源码。 protected RFuture&lt;Boolean&gt; unlockInnerAsync(long threadId) &#123; return commandExecutor.evalWriteAsync(getName(), LongCodec.INSTANCE, RedisCommands.EVAL_BOOLEAN, "if (redis.call('hexists', KEYS[1], ARGV[3]) == 0) then " + "return nil;" + "end; " + "local counter = redis.call('hincrby', KEYS[1], ARGV[3], -1); " + "if (counter &gt; 0) then " + "redis.call('pexpire', KEYS[1], ARGV[2]); " + "return 0; " + "else " + "redis.call('del', KEYS[1]); " + "redis.call('publish', KEYS[2], ARGV[1]); " + "return 1; "+ "end; " + "return nil;", Arrays.&lt;Object&gt;asList(getName(), getChannelName()), LockPubSub.UNLOCK_MESSAGE, internalLockLeaseTime, getLockName(threadId));&#125; 核心也是Lua实现，翻译下： //锁是否存在if exists $lockName == 0 publish $channel 0 return 1//是否持有锁if hexists $lockName $threadUid ==0 return nil//减少持有计数counter = hincrby $lockName $threadUid -1if counter &gt; 0 //刷新锁 pexpire $lockName $expireTime return 0else //释放锁 del $lockName publish $channel 0 return 1 逻辑应该容易看懂，其中publish是为了通知其他线程锁释放了，可以来抢啦。 分布式限流器的实现如果一个服务有多个实例，做限流的话，通常有两种方式，一种是对每个实例单独限制，一种是对所有实例整体的流量做限制。Redisson中对这两种都有实现，以限流器的类型来区分，第一种称作PER_CLIENT，第二种为OVERALL。 限流算法有若干种，Redisson采用的是令牌桶算法，核心实现同样是Lua： private &lt;T&gt; RFuture&lt;T&gt; tryAcquireAsync(RedisCommand&lt;T&gt; command, Long value) &#123; return commandExecutor.evalWriteAsync(getName(), LongCodec.INSTANCE, command, "local rate = redis.call('hget', KEYS[1], 'rate');" + "local interval = redis.call('hget', KEYS[1], 'interval');" + "local type = redis.call('hget', KEYS[1], 'type');" + "assert(rate ~= false and interval ~= false and type ~= false, 'RateLimiter is not initialized')" + "local valueName = KEYS[2];" + "if type == '1' then " + "valueName = KEYS[3];" + "end;" + "local currentValue = redis.call('get', valueName); " + "if currentValue ~= false then " + "if tonumber(currentValue) &lt; tonumber(ARGV[1]) then " + "return redis.call('pttl', valueName); " + "else " + "redis.call('decrby', valueName, ARGV[1]); " + "return nil; " + "end; " + "else " + "assert(tonumber(rate) &gt;= tonumber(ARGV[1]), 'Requested permits amount could not exceed defined rate'); " + "redis.call('set', valueName, rate, 'px', interval); " + "redis.call('decrby', valueName, ARGV[1]); " + "return nil; " + "end;", Arrays.asList(getName(), getValueName(), getClientValueName()), value); 简化为伪代码： //令牌桶容量$rate = hget $configKey rate//间隔（比如rate=10,interval=1000,就代表1000毫秒中允许10次，即QPS为10）$interval = hget $configKey interval//类型$type = hget $configKey type//检查限流器是否已经初始化assert($rate,$interval,$type都存在)//$valueName是存储令牌桶的key$valueName = $valueKey//如果是PER_CLIENT类型if type == '1' $valueName = $clientValueKey//$currentValue是当前令牌数，$acquireValue是申请的令牌数$currentValue = get $valueNameif $currentValue != false //令牌桶已经存在 if $currentValue &lt; $acquireValue //令牌小于申请数，申请令牌失败 return pttl $valueName else //令牌 减去 申请数 decrby $valueName $acquireValueelse //令牌桶不存在 //检查申请数不能大于令牌桶容量 assert($rate&gt;$acquireValue) //创建令牌桶 set $valueName $rate px $interval decrby $valueName $acquireValue return nil 值得一提的是，令牌桶的配置信息也保存到了redis中，可以看到rate，interval，type信息都是从一个hash结构中获取的，从获取令牌桶配置到更新令牌桶整个过程是原子化的。这是考虑到多个应用实例的情况下，每个实例拿到的限流器的配置可能出现不一致的情况，特别是在修改限流器配置的时候。 参考文献Redisson github转载请注明原文地址：Liam’s Blog]]></content>
      <tags>
        <tag>redis</tag>
        <tag>lock</tag>
        <tag>rate limiter</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark Core 解析 4：内存模型]]></title>
    <url>%2F2020%2F02%2F29%2Fspark-core-memory%2F</url>
    <content type="text"><![CDATA[Spark内存模型Spark之所以快，很大程度上是因为它善于利用内存，大量利用内存进行存储和计算，从而减少磁盘IO，提升执行效率。 从1.6版本开始，Spark引入了统一内存管理模型（之前版本只有静态内存管理，这里不细说），找到两张图描述的很清楚： 为什么有两张图呢，因为Spark既能使用JVM堆内存(on-heap)，也能使用堆外内存(off-heap)，两张图分别描述的是这两块内存的情况。图中已经把Spark对内存容量的划分很形象的说明了，网上相关文章也不少，不再赘述。 几个需要注意的点 Storage与Execution区域之间的虚线，代表Storage与Execution内存的容量是可以动态变化的，比如Storage内存不足的时候，可以占用Execution的内存。但是，on-heap中的Storage不可以占用off-heap中的Execution内存，因为on-heap及off-heap整个的大小是固定的，没法互相占用。 Storage与Execution空间都不足时，都需要溢写至磁盘；Execution空间不足时，若有空间被Storage借用，该空间可以通过淘汰或转存磁盘的方式归还；Storage空间不足时，若有空间被Execution借用，则无法立即归还，只能等待用完释放。 Spark对堆内内存的管理是一种逻辑上的”规划式”的管理，因为对象实例占用内存的申请和释放都由JVM完成，Spark只能在申请后和释放前记录这些内存。说白了Spark只是个记账的，记录每次申请了多少内存，就能算出还剩多少内存。然鹅这个内存的帐并不是那么容易精确记录的，往往会对不上帐。首先，对于off-heap堆外内存来说，内存可以比较精确地申请和释放，问题不大。对于on-heap内存来说，序列化的对象可以精确计算大小，但非序列化的对象就只能估算了(出于性能考虑)，所以存在记账的内存大小不准的情况。另外，on-heap内存的回收是JVM自动进行的，账本上释放掉的内存空间，不一定已经被回收。因为记账的不准，所以即使进行了内存管理还是会有OOM的风险。 统一内存模型的实现核心的实现类是UnifiedMemoryManager，其中维护了4个内存账本：onHeapStorageMemoryPool，offHeapStorageMemoryPool，onHeapExecutionMemoryPool，offHeapExecutionMemoryPool，分别对应啥一看名字就知道了。 虽然他们叫MemoryPool，还提供了releaseMemory、acquireMemory之类的方法，看上去挺虎，但实际上只是记录已用内存大小，进行数值上的加加减减而已。而Storage与Execution内存的互相借用，其实只是账本MemoryPool大小数值的调整而已。 Execution执行内存每个Executor有一个MemoryManager，用来管理该Executor节点的内存；每个TaskAtempt都有一个TaskMemoryManger管理单次Task执行的内存；每个TaskMemoryManger对当前TaskAtempt中的多个MemoryConsumer进行管理，负责分配内存给MemoryConsumer；MemoryConsumer就是Spark中最终的执行内存消费者，是一个抽象类，比较典型的实现类有ExternalAppendOnlyMap、ShuffleExternalSorter、ExternalSorter等，不难发现这些类都是与shuffle相关的（还有一些实现类是Spark SQL相关的），也即是说执行内存基本就是shuffle使用的内存。 Shuffle相关可以参考Spark Core 解析 3：Shuffle Storage存储内存主要用于存储cache和broadcast的数据，为Spark的存储体系服务。介绍存储体系的时候再细说。 参考文献Apache Spark 内存管理详解]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>Spark Core</tag>
        <tag>memory</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark Core 解析 3：Shuffle]]></title>
    <url>%2F2019%2F12%2F29%2Fspark-core-shuffle%2F</url>
    <content type="text"><![CDATA[Overview所谓shuffle就是将数据按新的规则进行分区的过程，将数据分区从旧分区转变成新分区。 这个过程分为两个阶段，第一个阶段称为shuffle write，每个计算节点需要对自己持有的那部分数据按新分区规则进行重新分区，并按新分区写入文件。shuffle write完成后，同一个新分区的数据会分散在不同的节点上，这样是不利于接下来的计算的，必须把同一个新分区的数据汇集到一个节点才行，所以需要第二阶段shuffle read。shuffle read阶段将按照新分区，拉取shuffle write生成的文件，相同新分区的数据汇集到同一节点，组合成新的分区，这样才算是实现了新的数据分区。 Spark中负责实现shuffle的组件是ShuffleManger，其核心是ShuffleWriter和ShuffleReader两块，分别负责shuffle write和read的实现。 从下图可以看出，Spark的ShuffleManger经过不断的发展，到目前只剩下了一种即SortShuffleManager，本文的分析即是基于这种实现。 ShuffleWriter概述顾名思义，ShuffleWriter就是Spark中负责shuffle write阶段的组件啦。 Spark2.x中虽然只有一种Shuffle管理器(SortShuffleManager)，但是支持3种不同行为的ShuffleWriter，分别是SortShuffleWriter、BypassMergeSortShuffleWriter、UnsafeShuffleWriter。 下面先简单介绍下这几种ShuffleWriter，然后再做详细分析。 BypassMergeSortShuffleWriter绕过排序和聚合的ShuffleWriter，这个是shuffle write最朴素的实现。它将数据按新分区写到不同的文件，最后再把这些文件合并成一个文件，同时生成一个索引文件来标识数据块与分区的对应关系。 SortShuffleWriter这是基于排序的ShuffleWriter，在BypassMergeSortShuffleWriter的基础之上，它可以支持对数据进行聚合，而且会对数据进行分区排序。 PS:为什么shuffle write阶段要对数据进行排序？因为reduce操作通常会要对数据排序，如果在map端进行一次初排，可以减轻reduce的压力。 UnsafeShuffleWriter可以使用堆外内存的ShuffleWriter，它的行为相当于不带聚合功能的SortShuffleWriter，但是可以使用堆外内存提高性能。 另一个优化点是，支持对序列化后的二进制数据排序，不仅能减少内存的消耗(因为序列化后的数据更紧凑)，也能避免多次序列化反序列化(溢出到文件时需要序列化，合并文件时需要反序列化)的性能损耗。 会采用哪个ShuffleWriterSpark自身会按如下规则对ShuffleWriter进行选择，从上往下依次判断： 1 当分区数(按新规则的)小于等于配置spark.shuffle.sort.bypassMergeThreshold(默认为200)的值，使用BypassMergeSortShuffleWriter； 2 如果当前依赖的serializer的序列化结果支持重排(后面解释)，则使用UnsafeShuffleWriter； 3 否则采用SortShuffleWriter。 ShuffleReader概述ShuffleReader只有一种即BlockStoreShuffleReader，顾名思义就是从BlockStore读取shuffle数据。在进行shuffle read时，会异步拉取数据到内存或磁盘(大小超过一定阈值时)。拉取数据的同时对数据进行聚合(如果定义了聚合器)和排序(如果定义了排序器)操作。 到此为止，基本概念讲解完毕，下面开始讲原理。 Sorter 排序器在shuffle过程中，有一个步骤不仅比较耗费资源，也是性能瓶颈之一，这个步骤就是排序，用来排序的组件称为Sorter。 要理解这3种ShuffleWriter的区别，必须先了解Sorter，因为他们的排序过程依赖不同的Sorter，导致他们的行为各异。 SortShuffleWriter使用的是ExternalSorter，UnsafeShuffleWriter采用的是ShuffleExternalSorter，BypassMergeSortShuffleWriter则没有使用Sorter。 ExternalSorter 外部排序器除了排序，这个排序器还支持数据的聚合和溢写到磁盘。它使用到了几个重要的数据结构，下面详细介绍下。 AppendOnlyMap （划重点）AppendOnlyMap是一个支持新增和修改的哈希表，但是不支持删除元素。 底层存储结构很简单，就是一个数组Array[AnyRef]，元素的key和value紧挨着存放，像这样：key0,value0,null,null,...,key1,value1,...。假如AppendOnlyMap的容量为capacity，那么这个数组的大小就是2 * capacity。 key到数组索引的映射也很简单，就是对key计算hash值，然后对容量取模，最后乘以2，hash(key) % capacity * 2。如果发生了哈希冲突，则继续探测后面的位置，第一次探测+2的位置，第二次探测+2+4的位置，以此类推，可以总结出一个公式 $idx+2*\sum_{i=1}^{k}i$ ，其中idx是根据hash值计算出的位置，k代表探测次数。 获取元素如何取出元素呢，比如数据组名为data，key计算出索引为4，如果data(4)==key，那么对应的值就是data(5)，如果data(4)!=key，那继续检查data(4+2)、data(4+2+4)等等是否等于key。 PS：支持key为null的元素，单独存储在一个变量中；容量有个约束，它一定是2的幂。 插入元素先根据上面的方法，计算出key对应的数据索引idx，如果idx上没有存储数据，那就直接在idx存下key，idx+1的位置存下value。如果idx上已经存了数，刚好就是要插入的key值，那也好办，直接更新value即可。 若idx上已经存了另一个key值，那就发生冲突了。接下来尝试idx+2的位置，如果继续冲突，则继续尝试idx+2+4、idx+2+4+6、…的位置，直到成功。 聚合元素用来修改元素的API是changeValue(key: K, updateFunc: (Boolean, V) =&gt; V): V，处理流程基本与插入元素一样，只是当key已经存在时，会用updateFunc处理旧值与新值，如果传入的updateFunc是将新值与旧值merge的函数，那么就可以用changeValue方法来实现元素聚合的目的。 内置排序进行排序的API是destructiveSortedIterator(keyComparator: Comparator[K]): Iterator[(K, V)]，需要传入用来比较key的比较器，返回一个迭代器。 函数首先要对底层数组进行整理，将各元素向前移动，使得元素紧挨着排列，也就是把数组中空着的位置用后面的元素填上，然后使用TimSort排序算法(插入排序与归并排序结合的一种算法)对数组进行排序(使用传入的Comparator比较key)，最后输出这个排序后数组的迭代器。 需要注意的是，经过排序后，AppendOnlyMap变成了destroyed状态，不再支持新增和修改元素，只能通过迭代器访问元素。 小结一下AppendOnlyMap是一个类似map的数据结构，支持对相同元素进行聚合，同时也可以对元素进行排序，但是不支持删除元素。 PartitionedAppendOnlyMap是AppendOnlyMap的子类，继承了AppendOnlyMap的功能，还另外实现了SizeTracker和WritablePartitionedPairCollection特质，主要是增加了集合大小估算(使用内存大小)、按分区和key排序、写入磁盘的功能。 PartitionedPairBuffer底层是与AppendOnlyMap使用的一样的数组，但是元素不按哈希值存储，而是顺序存储(这样排序前就不需要整理数据了)，同样支持排序，但是不支持对元素的聚合(因为采用的存储方式不支持随机存取元素)。与PartitionedAppendOnlyMap类似，支持集合大小估算(使用内存大小)、按分区和key排序、写入磁盘的功能。 ExternalSorter 原理ExternalSorter使用了PartitionedAppendOnlyMap和PartitionedPairBuffer作为数据buffer，或者说数据容器： 当需要进行map端聚合的时候，使用PartitionedAppendOnlyMap存储数据，这样就拥有了对数据进行聚合和分区排序功能； 当不需要进行map端聚合的时候，使用PartitionedPairBuffer，就获得了性能更好(因为不需要整理数据)的分区排序功能。 总的来说，ExternalSorter的功能有：缓冲数据、分区排序、输出排序后的数据到文件。 溢写另外，ExternalSorter实现了将数据溢出到磁盘的功能，这样即使内存无法装下所有的数据，也能进行分区排序。溢写到磁盘的数据，最终会与内存中的数据合并后再持久化到磁盘。 所谓溢写，是指内存装不下不断增加的数据了，只好把内存中的数据写到磁盘的过程。ExternalSorter的溢写过程大致是： 1 当buffer内存使用量大于阈值，并且申请不到更多内存的时候，启动溢写； 2 调用buffer的排序函数，并将数据写入磁盘； 3 创建一个新的buffer，继续接受新数据。 输出最终结果的时候，会把所有的溢写文件，连同内存中的数据，通过归并排序(merge sort)合并成一个文件。 ShuffleExternalSorter这也是一种外部排序器，支持分区排序和数据溢出到磁盘，但是不支持数据聚合，也不支持合并溢写文件。溢写文件合并的工作由UnsafeShuffleWriter完成。比较特别的是，这个排序器可以使用堆外(off-heap)内存，因为使用了Unsafe类操作内存。Unsafe类不仅能操作off-heap内存，也能操作on-heap内存，所以ShuffleExternalSorter是既能使用堆内内存，也能使用堆外内存的。 ShuffleReaderShuffleReader只有一种即BlockStoreShuffleReader。在进行shuffle read时，会先创建ShuffleBlockFetcherIterator，通过ShuffleClient开始异步拉取数据到内存或磁盘(大小超过一定阈值时)。拉取数据的同时对数据进行聚合(如果定义了聚合器)和排序(如果定义了排序器)操作。其中聚合操作使用的是ExternalAppendOnlyMap，排序则是使用ExternalSorter。 经典的SortShuffle过程简化版本shuffle write阶段： 1 SortShuffleWriter不断从RDD的迭代器取出数据并存入buffer。如果有定义聚合操作，会采用PartitionedAppendOnlyMap作为buffer，边插入边聚合数据，否则使用PartitionedPairBuffer，只插入数据不做聚合。存入buffer过程中，如果内存不够，会触发spill，将数据排序后溢写到磁盘； 2 对buffer中的数据按新分区和key进行排序，如果有spill文件，则进行合并，然后输出一个数据文件(data file)和一个索引文件(index file)，返回MapStatus。 shuffle read阶段： 1 BlockStoreShuffleReader先通过mapOutputTracker获取需要拉取的block信息，然后开始拉取数据到buffer中； 2 拉取数据的同时，如果该ShuffleDependency有定义聚合器，则会通过ExternalAppendOnlyMap对数据进行聚合；接下来，如果该ShuffleDependency有定义排序器，则会使用ExternalSorter进行排序，最终会返回一个迭代器。 详细版本假若使用的是SortShuffleWriter和BlockStoreShuffleReader，接下来对照2.4版本的源码，做个整体流程分析。 这一切从ShuffleMapTask开始… （不知道什么是ShuffleMapTask，请看Spark Core解析 2：Scheduler 调度体系） 1 ShuffleMapTask的runTask方法被Executor的某个线程执行，task开始执行； 2 runTask方法调用SortShuffleManager.getWriter方法，创建SortShuffleWriter实例，然后调用SortShuffleWriter.write方法，开启shuffle write阶段； 3 write方法中会new一个ExternalSorter实例，并调用这个实例的insertAll方法，并传入RDD(所属stage最后一个RDD)对应的一个分区的Iterator，进入数据缓冲聚合阶段； 4 insertAll方法中，每从Iterator中取一个值，就把这个值存入内存中的缓冲数据结构buffer。从Iterator中取值会触发RDD上用户定义的计算逻辑，取出的这一条数据其实是从数据源读出并经过各种父RDD的计算逻辑后得到的结果。如果有定义聚合操作，会采用PartitionedAppendOnlyMap作为buffer，边插入边聚合数据，否则使用PartitionedPairBuffer，只插入数据不做聚合。存入buffer过程中，如果内存不够，会触发spill，将数据排序后溢写到磁盘，然后创建新的buffer，继续存入数据； 5 数据全部存入buffer后，开始排序输出阶段。SortShuffleWriter.write方法调用ExternalSorter.writePartitionedFile方法，对buffer中的数据按新分区和key进行排序(如果有spill文件，则利用heap结构进行merge sort)，然后输出一个数据文件(data file)和一个索引文件(index file)，索引文件中记录了数据文件中每个分区的数据开始的offset。（注意：buffer的输入数据虽然只有一个分区，但是输出会按新分区方式输出多个分区的数据。） 6 write方法写完数据后，返回MapStatus（包含shuffleServerId和分区信息），最终MapStatus会发送给DAGScheduler，然后被MapOutputTracker记录任务信息， 7 shuffle write结束，下面说说shuffle read。shuffle read既可能发生在ShuffleMapTask也可能发生在ResultTask，这取决于task所属stage包含的RDD的dependency是否含有ShuffleDependency，对于ShuffleDependency，RDD的compute函数会调用BlockStoreShuffleReader.read方法，开启read流程。 8 read函数中会先通过mapOutputTracker获取需要拉取的block信息，然后创建ShuffleBlockFetcherIterator，ShuffleBlockFetcherIterator被创建后会立即开始拉取数据(通过ShuffleClient)，拉取过程是异步的，拉取到的数据会暂存到buffer中。 9 拉取数据的同时，如果该ShuffleDependency有定义聚合器，则会通过ExternalAppendOnlyMap对数据进行聚合；接下来，如果该ShuffleDependency有定义排序器，则会使用ExternalSorter进行排序。 10 read函数最终会返回一个Iterator，成为task的数据来源。 小结：如果有n个ShuffleMapTask，write阶段最终会生成2n个临时文件(不含spill文件)，read阶段除了spill文件不会产生临时数据文件。 黑科技UnsafeShuffle过程UnsafeShuffleWriter.write方法，间接调用ShuffleExternalSorter.insertRecord方法，传入序列化后的数据record，数据缓冲过程开始； 缓冲阶段第1步，根据情况进行溢写。ShuffleExternalSorter.insertRecord方法中，先检查记录数，达到阈值，则进行spill溢写；溢写过程大致为：调用ShuffleInMemorySorter.getSortedIterator对存入的recordAddress地址排序，这里只是按partitionId排序(RadixSort和TimSort两种算法)，按照排序好的recordAddress，依次输出数据(仍然是序列化后的)到一个文件，并记录每个partition的offset到SpillInfo中； 缓冲阶段第2步，申请内存块page。如果ShuffleExternalSorter的currentPage空间不足，会先申请page(即MemoryBlock)；申请过程会依次使用到TaskMemoryManager.allocatePage和MemoryManager.tungstenMemoryAllocator().allocate，依据MemoryManager使用的MemoryAllocator是HeapMemoryAllocator还是UnsafeMemoryAllocator，申请到的MemoryBlock是on-heap和off-heap类型地址的其中一种； 缓冲阶段第3步，数据写入page。currentPage空间足够后，依次将record字节大小，record本身复制到currentPage中以pageCursor开始的地址段中，并增加pageCursor游标地址； 缓冲阶段第4步，保存record地址。通过taskMemoryManager.encodePageNumberAndOffset得到recordAddress，连同partitionId存入ShuffleInMemorySorter； 当数据全部缓冲完毕，开始排序输出阶段。 接下来write方法会间接调用ShuffleExternalSorter.writeSortedFile，将内存中剩下的数据全部溢写； UnsafeShuffleWriter.mergeSpills方法将spill的文件全部合并成一个，这里会利用之前保存的SpillInfo，将相同partition的数据合到一起；(这里合并有三种实现：transferTo-based fast merge，fileStream-based fast merge，slow merge) 通过shuffleBlockResolver.writeIndexFileAndCommit创建索引文件； 返回mapStatus； shuffle write阶段结束，read阶段与SortShuffle相同 小结：如果配置了spark.memory.offHeap.enabled和spark.memory.offHeap.size参数，则会使用off-heap缓冲数据； 数据和地址指针分开存储，排序只是对地址指针进行。 UnsafeShuffleWriter的黑科技主要体现在： write全程只在第一步做了数据序列化，后面操作的都是序列化后的数据，甚至写文件时也没有再进行序列化； 使用off-heap内存存储数据，大大减少JVM GC压力，而且避免了IO时数据在堆内堆外内存之间的拷贝； 使用地址指针进行排序，减少了序列化反序列化的性能消耗； spill文件合并时，‘transferTo-based fast merge’使用NIO提供的transferTo方法，文件数据直接在内核空间(kernel-space)拷贝合并，提升了性能。 参考文献Spark内核设计的艺术转载请注明原文地址：Liam’s Blog]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>Spark Core</tag>
        <tag>shuffle</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark Core解析 2：Scheduler 调度体系]]></title>
    <url>%2F2019%2F11%2F07%2Fspark-core-scheduler%2F</url>
    <content type="text"><![CDATA[Overview调度系统，是贯穿整个Spark应用的主心骨，从调度系统开始入手了解Spark Core，比较容易理清头绪。 Spark的资源调度采用的是常见的两层调度，底层资源的管理和分配是第一层调度，交给YARN、Mesos或者Spark的Standalone集群处理，Application从第一层调度拿到资源后，还要进行内部的任务和资源调度，将任务和资源进行匹配，这是第二层调度，本文讲的就是这第二层调度。 Spark的调度体系涉及的任务包括3个粒度，分别是Job、Stage、Task。Job代表用户提交的一系列操作的总体，一个具体的计算任务，有明确的输入输出，一个Job由多个Stage组成；一个Stage代表Job计算流程的一个组成部分，一个阶段，包含多个Task；一个Task代表对一个分区的数据进行计算的具体任务。 层级关系：Job &gt; Stage &gt; Task 在Spark Core 解析：RDD 弹性分布式数据集中，已经解释了RDD之间的依赖，以及如何组成RDD血缘图。 所以本文主要目的就是解释清楚：Scheduler将RDD血缘图转变成Stage DAG，然后生成Task，最后提交给Executor去执行的过程。 StageJob的不同分区的计算通常可以并行，但是有些计算需要将数据进行重新分区，这个过程称作shuffle(混洗)。Shuffle的过程是没法完全并行的，这时候就会出现task之间的等待，task的数量也可能发生变化，所以Spark中以shuffle为边界，对task进行划分，划分出来的每段称为Stage。 Stage代表一组可以并行的执行相同计算的task，每个任务必须有相同的分区规则，这样一个stage中是没有shuffle的。 在一个Spark App中，stage有一个全局唯一ID，stage id是自增的。 Stage分为两种： ResultStage：最后执行的stage，负责Job最终的结果输出，每个Job有且仅有一个ResultStage； ShuffleMapStage：该stage的输出不是最终结果，而是其他stage的输入数据，通常涉及一次shuffle计算。 stage创建流程： 从最终执行action的RDD开始，沿着RDD依赖关系遍历，一旦发现某个RDD的dependency是ShuffleDependency，就创建一个ShuffleMapStage。 最后创建ResultStage。 example 1val rg=sc.parallelize(List((1,10),(2,20)))rg.reduceByKey(_+_).collect 这里reduceByKey操作引起了一次shuffle，所以job被切分成了2个stage。 example 2val rddA=sc.parallelize(List((1,"a"),(2,"b"),(3,"c")))val rddB=sc.parallelize(List((1,"A"),(2,"B"),(3,"C")))rddA.join(rddB).collect join操作导致rddA和rddB都进行了一次shuffle，所以有3个stage。 example 3import org.apache.spark.HashPartitionerval rddA=sc.parallelize(List((1,"a"),(2,"b"),(3,"c"))).partitionBy(new HashPartitioner(3))val rddB=sc.parallelize(List((1,"A"),(2,"B"),(3,"C")))rddA.join(rddB).collect WHAT ? 因为rddA已经定义了Partitioner，这里join操作会保留rddA的分区方式，所以对rddA的依赖是OneToOneDepenency，而对于rddB则是ShuffleDependency。 探索：一个RDD被依赖多次，会如何val rddA=sc.parallelize(List((1,"a"),(2,"b"),(3,"c")))rddA join rddA collect 一个RDD被两个stage使用了。 小结综上，stage的划分一定是依据shuffle即ShuffleDependency，跟算子和RDD变量的定义没有很强的关系，example2和3中的join操作rddA.join(rddB).collect看起来一模一样，但实际产生的stage划分却差别很大。 Task与stage对应，task也分为两种： ShuffleMapTask：即ShuffleMapStage中的task，主要完成map、shuffle计算。 ResultTask：ResultStage中的task，主要完成最终结果输出或者返回结果给driver的任务。 一个stage有多少个partition就会创建多少个task，比如一个ShuffleMapStage有10个partition，那么就会创建10个ShuffleMapTask。 一个Stage中的所有task组成一个TaskSet。 Job Submitgraph TB R(RDD.action)-->S(SparkContext.runJob)-- RDD -->D(DAGScheduler.runJob) -- TaskSet -->T(TaskScheduler.submitTasks)-- TaskDescription -->E(Executor.launchTask) RDD在action操作中通过SparkContext.runJob方法触发Job执行流程，该方法将调用DagScheduler.runJob方法，将RDD传入DagScheduler。然后，DAGScheduler创建TaskSet提交给TaskScheduler，TaskScheduler再将TaskSet封装成TaskDescription发送给Executor，最后Executor会将TaskDescription提交给线程池来运行。 Stage Scheduler(high-level)DagSchedulerStage级别的调度是DagScheduler负责的，也是Spark调度体系的核心。 DagScheduler的工作模式sequenceDiagram participant M as main thread participant L as eventProcessLoop participant E as event thread M-->>L: post event E-->>L: handle event DagScheduler内部维护了一个事件消息总线eventProcessLoop(类型为DAGSchedulerEventProcessLoop)，其实就是一个用来存储DAGSchedulerEvent类型数据的队列。 当DagScheduler的一些方法被调用的时候（如submitJob方法），并不会在主线程中处理该任务，而是post一个event(如JobSubmitted)到eventProcessLoop。eventProcessLoop中有一个守护线程，会不断的依次从队列中取出event，然后调用对应的handle(如handleJobSubmitted)方法来执行具体的任务。 Stage调度流程 1.submit job DagScheduler.runJob方法会调用submitJob方法，向eventProcessLoop发送一个JobSubmitted类型的消息，其中包含了RDD等信息。当eventProcessLoop接收到JobSubmitted类型的消息，会调用DagScheduler.handleJobSubmitted方法来处理消息。 sequenceDiagram participant M as main thread(runJob) participant L as eventProcessLoop participant E as event thread(handleJobSubmitted) M-->>L: post JobSubmitted event E-->>L: handle JobSubmitted event 2.create stage DagScheduler在它的handleJobSubmitted方法中开始创建ResultStage。ResultStage中包含了最终执行action的finalRDD，以及计算函数func。 ResultStage有个parents属性，这个属性是个列表，也就是说可以有多个parent stage。创建ResultStage时需要先创建它的parent stage来填充这个属性，也就是说要创建ResultStage直接依赖的所有ShuffleMapStage。 通过stage.rdd.dependencies属性，采用宽度优先遍历，一旦发现某个RDD(假设叫rddA)的dependency是ShuffleDependency，就创建一个ShuffleMapStage，ShuffleMapStage中包含的关键信息与ResultStage不同，是rddA的ShuffleDependency和rddA的ShuffleDependency.rdd，也就是说新创建的ShuffleMapStage持有的信息是他自身的最后一个RDD和该RDD的子RDD的dependency。 创建一个ShuffleMapStage的过程同理会需要创建它的parent stage，也是若干ShuffleMapStage。如此递归下去，直到创建完所有的ShuffleMapStage，最后才完成ResultStage的创建。最后创建出来的这些Stage(若干ShuffleMapStage加一个ResultStage)，通过parent属性串起来，就像这样 graph TD A[ResultStage]-- parent -->B[ShuffleMapStage 1] A-- parent -->C[ShuffleMapStage 2] B-- parent -->D[ShuffleMapStage 3] 这就生成了所谓的DAG图，但是这个图的指向跟执行顺序是反过来的，如果按执行顺序来画DAG图，就是常见的形式了： graph TD D[ShuffleMapStage 3]-->C[ShuffleMapStage 2] C[ShuffleMapStage 2]-->A[ResultStage] B[ShuffleMapStage 1]-->A[ResultStage] 3.submit stage DagScheduler.handleJobSubmitted方法创建好ResultStage后会提交这个stage(submitStage方法)，在提交一个stage的时候，会要先提交它的parent stage,也是通过递归的形式，直到一个stage的所有parent stage都被提交了，它自己才能被提交，如果一个stage的parent还没有完成，则会把这个stage加入waitingStages。也就是说，DAG图中前面的stage会被先提交。当一个stage的parent都准备好了，也就是执行完了，它才会进入submitMissingTasks的环节。 4.submit task Task是在DagScheduler（不是TaskScheduler）的submitMissingTasks方法中创建的，包括ShuffleMapTask和ResultTask，与Stage对应。归属于同一个stage的这批Task组成一个TaskSet集合，最后提交给TaskScheduler的就是这个TaskSet集合。 Task Scheduler(low-level)Task的调度工作是由TaskScheduler与SchedulerBackend紧密合作，共同完成的。 TaskScheduler是task级别的调度器，主要作用是管理task的调度和提交，是Spark底层的调度器。 SchedulerBackend是TaskScheduler的后端服务，有独立的线程，所有的Executor都会注册到SchedulerBackend，主要作用是进行资源分配、将task分配给executor等。 Task调度流程 第一个线程是DAGScheduler的事件处理线程，在其中，Task先经过DAGScheduler（蓝色箭头表示）封装成TaskSet，再由TaskScheduler（绿色箭头）封装成TaskSetManager，并加入调度队列中。 SchedulerBackend在收到ReviveOffers消息时，会从线程池取一个线程进行makeOffers操作，WorkerOffer创建后传递给TaskScheduler进行分配。 图中第二个线程就是SchedulerBackend的一个事件分发线程，从Pool中取出最优先的TaskSetManager，然后将WorkerOffer与其中的Task进行配对，生成TaskDescription，发送给WorkerOffer指定的Executor去执行。 工作流程 1 DAGScheduler(submitMissingTasks方法中)调用TaskScheduler.submitTasks()创建并提交TaskSet给TaskScheduler； 2 TaskScheduler拿到TaskSet后会创建一个TaskSetManager来管理它，并且把TaskSetManager添加到rootPool调度池中； 3 调用SchedulerBackend.reviveOffers()方法； 4 SchedulerBackend发送ReviveOffers消息给DriverEndpoint； 5 DriverEndpoint收到ReviveOffers消息后，会调用makeOffers()方法创建WorkerOffer，并通过TaskScheduler.resourceOffers()返回offer； 6 TaskScheduler从rootPool获取按调度算法排序后的TaskSetManager列表，取第一个TaskSetManager，逐个给TaskSet的Task分配WorkerOffer，生成TaskDescription(包含offer信息)； 7 调用SchedulerBackend.DriverEndpoint的launchTasks方法，将TaskDescription序列化并封装在LaunchTask消息中，发送给offer指定的executor。LaunchTask消息被ExecutorBackend收到后，会将Task信息反序列化，传给Executor.launchTask()，最后使用Executor的线程池中的线程来执行这个Task。 梳理Stage,TaskSet,TaskSetManager是一一对应的，数量相等，都是只存在driver上的。Parition,Task,TaskDescription是一一对应，数量相同，Task和TaskDescription是会被发到executor上的。 TaskScheduler的调度池与DAGScheduler不同的是TaskScheduler有调度池，有两种调度实体，Pool和TaskSetManager。与YARN的调度队列类似，采用了层级队列的方式，Pool是TaskSetManager的容器，起到将TaskSetManager分组的作用。 SchedulableSchedulable是调度实体的基类，有两个子类Pool和TaskSetManager。 要理解调度规则，必须知道下面几个属性： parent：所属调度池，顶层的调度池为root pool； schedulableQueue：包含的调度对象组成的队列； schedulingMode：调度模式，FIFO or FAIR； weight：权重 minShare：最小分配额(CPU核数) runningTasks：运行中task数 priority：优先级 stageId：就是stageId name：名称 Pool和TaskSetManager对于这些属性的取值有所不同，从而导致了他们的调度行为也不一样。 properties Pool TaskSetManager weight config 1 minShare config 0 priority 0 jobId stageId -1 stageId name config TaskSet_{taskSet.id} runningTasks Pool所含TaskSetManager的runningTasks和 TaskSetManager运行中task数 Pools创建流程TaskScheduler有个属性schedulingMode，值取决于配置项spark.scheduler.mode，默认为FIFO。这个属性会导致TaskScheduler使用不同的SchedulableBuilder，即FIFOSchedulableBuilder和FairSchedulableBuilder。 TaskScheduler在初始化的时候，就会创建root pool，根调度池，是所有pool的祖先。它的属性取值为： name: &quot;&quot; (空字符串)schedulingMode: 同TaskScheduler的schedulingMode属性weight: 0minShare: 0 注意root pool的调度模式确定了。 接下来会执行schedulableBuilder.buildPools()方法， 如果是FIFOSchedulableBuilder，则什么都不会发生。 若是FairSchedulableBuilder 1 依据scheduler配置文件(后面会说)，开始创建pool(可以是多个pool，FIFO，FAIR都有可能，取决于配置文件)，并都加入root pool中。 2 如果现在root pool中没有名为”default”的pool(即配置文件中没有定义一个叫default的pool)，创建default pool，并加入root pool中。这时default pool它的属性取值是固定的： name: &quot;default&quot;schedulingMode: FIFOweight: 1minShare: 0 Task加入pool流程当TaskScheduler提交task的时候，会先创建TaskSetManager，然后通过schedulableBuilder添加到pool中。 如果是FIFOSchedulableBuilder，则会直接把TaskSetManager加入root pool队列中。 若是FairSchedulableBuilder 1 从spark.scheduler.pool配置获取pool name，没有定义则用’default’； 2 从root pool遍历找到对应名称的pool，把TaskSetManager加入pool的队列。如果没有找到，则创建一个该名称的pool，采用与default pool相同的属性配置，并加入root pool。 调度池结构经过上面两部分，最终得到的调度池结构如下： spark.scheduler.mode=FIFO spark.scheduler.mode=FAIR Fair Scheduler pools配置Fair Scheduler Pool的划分依赖于配置文件，默认的配置文件为’fairscheduler.xml’，也可以通过配置项”spark.scheduler.allocation.file”指定配置文件。 煮个栗子，文件内容如下： &lt;?xml version="1.0"?&gt;&lt;allocations&gt; &lt;pool name="prod"&gt; &lt;schedulingMode&gt;FAIR&lt;/schedulingMode&gt; &lt;weight&gt;1&lt;/weight&gt; &lt;minShare&gt;2&lt;/minShare&gt; &lt;/pool&gt; &lt;pool name="test"&gt; &lt;schedulingMode&gt;FIFO&lt;/schedulingMode&gt; &lt;weight&gt;2&lt;/weight&gt; &lt;minShare&gt;3&lt;/minShare&gt; &lt;/pool&gt;&lt;/allocations&gt; 这里配置了两个pool，prod和test，并且配置了相关属性，这两个pool都会添加到root pool中。 调度算法以SchedulingAlgorithm为基类，内置实现的调度算法有两种FIFOSchedulingAlgorithm和FairSchedulingAlgorithm，其逻辑如下： FIFO: 先进先出，优先级比较算法如下， 1.比较priority，小的优先； 2.priority相同则比较StageId，小的优先。 FAIR：公平调度，优先级比较算法如下， 1.runningTasks小于minShare的优先级比不小于的优先级要高。 2.若两者运行的runningTasks都比minShare小，则比较minShare使用率(runningTasks/max(minShare,1))，使用率越低优先级越高。 3.若两者的minShare使用率相同，则比较权重使用率(runningTasks/weight)，使用率越低优先级越高。 4.若权重也相同，则比较name，小的优先。 Pool为FIFO模式下的几种情形TaskSetManager之间的比较，其实就是先比较jobId再比较stageId，谁小谁优先，意味着就是谁先提交谁优先。 Pool之间的比较，不存在！FIFO的pool队列中是不会有pool的。 Pool为FAIR模式下的几种情形TaskSetManager之间的比较，因为minShare=0，weight=1，FAIR算法变成了： 1 runningTasks小的优先 2 runningTasks相同则比较name Pool之间的比较，就是标准的FAIR算法。 当root pool为FAIR模式，先取最优先的pool，再从pool中，按pool的调度模式取优先的TaskSetManager。 开始使用FAIR mode启用FAIR模式： 1 准备好fairscheduler.xml文件 2 启动参数添加 --conf spark.scheduler.mode=FAIR 3 运行启动命令，如spark-shell --master yarn --deploy-mode client --conf spark.scheode=FAIR 启动后如果直接运行Job会自动提交到default pool，那么如何提交Job到指定pool？SparkContext.setLocalProperty(“spark.scheduler.pool”,”poolName”) 如果每次只运行一个Job，开启FAIR模式的意义不大，那么如何同时运行多个Job？要异步提交Job，需要用到RDD的async action，目前有如下几个： countAsynccollectAsynctakeAsyncforeachAsyncforeachPartitionAsync 举个例子： sc.setLocalProperty("spark.scheduler.pool","test")b.foreachAsync(_=&gt;Thread.sleep(100))sc.setLocalProperty("spark.scheduler.pool","production")b.foreachAsync(_=&gt;Thread.sleep(100)) 这样就会有两个任务在不同的pool同时运行： FAIR mode应用场景场景1：Spark SQL thrift server作用：让离线任务和交互式查询任务分配到不同的pool，给交互式查询任务更高的优先级，这样长时间运行的离线任务就不会一直占用所有资源，阻塞交互式查询任务。 场景2：Streaming job与Batch job同时运行作用：比如用Streaming接数据写入HDFS，可能产生很多小文件，可以在低优先级的pool定时运行batch job合并小文件。 另外可以参考Spark Summit 2017的分享：Continuous Application with FAIR Scheduler 参考Spark内核设计的艺术 spark任务调度FIFO和FAIR的详解 Job Scheduling 转载请注明原文地址：https://liam-blog.ml/2019/11/07/spark-core-scheduler/]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>Spark Core</tag>
        <tag>Scheduler</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark Core解析 1：RDD 弹性分布式数据集]]></title>
    <url>%2F2019%2F10%2F23%2Fspark-core-rdd%2F</url>
    <content type="text"><![CDATA[引言Spark Core是Spark的核心部分，是Spark SQL，Spark Streaming，Spark MLlib等等其他模块的基础, Spark Core提供了开发分布式应用的脚手架，使得其他模块或应用的开发者不必关心复杂的分布式计算如何实现，只需使用Spark Core提供的分布式数据结构RDD及丰富的算子API，以类似开发单机应用的方式来进行开发。 图中最下面那个就是Spark Core啦，日常使用的RDD相关的API就属于Spark Core，而Dataset、DataFrame则属于Spark SQL。 RDD 概览本文基于Spark 2.x。 定义RDD (Resilient Distributed Dataset，弹性分布式数据集)： Resilient：不可变的、容错的 Distributed：数据分散在不同节点（机器，进程） Dataset：一个由多个分区组成的数据集 特征In-Memory：RDD会优先使用内存Immutable（Read-Only）：一旦创建不可修改Lazy evaluated：惰性执行Cacheable：可缓存，可复用Parallel：可并行处理Typed：强类型，单一类型数据Partitioned：分区的Location-Stickiness：可指定分区优先使用的节点 是Spark中最核心的数据抽象，数据处理和计算基本都是基于RDD。 组成一个RDD通常由5个要素组成： 一组分区(partition) 一个计算函数 一组依赖(直接依赖的父RDD) 一个分区器 (可选) 一组优先计算位置(e.g. 将Task分配至靠近HDFS块的节点进行计算) (可选) 与传统数据结构对比，只关心访问，不关心存储。通过迭代器访问数据，只要数据能被不重复地访问即可。 算子算子，即对RDD进行变换的操作，按照是否触发Job提交可以分为两大类： transformation：不会立即执行的一类变换，不会触发Job执行，会生成并返回新的RDD，同时记录下依赖关系。如：map,filter,union,join,reduceByKey。 action: 会立即提交Job的一类变换，不会返回新的RDD，而是直接返回计算结果。如：count,reduce,foreach。 算子与RDD的关系transformation类型的算子通常都会返回新的RDD，虽然只返回一个新的RDD给用户，但是在RDD的血缘关系图(RDD linage)中，有可能新增了多个RDD。 先看算子与RDD一对一的情况：map =&gt; MapPartitionsRDDfilter =&gt; MapPartitionsRDDreduceByKey =&gt; ShuffledRDD… 一对多：join =&gt; CoGroupedRDD-&gt;MapPartitionsRDD-&gt;MapPartitionsRDDdistinct =&gt; MapPartitionsRDD-&gt;ShuffledRDD-&gt;MapPartitionsRDD… 一个算子生成的多个RDD，也不一定归属于同一个stage，例如distinct算子，生成的第一个MapPartitionsRDD归属于前一个stage，其他的则归属于后一个stage，其中产生了一次shuffle。 Partition &amp; Partitioner为什么要把数据分区？把数据分成若干partition是为了将数据分散到不同节点不同线程，从而能进行分布式的多线程的并行计算。 按什么规则分区？RDD从数据源生成的时候，数据通常是随机分配到不同的partition或者保持数据源的分区，如sc.parallelize(…)，sc.textFile(…)。 这对于某些RDD操作来说是没有问题的，比如filter(),map(),flatMap()，rdd.union(otherRDD)，rdd.intersection(otherRDD)，rdd.subtract(otherRDD)。 但是对于reduceByKey(),foldByKey(),combineByKey(),groupByKey()，sortByKey()，cogroup(), join() ,leftOuterJoin(), rightOuterJoin()这些操作，随机分配分区就非常不友好，会带来很多额外的网络传输。影响一个分布式计算系统性能的最大敌人就是网络传输，所以必须尽量最小化网络传输。 为了减少网络传输，怎么分区才合理？对于reduceByKey操作应该把相同key的数据放到同一分区；对于sortByKey操作应该把同一范围的数据放到同一分区。 可见不同的操作适合不同的数据分区规则，Spark将划分规则抽象为Partitioner(分区器) ，分区器的核心作用是决定数据应归属的分区，本质就是计算数据对应的分区ID。 在Spark Core中内置了2个Partitioner来支持常用的分区规则(Spark MLlib,Spark SQL中有其他的)。 HashPartitioner 哈希分区器 RangePartitioner 范围分区器 HashPartitioner哈希分区器是默认的分区器，也是使用最广泛的一个，作用是将数据按照key的hash值进行分区。 分区ID计算公式非常简单：key的hash值 % 分区个数 ， 如果key为null，则返回0. 也就是将key的hash值(Java中每个对象都有hash code,对象相等则hash code相同)，除以分区个数，取余数为分区ID，这样能够保证相同Key的数据被分到同一个分区，但是每个分区的数据量可能会相差很大，出现数据倾斜。 RangePartitionerRangePartitioner的作用是根据key，将数据按范围大致平均的分到各个分区，只支持能排序的key。 要知道一个key属于哪个分区，需要知道每个分区的边界值。确定边界值需要对数据进行排序，因为数据量通常较大，通过样本替代总体来估计每个分区的边界值。 采样流程： 使用水塘抽样对总体进行采样； 针对数据量远超平均值的分区，进行传统抽样(伯努利抽样)。 使用场景：sortByKey 扩展问题： 如何使用对于一个没有明确指定Partitioner的情况下，reduceByKey(),foldByKey(),combineByKey(),groupByKey()等操作会默认使用HashPartitioner。sortByKey操作会采用RangePartitioner。 reduceByKey也有一个可以自定义分区器的版本：reduceByKey(partitioner: Partitioner, func: (V, V) =&gt; V) Function传入给transformation的函数transformation会生成新的RDD，传给RDD transformation的函数最终会以成员变量的形式存储在新生成的RDD中。 以map函数为例。 val r11 = r00.map(n =&gt; (n, n)) map函数接受的参数类型为f: T =&gt; U，因为Scala支持函数式编程，函数可以像值一样存储在变量中，也可以作为参数传递。f参数的类型T =&gt; U代表一种函数类型，这个函数的输入参数的类型必须为T，输出类型为U，这里T和U都是泛型，T代表RDD中数据的类型，对于RDD[String]来说，T就是String。 最终f参数，会转换成有关迭代器的一个函数，存储到RDD的f成员变量中。 最终存储的类型为：f: (TaskContext, Int, Iterator[T]) =&gt; Iterator[U]对于map来说是这样一个函数(context, pid, iter) =&gt; iter.map(f)也就是说我们传入到RDD.map的f函数，最终传给了Iterator.map函数。 传入给action的函数action不会生成新的RDD，而是将函数传递给Job。 Dependency当RDD1经过transformation生成了RDD2，就称作RDD2依赖RDD1，RDD1是RDD2的父RDD，他们是父子关系。 先看一个例子 val r00 = sc.parallelize(0 to 9)val r01 = sc.parallelize(0 to 90 by 10)val r10 = r00 cartesian r01val r11 = r00.map(n =&gt; (n, n))val r12 = r00 zip r01val r13 = r01.keyBy(_ / 20)val r20 = Seq(r11, r12, r13).foldLeft(r10)(_ union _) 我们看下RDD之间的依赖关系图 RDD的依赖关系网又叫RDD的血统(lineage)，可以看做是RDD的逻辑执行计划。 同义词：RDD lineage，RDD operator graph，RDD dependency graph Dependency存储父RDD与子RDD之间的依赖关系记录在子RDD的属性中(deps: Seq[Dependency[_]])，数据类型为Dependency(可以有多个)，Dependency中保存了父RDD的引用，这样通过Dependency就能找到父RDD。 Dependency分类Dependency不仅描述了RDD之间的依赖关系，还进一步描述了不同RDD的partition之间的依赖关系。 依据partition之间依赖关系的不同Dependency分为两大类： NarrowDependency 窄依赖，1个父分区只对应1个子分区，这时父RDD不需要改变分区方式。如：map、filter、union，co-paritioned join ShuffleDependency Shuffle依赖(宽依赖)，1个父分区对应多个子分区，这种情况父RDD必须重新分区，才能符合子RDD的需求。如：groupByKey、reduceByKey、sortByKey，（not co-paritioned）join NarrowDependencyNarrowDependency是一个抽象类，一共有3中实现类，也就是说有3种NarrowDependency。 OneToOneDependency：一对一依赖，比如map, RangeDependency：范围依赖，如 union PruneDependency：裁剪依赖，过滤掉部分分区，如PartitionPruningRDD ShuffleDependency出现shuffle依赖表示父RDD与子RDD的分区方式发生了变化。 RDD分类RDD的具体实现类有几十种(大概60+)，介绍下最常见的几种。 scala&gt; r20.toDebugStringres34: String =(28) UnionRDD[38] at union at &lt;pastie&gt;:31 [] | UnionRDD[37] at union at &lt;pastie&gt;:31 [] | UnionRDD[36] at union at &lt;pastie&gt;:31 [] | CartesianRDD[32] at cartesian at &lt;pastie&gt;:27 [] | ParallelCollectionRDD[30] at parallelize at &lt;pastie&gt;:25 [] | ParallelCollectionRDD[31] at parallelize at &lt;pastie&gt;:26 [] | MapPartitionsRDD[33] at map at &lt;pastie&gt;:28 [] | ParallelCollectionRDD[30] at parallelize at &lt;pastie&gt;:25 [] | ZippedPartitionsRDD2[34] at zip at &lt;pastie&gt;:29 [] | ParallelCollectionRDD[30] at parallelize at &lt;pastie&gt;:25 [] | ParallelCollectionRDD[31] at parallelize at &lt;pastie&gt;:26 [] | MapPartitionsRDD[35] at keyBy at &lt;pastie&gt;:30 [] | ParallelCollectionRDD[31] at parallelize at &lt;pastie&gt;:26 [] 不同的RDD代表着不同的‘计算模式’：MapPartitionsRDD，对Iterator的每个值应用相同的函数； ShuffledRDD，对Iterator执行combineByKey的模式，可以指定createCombiner: V =&gt; C,mergeValue: (C, V) =&gt; C, mergeCombiners: (C, C) =&gt; C, compute函数返回ShuffleReader生成的迭代器。 MapPartitionsRDDMapPartitionsRDD对于父RDD的依赖类型只能是OneToOneDependency，代表将函数应用到每一个分区的计算。 相关transformation：map, flatMap, filter, mapPartitions等等 scala&gt; sc.parallelize(0 to 10000).map(x=&gt;(x%9,1)).dependenciesres35: Seq[org.apache.spark.Dependency[_]] = List(org.apache.spark.OneToOneDependency@7c6843f) ShuffledRDD对于父RDD的依赖类型只能是ShuffleDependency，代表需要改变分区方式进行shuffle的计算。 会创建ShuffledRDD的transformation：RDD：coalescePairRDDFunctions： reduceByKey, combineByKeyWithClassTag ， partitionBy (分区方式不同时) 等OrderedRDDFunctions： sortByKey， repartitionAndSortWithinPartitions RDD CheckpointCheckpoint检查点，是一种截断RDD依赖链，并把RDD数据持久化到存储系统(通常是HDFS或本地)的过程。主要作用是截断RDD依赖关系，防止stack overflow(与DAG递归调用有关)。存储的数据包括RDD计算后的数据和partitioner。 Checkpoint分为两种： reliable ：调用函数为RDD.checkpoint()，数据保存到可靠存储HDFS，RDD的parent替换为ReliableCheckpointRDD； local：调用函数为RDD.localCheckpoint()，数据保存到spark cache中(不是本地)，RDD的parent替换为LocalCheckpointRDD。当executor挂掉，数据会丢失。 注意：与streaming中的checkpointing不同，streaming中的checkpointing会同时保存元数据和RDD数据，可以用于Application容错。 如何使用scala&gt; :paste// Entering paste mode (ctrl-D to finish)val a=sc.parallelize(0 to 9)val b=a.map(_*10)val c=b.filter(_&gt;10)// Exiting paste mode, now interpreting.a: org.apache.spark.rdd.RDD[Int] = ParallelCollectionRDD[0] at parallelize at &lt;console&gt;:24b: org.apache.spark.rdd.RDD[Int] = MapPartitionsRDD[1] at map at &lt;console&gt;:25c: org.apache.spark.rdd.RDD[Int] = MapPartitionsRDD[2] at filter at &lt;console&gt;:26scala&gt; c.toDebugStringres0: String =(4) MapPartitionsRDD[2] at filter at &lt;console&gt;:26 [] | MapPartitionsRDD[1] at map at &lt;console&gt;:25 [] | ParallelCollectionRDD[0] at parallelize at &lt;console&gt;:24 [] scala&gt; sc.setCheckpointDir("/tmp/spark-checkpoint")scala&gt; b.checkpointscala&gt; b.countres4: Long = 10scala&gt; c.toDebugStringres5: String =(4) MapPartitionsRDD[2] at filter at &lt;console&gt;:26 [] | MapPartitionsRDD[1] at map at &lt;console&gt;:25 [] | ReliableCheckpointRDD[3] at count at &lt;console&gt;:26 [] scala&gt; b.toDebugStringres6: String =(4) MapPartitionsRDD[1] at map at &lt;console&gt;:25 [] | ReliableCheckpointRDD[3] at count at &lt;console&gt;:26 [] //local scala&gt; c.localCheckpointscala&gt; c.countres9: Long = 8scala&gt; c.toDebugStringres10: String =(4) MapPartitionsRDD[2] at filter at &lt;console&gt;:26 [Disk Memory Deserialized 1x Replicated] | CachedPartitions: 4; MemorySize: 104.0 B; ExternalBlockStoreSize: 0.0 B; DiskSize: 0.0 B | LocalCheckpointRDD[4] at count at &lt;console&gt;:26 [Disk Memory Deserialized 1x Replicated] 查看HDFS上存储的checkpoint文件hdfs dfs -ls /tmp/spark-checkpoint/74acd422-2693-4f47-b786-69b4f8dc33ad/rdd-1Found 4 items-rw-r--r-- 2 ld-liuyuan_su hdfs 91 2019-10-09 08:40 /tmp/spark-checkpoint/74acd422-2693-4f47-b786-69b4f8dc33ad/rdd-1/part-00000-rw-r--r-- 2 ld-liuyuan_su hdfs 101 2019-10-09 08:40 /tmp/spark-checkpoint/74acd422-2693-4f47-b786-69b4f8dc33ad/rdd-1/part-00001-rw-r--r-- 2 ld-liuyuan_su hdfs 91 2019-10-09 08:40 /tmp/spark-checkpoint/74acd422-2693-4f47-b786-69b4f8dc33ad/rdd-1/part-00002-rw-r--r-- 2 ld-liuyuan_su hdfs 101 2019-10-09 08:40 /tmp/spark-checkpoint/74acd422-2693-4f47-b786-69b4f8dc33ad/rdd-1/part-00003 RDD CacheCache机制是Spark提供的一种将数据缓存到内存(或磁盘)的机制，主要用途是使得中间计算结果可以被重用。 常见的使用场景有如下几种，底层都是调用RDD的cache，这里只讲RDD的cache。rdd.cache()dataset.cache()spark.sql(&quot;cache table test.test&quot;)... Spark的Cache不仅能将数据缓存到内存，也能使用磁盘，甚至同时使用内存和磁盘，这种缓存的不同存储方式，称作‘StorageLevel(存储级别)’。 可以这样使用：rdd.persist(StorageLevel.MEMORY_ONLY)。 Spark目前支持的存储级别如下：NONE (default)DISK_ONLDISK_ONLY_2MEMORY_ONLY (cache操作使用的级别)MEMORY_ONLY_2MEMORY_ONLY_SERMEMORY_ONLY_SER_2MEMORY_AND_DISKMEMORY_AND_DISK_2MEMORY_AND_DISK_SERMEMORY_AND_DISK_SER_2OFF_HEAP 2代表存储份数为2，也就是有个备份存储。SER代表存储序列化后的数据。 DISK_ONLY后面没跟SER，但其实只能是存储序列化后的数据。 要cache RDD,常用到两个函数, cache()和persist()，cache方法本质上是persist(StorageLevel.MEMORY_ONLY)，也就是说persist可以指定StorageLevel，而cache不行。 Checkpoint vs Cache Cache用于缓存，采用临时保存，Executor挂掉会导致数据丢失，但是数据可以重新计算。 Checkpoint用于截断依赖链，reliable方式下Executor挂掉不会丢失数据，数据一旦丢失不可恢复。 RDD Broadcast一种将数据在不同节点间共享的机制，可以将指定的只读数据广播分发到每个Executor，每个Executor有一份完整的备份。 是一种高效的数据共享机制，被广播的数据可以被不同的stage和task共享，而不需要给每个task拷贝一份。 Broadcast机制有个非常重要的作用，Spark就是通过它将task分发给各个Executor。 下面举个使用的例子 rddA k low 1 a 2 b 3 c rddB k up 1 A 2 B 3 C rddAB k low up 1 a A 2 b B 3 c C scala&gt; val rddA=sc.parallelize(List((1,"a"),(2,"b"),(3,"c")))rddA: org.apache.spark.rdd.RDD[(Int, String)] = ParallelCollectionRDD[5] at parallelize at &lt;console&gt;:24scala&gt; val rddB=sc.parallelize(List((1,"A"),(2,"B"),(3,"C")))rddB: org.apache.spark.rdd.RDD[(Int, String)] = ParallelCollectionRDD[6] at parallelize at &lt;console&gt;:24scala&gt; val rddAB=rddA.join(rddB)rddAB: org.apache.spark.rdd.RDD[(Int, (String, String))] = MapPartitionsRDD[9] at join at &lt;console&gt;:27scala&gt; rddAB.collectres11: Array[(Int, (String, String))] = Array((1,(a,A)), (2,(b,B)), (3,(c,C)))scala&gt; rddAB.toDebugStringres12: String =(4) MapPartitionsRDD[9] at join at &lt;console&gt;:27 [] | MapPartitionsRDD[8] at join at &lt;console&gt;:27 [] | CoGroupedRDD[7] at join at &lt;console&gt;:27 [] +-(4) ParallelCollectionRDD[5] at parallelize at &lt;console&gt;:24 [] +-(4) ParallelCollectionRDD[6] at parallelize at &lt;console&gt;:24 []scala&gt; val rddBMap=sc.broadcast(rddB.collectAsMap)rddBMap: org.apache.spark.broadcast.Broadcast[scala.collection.Map[Int,String]] = Broadcast(9)scala&gt; val rddABMapJoin= rddA.map&#123;case(k,v) =&gt; (k,(v,rddBMap.value.get(k).get))&#125;rddABMapJoin: org.apache.spark.rdd.RDD[(Int, (String, String))] = MapPartitionsRDD[10] at map at &lt;console&gt;:27scala&gt; rddABMapJoin.collectres13: Array[(Int, (String, String))] = Array((1,(a,A)), (2,(b,B)), (3,(c,C)))scala&gt; rddABMapJoin.toDebugStringres14: String =(4) MapPartitionsRDD[10] at map at &lt;console&gt;:27 [] | ParallelCollectionRDD[5] at parallelize at &lt;console&gt;:24 [] 通过broadcast机制，将原本的两个stage计算减少为1个stage。这里模拟实现了map-side join。 Broadcast VS CacheCache也会把数据分发到各个节点，但是一个节点上通常只有部分分区的数据，而Broadcast会保证每个节点都有完整的数据。Broadcast会消耗更多的内存，但是带来了更好的性能。 RDD AccumulatorsBroadcast机制有个短板，它的变量是只读的，于是Spark提供了Accumulators(累加器)来弥补。 Accumulator的值可以增减，但是不能直接修改为指定值。 scala&gt; val acc=sc.longAccumulatoracc: org.apache.spark.util.LongAccumulator = LongAccumulator(id: 200, name: None, value: 0)scala&gt; rddA.map(_=&gt;acc.add(-1)).countres15: Long = 3scala&gt; acc.valueres17: Long = -3 参考Spark RDDs Simplified Understanding Spark Partitioning Checkpointing Spark内核设计的艺术 转载请注明原文地址：https://liam-blog.ml/2019/10/23/spark-core-rdd/]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>Spark Core</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Scala implicit 隐式转换安全驾驶指南]]></title>
    <url>%2F2019%2F09%2F28%2Fscala-implicit%2F</url>
    <content type="text"><![CDATA[这篇短文将结合实例对隐式转换的各种场景进行解释和总结，希望看完的人能够安全驶过隐式转换这个大坑。 隐式转换函数隐式转换函数有两种作用场景。 1 转换为期望类型：就是指一旦编译器看到X，但需要Y，就会检查从X到Y的隐式转换函数。 2 转换方法的调用者：简单来说，如obj.f()，如果obj对象没有f方法，则尝试将obj转换为拥有f方法的类型。 object ImpFunction extends App &#123; class Dog(val name: String) &#123; def bark(): Unit = println(s"$name say: Wang !") &#125; implicit def double2int(d: Double): Int = d.toInt implicit def string2Dog(s: String): Dog = new Dog(s) val f: Int = 1.1 //转换为期望类型,1.1通过double2int转成了Int类型 println(f) "Teddy".bark() // 转换方法的调用者,字符串通过string2Dog转成了Dog, 于是有了bark方法&#125;// output// 1// Teddy say: Wang ! val f: Int = 1.1 因为类型不匹配，这段本来是无法通过编译的，但是编译器发现存在一个Double至Int的隐式转换函数，所以进行了隐式转换。 &quot;Teddy&quot;.bark() String类型本来是没有bark方法的，但是编译器发现了隐式转换string2Dog可以使得String转成一种拥有bark方法的类型，相当于进行了这样的转换：string2Dog(&quot;Teddy&quot;).bark()。 注意事项需要注意的是，编译器只关心隐式转换函数的输入输出类型，不关心函数名，为避免歧义，同一个作用域中不能有输入输出类型相同的两个隐式转换函数，不然编译器会报错。 隐式类Scala 2.10引入了一种叫做隐式类的新特性。隐式类指的是用implicit关键字修饰的类。使用情况与隐式转换函数类似，可以看做将类的构造函数定义为隐式转换函数，返回类型就是这个类。 package io.github.liam8.implobject ImpClass extends App &#123; implicit class Dog(val name: String) &#123; def bark(): Unit = println(s"$name say: Wang !") &#125; "Teddy".bark()&#125; 注意事项这段来自官网IMPLICIT CLASSES隐式类有以下限制条件： 1 只能在别的trait/类/对象内部定义。 object Helpers &#123; implicit class RichInt(x: Int) // 正确！&#125;implicit class RichDouble(x: Double) // 错误！ 2 构造函数只能携带一个非隐式参数。 implicit class RichDate(date: java.util.Date) // 正确！implicit class Indexer[T](collecton: Seq[T], index: Int) // 错误！implicit class Indexer[T](collecton: Seq[T])(implicit index: Index) // 正确！ 虽然我们可以创建带有多个非隐式参数的隐式类，但这些类无法用于隐式转换。 3 在同一作用域内，不能有任何方法、成员或对象与隐式类同名。注意：这意味着隐式类不能是case class。object Barimplicit class Bar(x: Int) // 错误！val x = 5implicit class x(y: Int) // 错误！implicit case class Baz(x: Int) // 错误！ 隐式参数 &amp; 隐式值package io.github.liam8.implobject ImpParam extends App &#123; def bark(implicit name: String): Unit = println(s"$name say: Wang !") implicit val t: String = "Hot Dog" bark&#125; 参数加上implicit就成了隐式参数，需要与隐式值(变量定义加上implicit)搭配使用，最后一行的bark缺少了一个String类型的参数，编译器找到了String类型的隐式值，便将其传入，相当于执行了bark(t)。 implicit关键字会作用于函数列表中的的所有参数，如def test(implicit x:Int, y: Double)这样定义函数，x和y就都成了隐式函数。但是通常我们只希望部分参数为隐式参数，就好比通常会给部分参数提供默认值而不是全部都指定默认值，于是隐式参数常常与柯里化函数一起使用，这样可以使得只有最后一个参数为隐式参数，例如def test(x: Int)(implicit y: Double)。 👇是完整的例子。 object ImpParamWithCurry extends App &#123; def bark(name: String)(implicit word: String): Unit = println(s"$name say: $word !") implicit val w: String = "Wang" bark("Hot Dog")&#125; 注意事项下面这段来自scala的隐式转换学习总结（详细） 1）当函数没有柯里化时，implicit关键字会作用于函数列表中的的所有参数。 2）隐式参数使用时要么全部不指定，要么全不指定，不能只指定部分。 3）同类型的隐式值只能在作用域内出现一次，即不能在同一个作用域中定义多个相同类型的隐式值。 4）在指定隐式参数时，implicit 关键字只能出现在参数开头。 5）如果想要实现参数的部分隐式参数，只能使用函数的柯里化， 如要实现这种形式的函数，def test(x:Int, implicit y: Double)的形式，必须使用柯里化实现：def test(x: Int)(implicit y: Double). 6）柯里化的函数， implicit 关键字只能作用于最后一个参数。否则，不合法。 7）implicit 关键字在隐式参数中只能出现一次，柯里化的函数也不例外！ 隐式对象类似于隐式值, 要结合隐式参数使用。先看一个栗子(下面的代码需要认真体会)。package io.github.liam8.implobject ImpObject extends App &#123; //定义一个`排序器`接口，能够比较两个相同类型的值的大小 trait Ordering[T] &#123; //如果x&lt;y返回-1，x&gt;y返回1，x==y则返回0. def compare(x: T, y: T): Int &#125; //实现一个Int类型的排序器 implicit object IntOrdering extends Ordering[Int] &#123; override def compare(x: Int, y: Int): Int = &#123; if (x &lt; y) -1 else if (x == y) 0 else 1 &#125; &#125; //实现一个String类型的排序器 implicit object StringOrdering extends Ordering[String] &#123; override def compare(x: String, y: String): Int = x.compareTo(y) &#125; //一个通用的max函数 def max[T](x: T, y: T)(implicit ord: Ordering[T]): T = &#123; if (ord.compare(x, y) &gt;= 0) x else y &#125; println(max(1, 2)) println(max("a", "b"))&#125;//output: // 2// b max函数的作用显然是返回x和y中的最大值，但是x和y的值类型不是固定的，max不知道如何比较x和y的大型，于是定义了一个隐式参数implicit ord: Ordering[T]，希望能传入一个Ordering[T]类型的排序器帮助进行x和y的比较。 在调用max(1, 2)的时候，编译器发现需要一个Ordering[Int]类型的参数，刚好implicit object IntOrdering定义了一个隐式对象符合要求，于是被用来传入max函数。 隐式对象跟上面的隐式值非常相似，只是类型特殊而已。 在Scala中scala.math.Ordering很常用的内置特质，如果你理解了这段代码，也就大致理解了Ordering的原理。 上下文界定(context bounds)这是一种隐式参数的语法糖。 再看上面隐式对象的例子，如果要添加一个min函数，大致就是这样def min[T](x: T, y: T)(implicit ord: Ordering[T]): T = &#123; if (ord.compare(x, y) &gt;= 0) y else x&#125; 但是max和min函数的参数都比较长，于是出现了一种简化的写法def min[T: Ordering](x: T, y: T): T = &#123; val ord = implicitly[Ordering[T]] if (ord.compare(x, y) &gt;= 0) y else x&#125; [T: Ordering]这种语法就叫上下文界定，含义是上下文中必须有一个Ordering[T]类型的隐式值，这个值会被传入min函数。但是由于这个隐式值并没有明确赋值给某个变量，没法直接使用它，所以需要一个implicitly函数把隐式值取出来。 implicitly函数的定义非常简单，作用就是将T类型的隐含值返回：@inline def implicitly[T](implicit e: T) = e 视界这个语法已经被废弃了，但是你还是可能会看到，简单解释下。def min[T &lt;% Ordered[T]](x: T, y: T): T = &#123; if (x &gt; y) y else x&#125; 视界的定义T &lt;% Ordered[T]的含义是T可以被隐式转换成Ordered[T]，这也是为什么x &gt; y可以编译通过。 上面的写法其实等同于下面这样，所以视界的语法不能用了也不要紧。def min[T](x: T, y: T)(implicit c: T =&gt; Ordered[T]): T = &#123; if (x &gt; y) y else x&#125; 隐式转换机制隐式转换通用规则 标记规则：只有标记为implicit的定义才是可用的。 作用域规则：插入的隐式转换必须以单一标识符的形式处于作用域中，或与转换的源或目标类型关联在一起。 单一标识符意思是不能插入形式为someVariable.convert(x)的转换，只能是convert(x)。单一标识符规则有个例外，编译器还将在源类型或转换的期望目标类型的伴生对象中寻找隐式定义。 有点难理解?看个例子!package io.github.liam8.implobject ImpCompObject extends App &#123; object Dog &#123; implicit def dogToCat(d: Dog) = new Cat(d.name) &#125; class Cat(val name: String) &#123; def miao(): Unit = println(s"$name say: Miao !") &#125; class Dog(val name: String) &#123; def bark(): Unit = println(s"$name say: Wang !") &#125; new Dog("Teddy").miao()&#125;//Teddy say: Miao ! 当前作用域中没有定义和引入隐式函数，但是在Dog的伴生对象中找到了，所以Dog可以被转成Cat，这个跟上下文没有关系，而是Dog自带技能。 无歧义规则：隐式转换唯有不存在其他转换的前提下有效。 单一调用规则：只会尝试一个隐式操作。 显示操作先行规则：若编写的代码类型检查无误，则不会尝试隐式操作。 转换时机 当类型与目标类型不一致时 当对象调用类中不存在的方法或成员时 缺少隐式参数时 也即是能用到隐式操作的有三个地方：转换为期望类型、指定（方法）调用者的转换、隐式参数。 转换机制这段来自深入理解Scala的隐式转换 即编译器是如何查找到缺失信息的，解析具有以下两种规则： 1.首先会在当前代码作用域下查找隐式实体（隐式方法 隐式类 隐式对象） 2.如果第一条规则查找隐式实体失败，会继续在隐式参数的类型的作用域里查找类型的作用域是指与该类型相关联的全部伴生模块，一个隐式实体的类型T它的查找范围如下： 1 如果T被定义为T with A with B with C,那么A,B,C都是T的部分，在T的隐式解析过程中，它们的伴生对象都会被搜索 2 如果T是参数化类型，那么类型参数和与类型参数相关联的部分都算作T的部分，比如List[String]的隐式搜索会搜索List的伴生对象和String的伴生对象 3 如果T是一个单例类型p.T，即T是属于某个p对象内，那么这个p对象也会被搜索 4 如果T是个类型注入S#T，那么S和T都会被搜索 上路前的话这段话来自《Scala编程》 隐式操作若过于频繁使用，会让代码变得晦涩难懂。因此，在考虑添加新的隐式转换之前，请首先自问是否能够通过其他手段，诸如继承、混入组合或方法重载，达到同样的目的。如果所有这些都不能成功，并且你感觉代码仍有一些繁复和冗余，那么隐式操作或许正好能帮到你。 所以。。。谨慎使用，小心翻车，good luck! 参考文献IMPLICIT CLASSES scala的隐式转换学习总结（详细） 《Scala编程》 深入理解Scala的隐式转换 本文代码Github仓库 转载请注明原文地址：https://liam-blog.ml/2019/09/28/scala-implicit/]]></content>
      <tags>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Scala并发编程实战 5：Executor线程池]]></title>
    <url>%2F2019%2F09%2F22%2FScala-Concurrency-Executor%2F</url>
    <content type="text"><![CDATA[创建线程是一个重量级操作，因为需要调用操作系统内核的API，所以最好不要频繁的创建和销毁线程，为了能够复用创建的线程，常用的办法的就是创建线程池。 Executorjava.util.concurren包中提供了若干接口和类来实现线程池，最常用的有Executor，ExecutorService，ThreadPoolExecutor。 Executor接口很简单定义如下：public interface Executor &#123; void execute(Runnable command);&#125; 这个接口的目的在于将任务与执行机制解耦，使得用户不需要手动创建线程，只要交给Executor就行了。 ExecutorServiceExecutorService接口则扩展了Executor接口，增加了若干实用的方法，最常用的两个方法：//关闭线程池void shutdown();//提交Callable任务以获取返回值&lt;T&gt; Future&lt;T&gt; submit(Callable&lt;T&gt; task); AbstractExecutorService抽象类是ExecutorService的实现，实现了若干模板方法。 最重要的类莫过于ThreadPoolExecutor，它是最最常用的ExecutorService实现类，下面重点说说。 ThreadPoolExecutorThreadPoolExecutor在构造时可以指定的参数最多有7个，另外还有3个使用一些默认参数的简化版本。public ThreadPoolExecutor(int corePoolSize, int maximumPoolSize, long keepAliveTime, TimeUnit unit, BlockingQueue&lt;Runnable&gt; workQueue, ThreadFactory threadFactory, RejectedExecutionHandler handler) corePoolSize 是保留的核心线程数，即使线程处于空闲也不会被回收，除非设置了allowCoreThreadTimeOut属性。 maximumPoolSize 最大线程数。当workQueue满了，会给新提交的任务创建新线程，这种情况下线程数会超过corePoolSize，但整个线程池的线程数必须有个上限，就是maximumPoolSize了。 keepAliveTime 回收线程前，允许保留空闲线程的时长。 workQueue 存储提交的任务的队列 threadFactory 创建线程的工厂类(ThreadFactory这个接口就定义了一个方法Thread newThread(Runnable r);) handler handler用于没有可用线程（线程数达到最大值，没有空闲线程）且workQueue队列满了的时候。 ThreadPoolExecutor 已经提供了以下 4 种策略。CallerRunsPolicy：提交任务的线程自己去执行该任务。AbortPolicy：默认的拒绝策略，会 throws RejectedExecutionException。DiscardPolicy：直接丢弃任务，没有任何异常抛出。DiscardOldestPolicy：丢弃最老的任务，其实就是把最早进入工作队列的任务丢弃，然后把新任务加入到工作队列。 ThreadPoolExecutory的构造函数一共有四种，使得用户可以省略threadFactory和handler中的一个或两个。 需要注意的情况当maximumPoolSize&gt;corePoolSize时，如果workQueue满了，新提交的任务会被新线程马上执行，而之前提交的在队列中等待的队列则继续等待。也就是说后提交的任务可能先执行了。当新线程执行完新提交的这个任务后，会转去执行队列中的数据，这时消费任务队列的线程数可能会大于corePoolSize，消费速度加快了。下面做个实验。package io.github.liam8.conimport java.util.concurrent.&#123;ArrayBlockingQueue, Callable, Future, RejectedExecutionException, ThreadPoolExecutor, TimeUnit&#125;object ExecutorDemo &#123; def main(args: Array[String]): Unit = &#123; // corePoolSize=1,maximumPoolSize=2,queue capacity=1 val executor = new ThreadPoolExecutor( 1, 2, 10, TimeUnit.SECONDS, new ArrayBlockingQueue[Runnable](1) ) val task1 = new Runnable &#123; override def run(): Unit = &#123; println("task1 running") Thread.sleep(3000) println("task1 complete") &#125; &#125; val task2 = new Runnable &#123; override def run(): Unit = &#123; println("task2 running") Thread.sleep(3000) println("task2 complete") &#125; &#125; val task3 = new Callable[String] &#123; override def call(): String = &#123; println("task3 running") Thread.sleep(3000) println("task3 complete") "xxx" &#125; &#125; val task4 = new Runnable &#123; override def run(): Unit = &#123; println("task4 running") Thread.sleep(3000) println("task4 complete") &#125; &#125; var task2Result: Future[String] = null var taskCount = 1 try &#123; executor.execute(task1) println("task1 submitted") taskCount += 1 executor.execute(task2) println("task2 submitted") taskCount += 1 task2Result = executor.submit(task3) println("task3 submitted") taskCount += 1 executor.execute(task4) println("task4 submitted") &#125; catch &#123; case e: RejectedExecutionException =&gt; println(s"task $taskCount be rejected") &#125; // 起一个线程跟踪线程池大小 val th = new Thread &#123; var threadNum = 0 override def run(): Unit = while (true) &#123; if (executor.getPoolSize != threadNum) &#123; threadNum = executor.getPoolSize println("pool size:" + threadNum) &#125; Thread.sleep(100) &#125; &#125; th.setDaemon(true) th.start() if (task2Result != null) &#123; println(task2Result.get(7, TimeUnit.SECONDS)) &#125; Thread.sleep(5000) executor.shutdown() &#125;&#125; output task1 runningtask1 submittedtask2 submittedtask3 submittedtask3 running //task3在task2之前运行了！task 4 be rejected // 线程数达到最大值，任务队列也满了，task4被拒绝(默认的handler)pool size:2task1 completetask3 completexxxtask2 running // 空闲的线程开始消费队列task2 completepool size:0 ExecutorsExecutors是JUC包中的一个静态工厂类，其中除了newFixedThreadPool，newSingleThreadExecutor方法，其他方法都不推荐使用，因为其他方法创建的线程池使用的是无界队列，可能会占用过多内存，甚至OOM，所以建议使用有界队列。 ExecutionContextScala另外提供了ExecutionContext和Future来简化线程池的使用，Future可以接受一个ExecutionContext类型的隐式参数，将传入的函数提交到ExecutionContext的线程池中运行。下面举个栗子，不做深入探讨。 package io.github.liam8.conimport java.util.concurrent.Executorsimport scala.concurrent.&#123;Await, ExecutionContext, ExecutionContextExecutorService, Future&#125;import scala.concurrent.duration._object ExecutionContextDemo &#123; def main(args: Array[String]): Unit = &#123; val pool = Executors.newFixedThreadPool(2) implicit val ec: ExecutionContextExecutorService = ExecutionContext.fromExecutorService(pool) val f = Future &#123; val t = Thread.currentThread().getName println(s"$t: future is coming") 123 &#125; val re = f.map(r =&gt; &#123; val t = Thread.currentThread().getName println(s"$t: mapping") r * r &#125;) re.onSuccess &#123; case x: Int =&gt; println(x) &#125; Await.result(f, 3.seconds) ec.shutdown() &#125;&#125; outputpool-1-thread-1: future is comingpool-1-thread-2: mapping15129 参考文献Executor与线程池：如何创建正确的线程池？ Futures Made Easy with Scala 本文代码Github仓库 转载请注明原文地址：https://liam-blog.ml/2019/09/22/Scala-Concurrency-Executor/]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Scala并发编程实战 4：Semaphore 信号量模型]]></title>
    <url>%2F2019%2F08%2F26%2FScala-Concurrency-Semaphore%2F</url>
    <content type="text"><![CDATA[Semaphore信号量模型，是一种通过维护计数器数值来控制并发数量的模型，Lock实现的互斥锁只允许一个线程访问临界区，而Semaphore允许有限多个线程访问临界区。 什么情况需要允许多个线程同时访问？最常见的需求就是池化资源，连接池、线程池、对象池等等。 java.util.concurren.Semaphore 是JDK中的实现类，常用方法有这些： public Semaphore(int permits);public void acquire() throws InterruptedException;public void release(); 构造函数可以传入permits，设置计数器的初始值，表示可以同时获取锁的线程数。acquire函数用于获取锁，release相反。 下面利用Semaphore实现一个对象池，里面可以存放任意需要复用的对象，如果存放的是连接对象，就变成了连接池。下面例子往对象池存放了几个字符串对象，再用若干线程同时去申请这个字符串资源。 package io.github.liam8.conimport java.text.SimpleDateFormatimport java.util.Dateimport java.util.concurrent.&#123;ConcurrentLinkedDeque, Semaphore, TimeUnit&#125;import collection.JavaConversions._object SemaphoreDemo &#123; def main(args: Array[String]): Unit = &#123; val pool = new SemaphoreDemo[String](2, List("aaaa", "bbbbb")) val formatDate = (date: Date) =&gt; new SimpleDateFormat("HH:mm:ss.SSS").format(date) for (i &lt;- 1 to 10) &#123; new Thread &#123; override def run(): Unit = &#123; pool.exec &#123; e =&gt; println(s"$&#123;formatDate(new Date)&#125; thread $i using $e") TimeUnit.SECONDS.sleep(3) println(s"$&#123;formatDate(new Date)&#125; thread $i done with $e") &#125; &#125; &#125;.start() &#125; &#125;&#125;class SemaphoreDemo[T](size: Int, items: List[T]) &#123; val pool: ConcurrentLinkedDeque[T] = new ConcurrentLinkedDeque[T](items) val semaphore: Semaphore = new Semaphore(size) def exec(func: T =&gt; Unit): Unit = &#123; semaphore.acquire() val t = pool.pop() try &#123; func(t) &#125; finally &#123; pool.add(t) semaphore.release() &#125; &#125;&#125; output09:56:52.313 thread 4 using bbbbb09:56:52.313 thread 2 using aaaa09:56:55.351 thread 2 done with aaaa09:56:55.351 thread 4 done with bbbbb09:56:55.352 thread 1 using aaaa09:56:55.352 thread 3 using bbbbb09:56:58.353 thread 1 done with aaaa09:56:58.353 thread 3 done with bbbbb09:56:58.353 thread 5 using bbbbb09:56:58.353 thread 6 using aaaa09:57:01.354 thread 6 done with aaaa09:57:01.354 thread 5 done with bbbbb09:57:01.355 thread 7 using aaaa09:57:01.355 thread 8 using bbbbb09:57:04.359 thread 7 done with aaaa09:57:04.359 thread 8 done with bbbbb09:57:04.360 thread 9 using aaaa09:57:04.360 thread 10 using bbbbb09:57:07.363 thread 9 done with aaaa09:57:07.363 thread 10 done with bbbbb 本文代码Github仓库]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Scala并发编程实战 3：Condition 条件变量]]></title>
    <url>%2F2019%2F08%2F03%2FScala-Concurrency-in-Practice-3%2F</url>
    <content type="text"><![CDATA[在Scala并发编程实战：Lock 锁中我们了解到如何通过Lock来实现互斥操作，但是获取锁之后，如果发现条件不满足（如消费一个队列中的数据时，发现队列为空），线程要如何等待条件满足（如队列不为空）并让出锁呢？这需要用到Condition条件变量，又称作条件队列。 Condition与JDK内置的管程的等待队列功能类似，主要功能都是让线程在队列上等待被唤醒，但是内置管程的锁只能有一个对应的队列，而一个Lock锁可以有多个Condition队列。 Condition接口的定义如下： public interface Condition &#123; void await() throws InterruptedException; void awaitUninterruptibly(); long awaitNanos(long nanosTimeout) throws InterruptedException; boolean await(long time, TimeUnit unit) throws InterruptedException; boolean awaitUntil(Date deadline) throws InterruptedException; void signal(); void signalAll();&#125; Condition的await,signal,signalAll方法分别对应Object类的wait,notify,notifyAll方法。 下面这个栗子就是实现一个线程安全的有界队列，队列满的时候，让入队操作的线程wait，队列空的时候就让出队操作的线程wait，当队列情况变化的时候，又能及时唤醒wait状态的线程。队列实现类中创建了两个Condition条件变量notFull和notEmpty。当put元素进入队列时，如果队列已满，则需要等待notFull条件，即在notFull的等待队列等候；当从队列take元素时，如果队列为空，则需要等待notEmpty条件，即在notEmpty的等待队列等候。 object ConditionDemo &#123; def main(args: Array[String]): Unit = &#123; val queue = new BoundedBuffer for (i &lt;- 1 to 10) &#123; new Thread &#123; override def run(): Unit = &#123; queue.put(s"Item$i") &#125; &#125;.start() &#125; for (i &lt;- 1 to 10) &#123; new Thread &#123; override def run(): Unit = &#123; queue.take &#125; &#125;.start() &#125; &#125; class BoundedBuffer &#123; final val lock = new ReentrantLock final val notFull = lock.newCondition final val notEmpty = lock.newCondition final val items = new Array[Any](3) var putptr = 0 var takeptr = 0 var count = 0 def put(x: Any): Unit = &#123; lock.lock() try &#123; while (count == items.length) &#123; notFull.await() &#125; items(putptr) = x putptr += 1 if (putptr == items.length) &#123; putptr = 0 &#125; count += 1 println(s"put $x") notEmpty.signal() &#125; finally &#123; lock.unlock() &#125; &#125; def take: Any = &#123; lock.lock() try &#123; while (count == 0) &#123; notEmpty.await() &#125; val x = items(takeptr) takeptr += 1 if (takeptr == items.length) &#123; takeptr = 0 &#125; count -= 1 println(s"take $x") notFull.signal() x &#125; finally &#123; lock.unlock() &#125; &#125; &#125;&#125; output:put Item8put Item10put Item5take Item8put Item7take Item10put Item9take Item5put Item1take Item7put Item4take Item9put Item2take Item1put Item6take Item4put Item3take Item2take Item6take Item3 本文代码Github仓库]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Scala并发编程实战 2：Lock 锁]]></title>
    <url>%2F2019%2F07%2F21%2FScala-Concurrency-in-Practice-2%2F</url>
    <content type="text"><![CDATA[synchronized作为内置锁，使用简单，不易出错，然鹅确有相当的局限性，例如，无法从等待获取锁的阻塞中中断，无法设置获取锁的超时。所以JUC提供了另一种更灵活的加锁方式，即Lock。 LockLock接口定义如下 public interface Lock &#123; void lock(); void lockInterruptibly() throws InterruptedException; boolean tryLock(); boolean tryLock(long timeout, TimeUnit unit) throws InterruptedException; void unlock(); Condition newCondition(); &#125; 从接口的定义不难发现，Lock不仅提供了常规的lock()阻塞式加锁，也提供了tryLock使得线程能在获取不到锁时，马上返回，甚至可以等待锁一段时间后，再返回。lockInterruptibly则提供了可中断的阻塞式获取锁方式。 Lock的锁需要显示释放，通常要与try...finally语句一起使用，避免死锁。 lock.lock(); try &#123; // update object state // catch exceptions and restore invariants if necessary &#125; finally &#123; lock.unlock(); &#125; ReentrantLockLock最常用的实现类是ReentrantLock，这是一个可重入锁(synchronized也是)。 ReentrantLock默认和内置锁一样，是非公平锁，但是支持公平锁模式，可以用ReentrantLock(true)创建公平锁。 可重入锁所谓可重入锁，也就是说一个线程可以在持有该锁的时候，再次获取该锁。可重入锁通常与一个计数器关联，第一次获取锁的时候，计数器从0变为1，再次获取锁，变为2，以此类推。释放锁的时候，计数器每次减1，直至减为0，该锁才真正释放给其他线程。为啥需要可重入锁举个例子(JCP书上的)public class Widget &#123; public synchronized void doSomething() &#123; ... &#125; &#125; public class LoggingWidget extends Widget &#123; public synchronized void doSomething() &#123; System.out.println(toString() + ": calling doSomething"); super.doSomething(); &#125; &#125; 子类覆盖了父类方法，并再次调用了父类的同步方法，如果锁不支持重入，则会导致死锁。 公平锁所谓公平锁，其实就是指锁的等待队列执行先进先出，等待最久的线程优先获得锁。但是内置锁和ReentrantLock默认都是非公平的，为啥？因为非公平锁的性能更好。一个事实是，一个线程从被唤醒到真正运行中间有不可忽视的延迟，这个延迟时间很可能长到足够一个运行中的线程获取锁，并完成操作，然后释放锁。也即是说，把锁给’等待最久的线程‘的过程中，可以让其他线程插队获取锁，并归还锁，还不会影响’等待最久的线程‘的运行。这样一来吞吐量就得到了提升。 Scala栗子package io.github.liam8.conimport java.util.concurrent.TimeUnitimport java.util.concurrent.locks.&#123;Lock, ReentrantLock&#125;object LockDemo &#123; private val rtl: Lock = new ReentrantLock() var inc: Int = 0 def get(): Int = &#123; rtl.lock() try &#123; inc &#125; finally &#123; rtl.unlock() &#125; &#125; def addOne(): Unit = &#123; rtl.lock() try &#123; TimeUnit.SECONDS.sleep(1) inc = 1 + get() &#125; finally &#123; rtl.unlock() &#125; &#125; def main(args: Array[String]): Unit = &#123; for (i &lt;- 1 to 10) &#123; new Thread &#123; override def run(): Unit = &#123; println(s"run thread $i") addOne() &#125; &#125;.start() &#125; while (true) &#123; println(s"inc=$inc") TimeUnit.SECONDS.sleep(1) &#125; &#125;&#125; outputrun thread 3run thread 8run thread 1run thread 9run thread 7run thread 4run thread 5run thread 2run thread 10run thread 6inc=0inc=0inc=2inc=3inc=4inc=5inc=6inc=7inc=8inc=8inc=10inc=10inc=10 本文代码Github仓库 转载请注明原文地址：https://liam-blog.ml/2019/07/21/Scala-Concurrency-in-Practice-2/]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Scala并发编程实战 1：Monitor与synchronized]]></title>
    <url>%2F2019%2F07%2F14%2FScala-Concurrency-in-Practice-1%2F</url>
    <content type="text"><![CDATA[Java并发编程最常用和易用的技术莫过于synchronized关键字，而Scala的并发编程之旅也可以从synchronized开始。而synchronized的背后其实是monitor技术。 什么是MonitorMonitor是解决并发编程问题的一种常用技术，可以有效解决互斥和同步两大常见问题，通常翻译为‘监视器’或‘管程’。个人认为‘管程‘更能表达monitor的含义，指的是管理共享变量以及对共享变量的操作过程，让他们支持并发。 Scala的synchronizedSynchronized是Java对monitor的实现，可以对代码块或方法使用，使得每次只能有一个线程访问，实现了线程互斥。当一个线程获取了锁，其他线程将在队列上等待，实现了线程同步。 Scala延用了这一关键字，但是语法有所不同。 //用于代码块obj.synchronized &#123; ...&#125;//用于方法def func(): Unit = this.synchronized &#123; ...&#125; 跟Java一样，这里的this是可以省略的，因为默认加锁的对象就是this，但是不建议省略。 Scala实例import java.util.concurrent.TimeUnitobject SynchronizedDemo &#123; private var inc: Int = 0 def addOne(): Unit = this.synchronized &#123; TimeUnit.SECONDS.sleep(1) inc += 1 &#125; def main(args: Array[String]): Unit = &#123; for (i &lt;- 1 to 10) &#123; new Thread &#123; override def run(): Unit = &#123; println(s"run thread with object method $i") addOne() &#125; &#125;.start() &#125; val instance = new SynchronizedDemo for (i &lt;- 1 to 10) &#123; new Thread &#123; override def run(): Unit = &#123; println(s"run thread with class method $i") instance.addOne() &#125; &#125;.start() &#125; while (true) &#123; println(s"object inc=$inc, class inc=$&#123;instance.inc&#125;") TimeUnit.SECONDS.sleep(1) &#125; &#125;&#125;class SynchronizedDemo &#123; private var inc: Int = 0 def addOne(): Unit = this.synchronized &#123; TimeUnit.SECONDS.sleep(1) inc += 1 &#125;&#125; 程序输出 run thread with class method 7run thread with class method 4run thread with object method 8run thread with object method 7run thread with class method 10run thread with class method 8run thread with class method 9run thread with object method 5run thread with object method 3run thread with object method 2run thread with object method 4run thread with object method 10run thread with object method 9run thread with class method 5run thread with class method 3object inc=0, class inc=0run thread with object method 1run thread with class method 6run thread with class method 1run thread with class method 2run thread with object method 6object inc=1, class inc=1object inc=2, class inc=2object inc=3, class inc=2object inc=4, class inc=4object inc=5, class inc=5object inc=6, class inc=6object inc=7, class inc=7object inc=8, class inc=8object inc=9, class inc=9object inc=10, class inc=10 解析 在object SynchronizedDemo和class SynchronizedDemo中均定义了一个inc变量和一个addOne方法，addOne方法的作用就是将inc加1。 main方法中，分别创建10个线程调用addOne方法，对inc进行10次加1操作。 因为inc变量不是线程安全的，所以对addOne方法加上synchronized关键字，使得修改操作是线程安全的。这样才能保证inc会从1加到10。 object和class中的this并不相同，object中的this指向的是名为SynchronizedDemo的object对象，class中的则是该class实例化后的对象。(Scala中没有静态类和静态方法，object SynchronizedDemo实际上是创建名为SynchronizedDemo的单例对象) 如果把class中定义的addOne改成如下：def addOne(): Unit = SynchronizedDemo.synchronized &#123; TimeUnit.SECONDS.sleep(1) inc += 1&#125; 两处定义的addOne方法就会互斥，输出就会变成如下： run thread with object method 2run thread with object method 1run thread with object method 3run thread with object method 4run thread with object method 5run thread with object method 6run thread with object method 7run thread with object method 8run thread with object method 9run thread with object method 10run thread with class method 1run thread with class method 2run thread with class method 3run thread with class method 4run thread with class method 5run thread with class method 6run thread with class method 7run thread with class method 8run thread with class method 9run thread with class method 10object inc=0, class inc=0object inc=1, class inc=0object inc=1, class inc=1object inc=1, class inc=2object inc=1, class inc=3object inc=1, class inc=4object inc=1, class inc=5object inc=1, class inc=6object inc=1, class inc=7object inc=1, class inc=8object inc=1, class inc=9object inc=1, class inc=10object inc=2, class inc=10object inc=3, class inc=10object inc=4, class inc=10object inc=5, class inc=10object inc=6, class inc=10object inc=7, class inc=10object inc=8, class inc=10object inc=9, class inc=10object inc=10, class inc=10 本文代码Github仓库 转载请注明原文地址：https://liam-blog.ml/2019/07/14/Scala-Concurrency-in-Practice-1/]]></content>
      <tags>
        <tag>Scala</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Maven用户都应该知道的一些事：关于依赖的常见问题]]></title>
    <url>%2F2019%2F06%2F20%2Fmaven-details-about-dependency%2F</url>
    <content type="text"><![CDATA[依赖范围(scope)不同选项的区别依赖范围参数的作用是控制依赖在不同阶段与classpath的关系，具体区别如下图所示。 表中没有列出的值是import，这个选项是用于引入dependencyManagement，下文会有介绍。 依赖调解，调解同一依赖的不同版本假设你的项目有如下的依赖树POM|-- A| `-- B 1.0|-- C| `-- D| `-- B 2.0`-- E `-- B 3.0 POM同时依赖了B的1.0和2.0版本，可Maven是不会重复引入相同坐标的依赖的，那么究竟哪个版本会生效呢？ Maven对于依赖的调解遵循两个基本原则： 1 依赖路径长度短者优先； 2 如果依赖路径长度相同，则后声明优先。 所以根据1，2.0版本被排除，根据2，3.0版本被实际引入。 如果你自己明确知道该引入哪个版本的B，那么直接在POM中声明B依赖就好了，因为这时的依赖路径是最短了。 可选依赖(optional)含义可选依赖的作用就是声明该依赖不被传递依赖。POM`-- A `-- B(optional) 这里B不会被引入。 Super POM(Project Object Model)Super POM是Maven自带的全局POM文件，所有的POM文件都默认继承了Super POM。其中定义了各种默认配置，以简化POM文件的编写。下面是Maven 3.5.4的Super POM的核心部分。&lt;project&gt; &lt;modelVersion&gt;4.0.0&lt;/modelVersion&gt; &lt;repositories&gt; &lt;repository&gt; &lt;id&gt;central&lt;/id&gt; &lt;name&gt;Central Repository&lt;/name&gt; &lt;url&gt;https://repo.maven.apache.org/maven2&lt;/url&gt; &lt;layout&gt;default&lt;/layout&gt; &lt;snapshots&gt; &lt;enabled&gt;false&lt;/enabled&gt; &lt;/snapshots&gt; &lt;/repository&gt; &lt;/repositories&gt; &lt;pluginRepositories&gt; &lt;pluginRepository&gt; &lt;id&gt;central&lt;/id&gt; &lt;name&gt;Central Repository&lt;/name&gt; &lt;url&gt;https://repo.maven.apache.org/maven2&lt;/url&gt; &lt;layout&gt;default&lt;/layout&gt; &lt;snapshots&gt; &lt;enabled&gt;false&lt;/enabled&gt; &lt;/snapshots&gt; &lt;releases&gt; &lt;updatePolicy&gt;never&lt;/updatePolicy&gt; &lt;/releases&gt; &lt;/pluginRepository&gt; &lt;/pluginRepositories&gt; &lt;build&gt; &lt;directory&gt;$&#123;project.basedir&#125;/target&lt;/directory&gt; &lt;outputDirectory&gt;$&#123;project.build.directory&#125;/classes&lt;/outputDirectory&gt; &lt;finalName&gt;$&#123;project.artifactId&#125;-$&#123;project.version&#125;&lt;/finalName&gt; &lt;testOutputDirectory&gt;$&#123;project.build.directory&#125;/test-classes&lt;/testOutputDirectory&gt; &lt;sourceDirectory&gt;$&#123;project.basedir&#125;/src/main/java&lt;/sourceDirectory&gt; &lt;scriptSourceDirectory&gt;$&#123;project.basedir&#125;/src/main/scripts&lt;/scriptSourceDirectory&gt; &lt;testSourceDirectory&gt;$&#123;project.basedir&#125;/src/test/java&lt;/testSourceDirectory&gt; &lt;resources&gt; &lt;resource&gt; &lt;directory&gt;$&#123;project.basedir&#125;/src/main/resources&lt;/directory&gt; &lt;/resource&gt; &lt;/resources&gt; &lt;testResources&gt; &lt;testResource&gt; &lt;directory&gt;$&#123;project.basedir&#125;/src/test/resources&lt;/directory&gt; &lt;/testResource&gt; &lt;/testResources&gt; ... Super POM中定义了中央依赖仓库、中央插件仓库，以及各种文件夹的默认路径。 POM继承如果你的项目有多个模块，通常每个模块之间会有一些相同的公共的依赖，你可以把依赖声明在模块各自的POM中，如下所示。.|-- mod A| `-- pom.xml| `-- P(1.0)`-- mod B `-- pom.xml `-- P(1.0) 这样A模块和B模块都依赖了P，但是如果有天你想要修改P的版本，又希望A,B模块依赖的P版本相同，那就得同时修改POM-A和POM-B，太不优雅了。 所以Maven提供了POM继承功能，让我们可以吧公共的依赖抽取出来。 .|-- pom.xml (父POM)| `-- P(1.0)|-- mod A| `-- pom.xml`-- mod B `-- pom.xml 做法如上，在父POM中声明依赖P，在AB模块的POM中声明对父POM的继承，便能实现AB模块对P依赖的引入。 AB模块中加入下面一段，便能实现对父POM的继承。&lt;parent&gt; &lt;groupId&gt;com.mycompany.app&lt;/groupId&gt; &lt;artifactId&gt;my-app&lt;/artifactId&gt; &lt;version&gt;1&lt;/version&gt; &lt;relativePath&gt;../pom.xml&lt;/relativePath&gt;&lt;/parent&gt; relativePath的默认值即是‘../pom.xml’，可以省略。 值得一提的是，这里父POM并不需要知道子POM的信息。 总之，有了继承，子模块就能拥有父模块同样的依赖。 POM聚合假设你的项目有多个模块，通常每个模块需要各自单独构建，如果想要所有模块能同时构建，则需要使用Maven的聚合功能。 使用聚合同样需要建一个父POM.|-- pom.xml (父POM)|-- A| `-- pom.xml`-- B `-- pom.xml 父POM内容如下：&lt;project&gt; &lt;modelVersion&gt;4.0.0&lt;/modelVersion&gt; &lt;groupId&gt;com.mycompany.app&lt;/groupId&gt; &lt;artifactId&gt;my-app&lt;/artifactId&gt; &lt;version&gt;1&lt;/version&gt; &lt;packaging&gt;pom&lt;/packaging&gt; &lt;modules&gt; &lt;module&gt;A&lt;/module&gt; &lt;module&gt;B&lt;/module&gt; &lt;/modules&gt;&lt;/project&gt; 其中packaging必须是pom，modules中声明需要聚合的子模块。与继承相反，子POM中不需要父POM的信息。 总之，有了聚合，所有对父模块执行的Maven命令，同样会对子模块执行。 依赖管理(dependencyManagement)是什么在前面讲继承的时候，我们通过父POM使得AB两个模块都拥有了依赖P，但是如果我们现在添加一个模块C，模块C并不需要依赖P，只需要父POM中的其他配置和依赖，该怎么办呢？ 这就要用到dependencyManagement这个配置了，dependencyManagement与dependencies元素不同在于并不会真的引入依赖，只是指定依赖的版本。 做法是在父POM中添加dependencyManagement配置&lt;dependencyManagement&gt; &lt;dependencies&gt; &lt;dependency&gt; &lt;groupId&gt;groupabc&lt;/groupId&gt; &lt;artifactId&gt;p&lt;/artifactId&gt; &lt;version&gt;1.0&lt;/version&gt; &lt;/dependency&gt; &lt;/dependencyManagement&gt; 然后在AB模块的POM中添加如下，注意不含版本信息。&lt;dependencies&gt; &lt;dependency&gt; &lt;groupId&gt;groupabc&lt;/groupId&gt; &lt;artifactId&gt;p&lt;/artifactId&gt; &lt;/dependency&gt;&lt;/dependencies&gt; 如此一来，AB模块都引入了依赖p的v1.0，而模块C没有引入依赖p。 另外还有pluginManagement与此类似，不过是针对插件而已。 参考文献Introduction to the POM Introduction to the Dependency Mechanism 《Maven实战》 转载请保留原文地址：Maven用户都应该知道的一些事：关于依赖的常见问题]]></content>
      <tags>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Maven用户都应该知道的一些事：构建生命周期和插件]]></title>
    <url>%2F2019%2F06%2F16%2Fmaven-lifecycle-and-plugin-goal%2F</url>
    <content type="text"><![CDATA[Maven的所有实际操作都是由插件完成的，如果没有插件，Maven什么都不会干。（即时你没有在POM中配置&lt;plugin&gt;元素，Super POM中也已经帮你引入了若干核心插件） 那么问题来了，项目构建过程中，Maven是怎么知道应该在什么时候调用哪个插件的呢？ 插件的调用时机，跟’生命周期’和’插件目标’有很大关系。 插件目标(Plugin Goal)是个什么鬼首先需要知道的是，Maven的所有具体任务都是交由插件实现的，而一个插件往往能实现若干个任务或功能。又或者可以说一个插件能实现若干个目标，比如编译源码、执行测试用例、打包项目等等。简单来说，在Maven中Plugin Goal可以看做是插件的一个功能。 Plugin Goal通常用‘插件前缀:插件目标‘的格式描述，常见的有：compiler:compile,surefire:test,dependency:tree等。 所谓插件前缀就是插件的名称的一个简写，比如compiler指的就是maven-complier-plugin这个插件。compiler:compile的意思既是maven-complier-plugin插件的compile目标。 构建生命周期(Build Lifecycle)又是什么构建生命周期是对构建过程的抽象和统一，包括了几乎所有的构建步骤，如清理、初始化、编译、测试、打包等等。 Maven为不用的目的定义了3套相互独立的生命周期： clean 用于清理项目 default 用于构建项目 site 用于构建项目站点 每种生命周期都是由若干阶段(phase)组成，如clean生命周期由pre-clean,clean,post-clean三个阶段组成。 阶段之间是有序的，而且后面的阶段依赖前面的阶段。用户执行mvn clean时，实际上maven会先执行pre-clean阶段，然后再执行clean阶段；执行mvn post-clean时，则会依次执行pre-clean,clean,post-clean三个阶段。 3个生命周期各自的阶段如下： Clean Lifecycle Phase Description pre-clean execute processes needed prior to the actual project cleaning clean remove all files generated by the previous build post-clean execute processes needed to finalize the project cleaning Default Lifecycle Phase Description validate validate the project is correct and all necessary information is available. initialize initialize build state, e.g. set properties or create directories. generate-sources generate any source code for inclusion in compilation. process-sources process the source code, for example to filter any values. generate-resources generate resources for inclusion in the package. process-resources copy and process the resources into the destination directory, ready for packaging. compile compile the source code of the project. process-classes post-process the generated files from compilation, for example to do bytecode enhancement on Java classes. generate-test-sources generate any test source code for inclusion in compilation. process-test-sources process the test source code, for example to filter any values. generate-test-resources create resources for testing. process-test-resources copy and process the resources into the test destination directory. test-compile compile the test source code into the test destination directory process-test-classes post-process the generated files from test compilation, for example to do bytecode enhancement on Java classes. For Maven 2.0.5 and above. test run tests using a suitable unit testing framework. These tests should not require the code be packaged or deployed. prepare-package perform any operations necessary to prepare a package before the actual packaging. This often results in an unpacked, processed version of the package. (Maven 2.1 and above) package take the compiled code and package it in its distributable format, such as a JAR. pre-integration-test perform actions required before integration tests are executed. This may involve things such as setting up the required environment. integration-test process and deploy the package if necessary into an environment where integration tests can be run. post-integration-test perform actions required after integration tests have been executed. This may including cleaning up the environment. verify run any checks to verify the package is valid and meets quality criteria. install install the package into the local repository, for use as a dependency in other projects locally. deploy done in an integration or release environment, copies the final package to the remote repository for sharing with other developers and projects. Site Lifecycle Phase Description pre-site execute processes needed prior to the actual project site generation site generate the project’s site documentation post-site execute processes needed to finalize the site generation, and to prepare for site deployment site-deploy deploy the generated site documentation to the specified web server 插件目标与生命周期的绑定插件目标是与生命周期的某个阶段绑定的，比如maven-clean-plugin的clean目标与clean阶段绑定，也就意味着当maven执行clean生命周期时，到达clean阶段的时候，会调用maven-clean-plugin的clean目标(删除项目输出目录)。 也就是说，插件目标的执行顺序是由其绑定到的生命周期阶段的顺序决定的，如果多个插件目标绑定到同一个阶段，那么先声明的插件会在后声明的插件之前执行。 对于一些核心插件，Maven已经内置默认将一些插件目标绑定到了生命周期阶段，特别是default生命周期，对于不同的打包类型（jar,war,pom等），Maven提供了不同的默认绑定。如下所示： Clean Lifecycle Bindings Phase plugin:goal clean clean:clean Default Lifecycle Bindings - Packaging ejb / ejb3 / jar / par / rar / war Phase plugin:goal process-resources resources:resources compile compiler:compile process-test-resources resources:testResources test-compile compiler:testCompile test surefire:test package ejb:ejb or ejb3:ejb3 or jar:jar or par:par or rar:rar or war:war install install:install deploy deploy:deploy Default Lifecycle Bindings - Packaging ear Phase plugin:goal generate-resources ear:generate-application-xml process-resources resources:resources package ear:ear install install:install deploy deploy:deploy Default Lifecycle Bindings - Packaging maven-plugin Phase plugin:goal generate-resources plugin:descriptor process-resources resources:resources compile compiler:compile process-test-resources resources:testResources test-compile compiler:testCompile test surefire:test package jar:jar and plugin:addPluginArtifactMetadata install install:install deploy deploy:deploy Default Lifecycle Bindings - Packaging pom Phase plugin:goal package install install:install deploy deploy:deploy Site Lifecycle Bindings Phase plugin:goal site site:site site-deploy site:deploy 参考文献 Introduction to the Build Lifecycle 《Maven实战》 转载请保留原文地址：Maven用户都应该知道的一些事：构建生命周期和插件]]></content>
      <tags>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hive窗口函数实战]]></title>
    <url>%2F2019%2F02%2F20%2Fhive-window-sql%2F</url>
    <content type="text"><![CDATA[本文将介绍使用Hive强大的窗口函数，解决实际问题的方法，仅供参考。 连续登陆/活跃/访问问题假设有一张登陆日志表：CREATE TABLE user_login ( uid int comment '用户ID', dt int comment '日期yyyymmdd') STORED AS PARQUET PS：如果用户在某天多次登陆，也只会有一条记录。 计算距离上次登录天数先来个最简单的案例, 要算出用户每次登陆，距离上次有多少天。 SELECT *,dt - lag(dt,1) OVER (PARTITION BY uid ORDER BY dt)from user_login lag 函数是取上N行的指定列的数据，N默认是1，所以也可以写成lag(dt)。 计算连续登录天数计算用户连续登陆了多少天，一段时间内可能会有多次连续登陆。 SELECT uid, max(dt)-min(dt) diff, collect_set(dt)from( SELECT a.uid, a.dt, dt - rn num from ( SELECT uid, dt, row_number() over ( PARTITION BY uid ORDER BY dt ) rn FROM user_login ) a) bGROUP BY uid,num 这里需要理解的关键点是’dt - rn’得到num这个计算，也就是将日期减去行号，这是个取巧的算法，看起来有点奇怪。 因为row_number()是按dt正序的，所以连续的dt减去行号，会得到同一个值，而非连续的则不会。举个例子： uid dt row_number dt - row_number (num) u1 20190203 1 20190202 u1 20190204 2 20190202 u1 20190206 3 20190203 这里其实就是子查询b的结果，理解了这个，最外层的语句就很好理解了。 标记用户连续间隔不超过3天的登录标记用户连续的活跃记录，只要不超过3天间隔，就算连续活跃。 假设有记录如下： uid dt u1 20190203 u1 20190204 u1 20190206 u1 20190210 u1 20190211 这里需求就是要把前3行和后2行区分出来，因为20190206和20190210之间隔了4天。 SELECT *,max(flag) OVER (PARTITION BY uid ORDER BY dt ROWS BETWEEN UNBOUNDED PRECEDING and CURRENT ROW) session_idfrom ( select uid,dt,if(dt-lag(dt)&gt;3,dt,0) OVER (PARTITION BY uid ORDER BY dt) flag from user_login) t 这里子查询t就是先标记处间隔大于3的记录。 uid dt flag u1 20190203 0 u1 20190204 0 u1 20190206 0 u1 20190210 20190210 u1 20190211 0 最外层的查询，通过窗口函数，从当前行往上找出最大的flag，因为按dt排序，得到的其实就是最近的flag，将该flag设置为自己的session_id，表示所属同一批连续活跃的记录。得到： uid dt flag session_id u1 20190203 0 0 u1 20190204 0 0 u1 20190206 0 0 u1 20190210 20190210 20190210 u1 20190211 0 20190210 现在前3行和后2行，就被标记为不同的组了。 如果我们有的是精确到分、秒的日志，这个解决方案可以用来给用户的访问记录打上会话标记，然后拿来计算单次访问时长，访问深度等等，只是这里的dt改成时间戳而已。 计算会话标记其实是数仓同事提的需求，也是我想出这个sql方案的初衷。 希望能帮到需要的人。^-^]]></content>
      <tags>
        <tag>hive</tag>
        <tag>sql</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[[读书笔记] 黑客与画家]]></title>
    <url>%2F2019%2F01%2F19%2FHackers-and-Painters-note%2F</url>
    <content type="text"><![CDATA[简介《黑客与画家》是硅谷创业之父Paul Graham的文集，这里的黑客指的是优秀的软件工程师，作者将黑客与画家作比较，认为与黑客最接近的职业是画家，这便是这本书书名的由来了。Paul Graham在1995年创办了Viaweb，帮助个人用户方便的开设网上商店，1998年以5千万美元的价格卖给Yahoo公司。2005年他创建了风投公司YC，被称为全世界最牛的创业孵化器，YC帮助了大量的创业公司（到2018年投资了1450家公司），比较成功的案例有：Airbnb，Stripe，Dropbox，Docker，GitLab等等。 这本书中包含了作者对黑客精神，创业，互联网，社会，编程语言的诸多观点，读完让人感觉受益匪浅，甚至会有创业的冲动，哈哈。 以下是对这本书内容的一些摘抄。 序 &amp; 前言黑客行为必须包含三个特点：好玩、高智商、探索精神。 1 为什么书呆子不受欢迎我认为，这就是问题的根源。“书呆子”的目标具有两重性。他们毫无疑问想让自己受欢迎，但是他们更愿意让自己聪明。“受欢迎”并不是你在课后时间随便做一做就能实现的，尤其是在美国的中学中，在这里，所有人为了个人魅力都会进行激烈竞争。 表面上，学校的使命是教育儿童。事实上，学校的真正目的是把儿童都关在同一个地方，以便大人们白天可以腾出手来把事情做完。 校园生活的两大恐怖之处——残忍和无聊——也是出于同样的原因。 如果你觉得人生糟透了，那不是因为体内激素分泌失调（你父母相信这种说法），也不是因为人生真的糟透了（你本人相信这种说法）。那是因为你对成年人不再具有经济价值（与工业社会以前的时期相比），所以他们把你扔在学校里，一关就是好几年，根本没有真正的事情可做。任何这种类型的组织都是可怕的生存环境。你根本不需要寻找其他的原因就能解释为什么青少年是不快乐的。 2 黑客与画家这个学科的一端是纯粹的数学家，他们自称“计算机科学家”，只是为了得到国防部研究局（DARPA）的项目资助。中间部分是计算机博物学家，研究各种专门性的题目，比如网络数据的路由算法。另一端则是黑客，只想写出有趣的软件，对于他们来说，计算机只是一种表达的媒介，就像建筑师手里的混凝土，或者画家手里的颜料。 创造优美事物的方式往往不是从头做起，而是在现有成果的基础上做一些小小的调整，或者将已有的观点用比较新的方式组合起来。这种类型的工作很难用研究性的论文表达。 黑客搞懂“计算理论”（theory of computation）的必要性，与画家搞懂颜料化学成分的必要性差不多大。 编程语言是用来帮助思考程序的，而不是用来表达你已经想好的程序。它应该是一支铅笔，而不是一支钢笔。 这似乎是大公司的普遍情况。大公司这样安排的原因是为了减少结果的标准差。因为实际上只有很少一部分黑客懂得如何正确设计软件， 真正竞争软件设计的战场是新兴领域的市场，这里还没有人建立过防御工事。只要你能做出大胆的设计，由一个人或一批人同时负责设计和实现产品，你就能在这里战胜大公司 黑客如何才能做自己喜欢的事情？我认为这个问题的解决方法是一个几乎所有创作者都知道的方法：找一份养家糊口的“白天工作”（day job）。这个词是从音乐家身上来的，他们晚上表演音乐，所以白天可以找一份其他工作。更一般地说，“白天工作”的意思是，你有一份为了赚钱的工作，还有一份为了爱好的工作。黑客解决生计问题的方法是找一份“白天工作”，然后在其余时间开发优美的软件， 因为黑客更像创作者，而不是科学家，所以要了解黑客，不应该在科学家身上寻找启示，而是应该观察其他类型的创作者。 也许对于黑客来说，采取像画家这样的做法很有好处：应该定期地从头开始，而不要长年累月地在一个项目上不断工作，并且试图把所有的最新想法都以修订版的形式包括进去。 创作者另一个学习的途径是通过范例。 黑客就像画家，工作起来是有心理周期的。有时候，你有了一个令人兴奋的新项目，你会愿意为它一天工作16个小时。等过了这一阵，你又会觉得百无聊赖，对所有事情都提不起兴趣。 普通黑客与优秀黑客的所有区别之中，会不会“换位思考”可能是最重要的单个因素。有些黑客很聪明，但是完全以自我为中心，根本不会设身处地为用户考虑。这样的人很难设计出优秀软件，因为他们不从用户的角度看待问题。 3 不能说的话如果别人告诉你应该相信什么，你就真的相信了，那么你就会和别人一样犯下同样的错误。 ，并不是所有不能说出口的话都是我们要找的答案。实际上，只有同时满足两个条件才行。第一个条件是，这些话不能说出口；第二个条件是，它们是正确的，或者看起来很可能正确，值得进一步讨论。 守口如瓶”的真正缺点在于，你从此无法享受讨论带来的好处了。讨论一个观点会产生更多的观点，不讨论就什么观点也没有。 4 良好的坏习惯公民自由真的是国家富强的原因，而不是结果吗？我认为是的。在我看来，一个人们拥有言论自由和行动自由的社会，往往最有可能采纳最优方案，而不是采纳最有权势的人提出的方案。 正是那些不服从管教的人们，才是美国财富与力量的源泉。 5 另一条路如果你想把钱藏在安全的地方，请问你是选择放在家中床垫下面，还是放在银行？这个比喻对服务器管理的方方面面都适用，不仅是安全性，还包括正常运行时间、带宽、负载管理、备份等，都是我们占优。 不少公司都很想知道，什么事情可以外包，什么事情不可以外包。一个可能的答案是，公司内部所有不直接感受到竞争压力的部门都应该外包出去，让它们暴露在竞争压力之下。 只有懂得设计的黑客，才能设计软件，不能交给对软件一知半解的设计师。如果你不打算自己动手设计和开发，那就不要创业。 6 如何创造财富从经济学观点看，你可以把创业想象成一个压缩过程，你的所有工作年份被压缩成了短短几年。你不再是低强度地工作四十年，而是以极限强度工作四年。在高技术领域，这种压缩的回报尤其丰厚，工作效率越高，额外报酬就越高。 真正重要的是做出人们需要的东西，而不是加入某个公司。 要致富，你需要两样东西：可测量性和可放大性。 乔布斯曾经说过，创业的成败取决于最早加入公司的那十个人。我基本同意这个观点，虽然我觉得真正决定成败的其实只是前五人。小团队的优势不在于它本身的小，而在于你可以选择成员。我们不需要小村庄的那种“小”，而需要全明星第一阵容的那种“小”。 选择公司要解决什么问题应该以问题的难度作为指引，而且此后的各种决策都应该以此为原则。Viaweb的一个经验法则就是“更上一层楼”。总的来说，这也是很好的处事原则。如果你有两个选择，就选较难的那个。如果你要选择是坐在家里看电视，还是外出跑步，那就出去跑步吧。这个方法有效的原因可能是遇到两个一难一易的选择时，往往出于懒惰的缘故，你会选择较易的那个选项。在意识深处，你其实知道不懒惰的做法会带来更好的结果，这个方法只是迫使你接受这一点。 7 关注贫富分化无论在物质上，还是在社会地位上，技术好像都缩小了富人与穷人之间的差距，而不是让这种差距扩大了。 9 设计者的品味等到你逐渐对一件事产生热情的时候，就不会满足于模仿了。你的品味就进入了第二阶段，开始自觉地进行原创。 10 编程语言解析头重脚轻”的语言：它们的内核设计得并非很好，但是却有着无数强大的函数库，可以用来解决特定的问题。 11 百年后的编程语言这有点像买房子的时候你应该先考虑地理位置。别的地方将来出问题都有办法弥补，但是地理位置是没法变的。 浪费程序员的时间而不是浪费机器的时间才是真正的无效率。随着计算机速度越来越快，这会变得越来越明显。 13 书呆子的复仇在大型组织内部，有一个专门的术语描述这种跟随大多数人的选择的做法，叫做“业界最佳实践”。这个词出现的原因其实就是为了让你的经理可以推卸责任。既然我选择的是“业界最佳实践”，如果不成功，项目失败了，那么你也无法指责我，因为做出选择的人不是我，而是整个“业界”。 第一，不同语言的编程能力不一样。第二，大多数经理故意忽视第一点。你把这两点事实结合起来，其实就得到了赚钱的诀窍 14 梦寐以求的编程语言让我们试着描述黑客心目中梦寐以求的语言来为以上内容做个小结。这种语言干净简练，具有最高层次的抽象和互动性，而且很容易装备，可以只用很少的代码就解决常见的问题。不管是什么程序，你真正要写的代码几乎都与你自己的特定设置有关，其他具有普遍性的问题都有现成的函数库可以调用。 15 设计与研究其中有一点是正确的，那就是如果你正在设计某种新东西，就应该尽快拿出原型，听取用户的意见。 原型（prototype）并不只是模型（model），不等于将来一定要另起炉灶，你完全能够在原型的基础上直接做出最后的成品。我认为，只要有可能，你就应该这样做。 为了把产品设计好，你必须对自己说：“哇，这个产品太棒了，我一定要设计好！”而不是心想：“这种垃圾玩意，只有傻瓜才会喜欢，随便设计一下就行了。”设计意味着做出符合人类特点和需要的产品。但是，“人类”不仅包括用户，还包括设计师，所以设计工作本身也必须符合设计师的特点和需要。]]></content>
      <tags>
        <tag>book</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark 2.x读写MySQL]]></title>
    <url>%2F2018%2F12%2F25%2Fspark-work-with-mysql-cn%2F</url>
    <content type="text"><![CDATA[简介从 spark 2.0 开始，我们可以使用DataFrameReader 和 DataFrameWriter来读写MySQL。 SparkSession.read 返回 DataFrameReader.Dataset.write 返回 DataFrameWriter. 煮几个栗子其中spark是SparkSession read exampleval prop=new java.util.Properties()prop.put("user","username")prop.put("password","yourpassword")val url="jdbc:mysql://host:3306/db_name"val df=spark.read.jdbc(url,"table_name",prop)df.show() read example2val jdbcDF = spark.read .format("jdbc") .option("url", "jdbc:mysql:dbserver") .option("dbtable", "schema.tablename") .option("user", "username") .option("password", "password") .load() read example3如果你想读取的不是一张表的数据，而是一句SQL的查询结果，那么可以这么操作：val sql="""select * from db.your_table where id&gt;1"""val jdbcDF = spark.read .format("jdbc") .option("url", "jdbc:mysql:dbserver") .option("dbtable", s"( $sql ) t") .option("user", "username") .option("password", "password") .load() write exampleimport org.apache.spark.sql.SaveModeval prop=new java.util.Properties()prop.put("user","username")prop.put("password","yourpassword")val url="jdbc:mysql://host:3306/db_name"//df是一个dataframe,也就是你要写入的数据df.write.mode(SaveMode.Append).jdbc(url,"table_name",prop)]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[基于Spark实现推荐算法-4:基于物品的协同过滤(实现篇)]]></title>
    <url>%2F2018%2F05%2F04%2Fspark-recommendation-item-based-cf%2F</url>
    <content type="text"><![CDATA[算法设计与实现基于物品的协同过滤又称Item-Based CF.基于Spark的Item-Based CF算法其实现原理和步骤与经典方法基本一致，不同的地方主要在于具体步骤内的并行化计算。 相似度算法在Spark MLlib中提供了余弦相似度的分布式实现，org.apache.spark.mllib.linalg.distributed包中的IndexedRowMatrix是一个分布式矩阵类，其中提供了一个columnSimilarities方法用于计算该矩阵各列之间的余弦相似度。 预测值计算采用加权求和的方法计算预测值. 实现步骤：步骤分解Step 1：读取用户评分数据，设用户数为U，物品数目为I，将数据转换为以用户为行，物品为列的二维矩阵R，R维度为（U×I）。矩阵的每一个数据表示某个用户对特定物品的评分。 Step 2：计算R每列之间的相似度，可以得到维度(I×I)的矩阵S。S(i,j)表示物品i和物品j之间的相似度。 Step 3：使用预测值计算公式计算用户对未评分物品的预测评分。得到预测评分矩阵P，P维度为（U×I），P(i,j) 表示用户i对物品j的预测评分。 Step 4：对用户i进行推荐。找出P的第i行中评分最高的前K个物品推荐给用户。K是需要推荐的物品数量。 基于Spark的实现鉴于从Spark 2.0.0开始基于Dataset的API成为了主要编程API，本文采用Dataset的API进行实现，使用的语言为Scala。基于Spark平台Item-Based CF可以分为4步实现： Step 1：读取评分数据集，这里是从Hive数据仓库读取数据，udata是表名，userId、itemId、rating是3个字段，分别是[用户ID，物品ID，评分]。这里按照8:2的比例将数据集分为训练集和测试集。 //读取评分数据集def dataSet(): (DataFrame, DataFrame) = &#123; val table = " udata" val df = spark.sql(s"select userId,itemId,rating from test.$table") val Array(training, test) = df.randomSplit(Array(0.8, 0.2)) training.cache() test.cache() (training, test) &#125; Step 2：将数据集转换成以用户为行、物品为列的二维评分矩阵，矩阵的每一个行是一个用户对所有物品的评分。然后求出评分矩阵每列之间的相似度，得到一个以物品ID为行和列，以相似度为数据的矩阵，这个矩阵就是物品相似度矩阵。 //计算相似度矩阵//评分数据转换成矩阵 def parseToMatrix(data: DataFrame): CoordinateMatrix = &#123; val parsedData = data.rdd.map &#123; case Row(user: Int, item: Int, rate: Int) =&gt; MatrixEntry(user, item, rate.toDouble) &#125; new CoordinateMatrix(parsedData) &#125; //计算相似度矩阵 def standardCosine(matrix: CoordinateMatrix): RDD[MatrixEntry] = &#123; val similarity = matrix.toIndexedRowMatrix().columnSimilarities() val sim = similarity.entries sim &#125; Step 3：计算测试集相似物品表。将测试集与训练集、物品相似度表进行左联接，得到测试集相似物品表，字段为[用户ID,物品ID,实际评分,相似物品评分,相似度]。然后将该表注册成临时表testAndSim，并且缓存起来，供下一步使用。 //计算测试集相似物品表val testItemSim = spark.sql( """ |select test.userId,test.itemId,test.rating testRating,training.rating,sim.sim |from test | left join training on test.userId=training.userId | left join itemSim sim on test.itemId=sim.itemX and training.itemId=sim.itemY """.stripMargin ) testItemSim.cache() testItemSim.createOrReplaceTempView("testAndSim") Step 4：预测测试集中用户对物品的评分。对上一步得到的测试集相似物品表testAndSim进行计算，将该表数据按照(userId,itemId)进行分组，取每组相似度前K个物品的评分。最后依照预测值计算公式求出预测值，得到预测评分表testAndPre，其中pre字段就是对应的预测评分值。 //预测评分val sqlRank = "select userId,itemId,testRating,rating,sim," + "rank() over (partition by userId,itemId order by sim desc) rank\n" + "from testAndSim"val testAndPre = spark.sql( "select userId,itemId,first(testRating) rate,nvl(sum(rating*sim)/sum(abs(sim)),0) pre\n" + "from( " + " select *" + " from (" + sqlRank + ") t " + s" where rank &lt;= $k " + ") w " + "group by userId,itemId")]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>algorithm</tag>
        <tag>recommendation</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[基于Spark实现推荐算法-2:基于用户的协同过滤(理论篇)]]></title>
    <url>%2F2018%2F05%2F03%2Fspark-recommendation-2%2F</url>
    <content type="text"><![CDATA[基于用户的协同过滤基于用户的协同过滤，即User-Based CF (User-Based Collaborative Filtering)，是基于一个这样的假设“跟你爱好相同的人喜欢的物品，你很可能也喜欢”，所以User-Based CF主要的任务就是找出用户的最近邻居，从而根据最近邻居的喜好做出未知项的评分预测。User-Based CF算法可以分为4个步骤：数据表示、最近邻查询、评分预测、推荐结果产生。 1.数据表示User-Based CF使用的用户—项目数据可以表示为二维矩阵。以用户对项目的评分数据为例，评分数据可以用如下方法表示， 用户评分数据表 Item/User User 1 … User k … User n Item 1 R1,1 … Item j Rj,k … Item m Rm,n 其中每一行代表物品所获得的评分。以Item j为例，Rj,k代表用户k给物品j的评分，但并不是所有用户都会对Item j进行评分，所以Rj,k可能存在也可能不存在。这里设用户数为n，物品数目为m，可以将数据转换为评分值组成的二维矩阵R，则R是维度为(m×n)的评分矩阵。 2.最近邻查询所谓“最近邻居”是指与当前用户最相似的其他若干用户，为了防止单个用户评分对推荐结果影响过大，导致偏差，通常取N（N为大于1的整数）个相似用户的数据。没有度量就没有比较，所以需要某种方法度量用户间的相似程度，这里相似程度简称为相似度。相似度的计算方法较多，以余弦相似度计算为例说明。余弦相似度(Cosine-based Similarity)是用两个向量间夹角的余弦值来衡量相似性。若要计算用户间的相似度，也就是要计算上一步中矩阵R的列向量间的相似度，取User X和User Y的向量分别为如下公式所示， x ⃗=(R_{1,x},…,R_{m,x} ) y ⃗=(R_{1,y},…,R_{m,y} )那么User X与User Y间的余弦相似度为， sim(X,Y)=cos⁡(x ⃗,y ⃗ )=\frac{x ⃗∙y ⃗}{‖x ⃗ ‖*‖y ⃗ ‖}其中$x ⃗∙y ⃗$表示向量间的内积，$‖x ⃗ ‖$是向量 x ⃗$的模，sim(X,Y)就是相似度的值。依次计算当前用户与其他所有用户的相似度，选取相似度最大的前K个用户作为该用户的最近邻居集合。 3.评分预测评分预测的方法也有多种，常用的是加权求和的方法计算预测值。将用户u的最近邻居用户对物品i的打分进行加权求和，权值为各个最近邻居用户与用户u的相似度，然后对加权求和的结果求平均，计算得到用户u对物品i的预测打分，公式如下： P_{u,i}=\frac{\sum_{n∈N}S_{u,n}*R_{n,i}} {\sum_{n∈N}(|S_{u,n}|)}N代表用户u的最近邻居用户集合，n是属于N中的一个用户， $S{u,n}$ 表示用户u和用户n的相似度，$R{n,i}$ 表示用户n对物品i的评分。根据该公式逐个计算出用户u未评分的所有物品的预测评分，得到预测评分集合。 4.推荐结果产生从上一步得到的预测评分集合中，选出评分最高的若干个物品作为推荐物品集，也就是最终的推荐结果。]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>algorithm</tag>
        <tag>recommendation</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[基于Spark实现推荐算法-1:推荐算法简介]]></title>
    <url>%2F2018%2F05%2F02%2Fspark-recommendation-1%2F</url>
    <content type="text"><![CDATA[个性化推荐系统简介个性化推荐系统的定义在 1997 年由 Resnick 和 Varian 提出:利用互联网向用户提供信 息和建议，帮助用户选择产品，或模拟售货员帮助用户完成购买行为的系统 。通常推荐 由三个要素组成:推荐算法、用户、候选推荐项目。简单来说，一次推荐过程就是推荐算 法从候选推荐项目中挑出某些项目给用户。 目前个性化推荐系统已经在电子商务、视频、音乐、新闻、博客等领域得到了广泛应 用。通常这些领域的网站和应用会推荐若干商品或者作品给用户，这些推荐项目通常以“猜 你喜欢”、“购买此商品的顾客也同时购买”、“相似的商品”等形式出现，所推荐的物品便 是推荐系统通过推荐算法从海量物品中挑选出的。由于推荐系统会根据用户的历史行为数 据给出因人而异的推荐结果，所以称之为个性化推荐。 一套完整的个性化推荐系统通常包括用户信息收集、推荐模型计算、推荐结果展示三 个部分。个性化推荐系统首先收集用户的网络操作行为——如浏览、评分、购买等，将这 些行为数据存储到数据仓库中。然后通过机器学习、数据挖掘等相关技术来对这些数据进 行分析，更进一步可以从这些历史数据中学习用户的兴趣爱好，运行推荐算法生成推荐模 型。有了推荐模型，便可以为用户提供个性化的推荐服务，实现主动推荐的目的。个性化 推荐技术可以充分提高信息系统或者站点的服务质量和使用效率，从而吸引更多的用户 。 个性化推荐系统的输入数据可以有多种来源途径，通常分为显示输入和隐式输入两种 类型。显示输入是指用户明确表达喜好的行为，例如给电影评分、给微博点赞、购物后给 予好评或差评等。隐式输入则一般是非特意的行为，如浏览商品详情页面、查看电影评价、 搜索关键词等，这些行为并不代表用户喜欢或讨厌某个物品，但是推荐系统能够从中挖掘 出用户的兴趣信息。 个性化推荐系统的输出也是多样化的，有各种各样的形式。最常见的是推荐列表形式，如亚马逊等电子商务网站的推荐商品列表、YouTube 等视频网站的推荐影片列表、微博等 社交网站的推荐关注用户等。这类是最直接的推荐形式，明确告诉了用户这些是推荐结果， 属于显示的推荐。另一类形式是比较隐式的推荐:购物网站在关键词搜索结果列表中加入 推荐的结果;新闻网站根据推荐算法优化文章的排序;网络问答社区把用户可能感兴趣的 话题优先展示。这些推荐系统融入到了传统的系统模块中，起到提升原有系统功能的效果。 推荐算法分类推荐算法是推荐系统的核心，直接决定着推荐系统的性能和效果。依据推荐方法的不 同，通常推荐算法可以分为如下几大类:基于内容的推荐(Content-Based Recommendation)， 协同过滤推荐 (Collaborative Filtering Recommendation) ， 混 合 型 推 荐 (Hybrid Recommendation) 。 基于内容的推荐 基于内容的推荐是用项目内容或特征来定义所要推荐的项目或对象，然后系统基于用户评价对象的特征学习用户的兴趣，依据用户资料与待预测项目的匹配程度进行推荐。项目或对象的特征通常是文本内容，比如标题、名称、标签及该物品的其他元数据。基于内容的推荐系统不可避免地受到有限的特征信息获取技术的约束，根据推荐对象的不同，可能会要用到文本挖掘技术、图片识别技术、视频挖掘技术等。特别是自动提取多媒体数据内容特征的相关技术还不是很成熟，导致基于内容的推荐算法在这方面的相关应用受到了很大限制。 协同过滤推荐协同过滤(Collaborative Filtering，CF)是一种根据用户对各种产品的交互与评分来推荐 新产品的推荐系统技术。协同过滤最突出的优点就在于它的鲁棒性，适用于多种输入数据: 无论是“显式”的输入数据(例如在电影网站上进行评分)还是“隐式”的(例如用户访 问了一个产品的页面但是没有对产品评分)都可以用来做协同过滤。仅仅根据这些交互， 协同过滤算法就能够知道哪些产品之间比较相似以及哪些用户之间比较相似，然后就可以 做出新的推荐 。相比与基于内容的推荐算法，协同过滤推荐算法不需要分析项目的内 容和特征，所以可以不依赖于特征信息获取技术，从而适用于各种类别的项目。 总体上来说，协同过滤是一种利用集体智慧的方法，使用部分或所有用户的行为数据来进行推荐，不同用户通过推荐系统间接地相互协作，来获取各自感兴趣的信息。协同过 滤的核心是找出用户或项目的邻居，即相似的用户或项目，然后取若干个最近邻居推荐给 用户。根据具体的实现，协同过滤又可以被分为多个子类别，分别是基于记忆的协同过滤 (Memory-Based Collaborative Filtering)，基于模型的协同过滤(Model-Based Collaborative Filtering)，基于社交网络关系的协同过滤(Social-Based Collaborative Filtering)等。 基于记忆的协同过滤基于记忆的协同过滤算法使用全部或大部分用户—项目数据来生成预测。基于记忆的 协同过滤常用的有两种 :基于物品的协同过滤(Item-Based Collaborative Filtering， Item-Based CF) 和基于用户的协同过滤 (User-Based Collaborative Filtering，User-Based CF)。 对于类似微博、新闻、论坛、社交和知识问答等内容极多、候选推荐项目庞大的系统， User-Based CF 方法通常优于 Item-Based CF。因为这种情况下，物品相似度的计算量巨大， 而且需要频繁更新，而使用 User-Based CF 能够避免计算物品间的相似度。相反，对于一 个用户数量远超过物品数量的应用来说，此时 Item-Based CF 的性能可能比 User-Based CF 更优，这是由于此种情况的物品间相似度要比用户间相似度更容易计算。由此可见，推荐 系统的设计者必须根据应用的特点来选择合适的算法 ，不能一概而论。 与基于内容的推荐算法相比，Item-Based CF 和 User-Based CF 有下列优点: 1) 能够过滤难以进行内容分析的信息，如艺术品、音乐、文本、视频; 2) 可以基于一些复杂的，难以描述的概念进行过滤，如品质、风格、流行度等。 然而，Item-Based CF 和 User-Based CF 也存在着以下的缺点: 1) 稀疏性问题。如果用户对物品的评价非常稀疏，没有充足的数据用于计算相似性， 那么基于评价所得到的用户或物品间的相似性可能不准确; 2) 可扩展性问题。随着用户和物品的增多，数据量将会剧增，系统的性能会越来越低; 3) 冷启动问题。如果某一物品从来没有被评价过，则该物品不会被列为相似物品，那么这个物品就不可能被推荐。类似的，对于一个从未评价过物品的用户，无法 获得推荐。 基于模型的协同过滤Model-Based CF 简单来说是先用历史数据训练得到推荐模型，再用此模型进行预测的推荐算法。常见的模型算法包括基于概率的朴素贝叶斯算法、聚类算法和基于矩阵分解的算法等 。 常用的矩阵分解算法有:正则化矩阵分解(Regularized Matrix Factorization)，带偏置 的矩阵分解(Biased Matrix Factorization)，交替最小二乘法(Alternating Least Squares，ALS) 和奇异值分解(Singular Value Decomposition，SVD)。这些算法的基本思想都是将稀疏且 高维的用户—物品评分矩阵分解为两个低维矩阵，分别表示用户的特征和物品的特征。用 户的特征矩阵可以代表用户的兴趣，同样的，物品的特征矩阵暗含了物品的特点，这两个 矩阵的乘积能反应出用户对物品的喜好程度 。所以该类方法的主要工作就是计算出用户 的特征矩阵和物品的特征矩阵。 基于社交网络关系的协同过滤简单来说，是一种使用用户的社交关系数据、用户好友的喜好数据等社交类数据来进行推荐的算法。该类算法适合于微博、微信等拥有用户社交关系数据的系统。 混合型推荐 混合推荐是指将多种推荐技术综合，取长补短，以此来获得更好的推荐效果。常用的方案是将协同过滤技术与另一种算法结合，然后将不同方法得出的推荐结果进行筛选和融合，从而提高推荐效果。]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>algorithm</tag>
        <tag>recommendation</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[用Spark Streaming实时计算海量用户UV]]></title>
    <url>%2F2018%2F05%2F01%2Fspark-streaming-uv%2F</url>
    <content type="text"><![CDATA[提出需求实时统计业务系统(web,APP之类)的访问人数,即所谓UV,或者DAU指标. 这个需求怕是流计算最最最常见的需求了. 计算UV的关键点就在于去重,即同一个人访问两次是只计一个UV的.在离线计算中统计UV比较容易想到的方法就是用group或distinct机制来去重.但是在实时计算场景,还用group就不太科学了,一个是全量数据的group是比较费时的,第二个是全量数据的group是很费内存和CPU的.特别是当用户量巨大的时候,还要做到秒级更新就更难了. 总结起来,需求就是:海量用户场景UV实时计算. 接受挑战不难发现,问题的主要难点就是去重. Spark Streaming目前没有给出内置方案(这个其实可以有),但是海量数据去重问题早就有解决办法了.所以Spark Streaming程序完全可以利用其他系统的现有方案解决去重问题,比如Redis. Redis的海量去重计数方案Bitmap方案所谓的Bitmap就是用一个bit位来标记某个元素对应的Value,比如ID为2的用户,就用第2个bit位来表示,然后用该位的值来表示该用户是否访问过.如果要计算UV,那就只要数一下有多少个1就行啦. 假设我们有40亿用户,使用Bitmap需要2^32个bit位,算下来也就500M左右. 你可能没想到的是,Redis中最常用的数据结构string,就可以实现bitmap算法. Redis提供了如下命令// 插入setbit key offset value//获取getbit key offset//计数BITCOUNT key [start] [end] 这里offset最大值就是2^32.比如ID为2的用户,可以setbit uv 2 1,来记录.最后要计算UV,就直接 BITCOUNT uv. 这步计数非常快,复杂度是O(1). HyperLogLog方案若要计算很多页面的UV,用bitmap还是比较费空间的,N个页面就得有N个500M.这时候HyperLogLog结构就是一个比较好的选择. Redis 在 2.8.9 版本添加了 HyperLogLog 结构。Redis HyperLogLog 是用来做基数统计的算法，HyperLogLog 的优点是，在输入元素的数量或者体积非常非常大时，计算基数所需的空间总是固定 的、并且是很小的。在 Redis 里面，每个 HyperLogLog 键只需要花费 12 KB 内存，就可以计算接近 2^64 个不同元素的基 数。这和计算基数时，元素越多耗费内存就越多的集合形成鲜明对比。但是，因为 HyperLogLog 只会根据输入元素来计算基数，而不会储存输入元素本身，所以 HyperLogLog 不能像集合那样，返回输入的各个元素。 也就是说HyperLogLog是一种基数统计算法,计算结果是近似值, 12 KB 内存就可以计算2^64 个不同元素的基数. Redis命令如下:redis 127.0.0.1:6379&gt; PFADD runoobkey &quot;redis&quot;1) (integer) 1redis 127.0.0.1:6379&gt; PFADD runoobkey &quot;mongodb&quot;1) (integer) 1redis 127.0.0.1:6379&gt; PFADD runoobkey &quot;mysql&quot;1) (integer) 1redis 127.0.0.1:6379&gt; PFCOUNT runoobkey(integer) 3 代码实现下面给出HyperLogLog方案的参考实现: stream.foreachRDD &#123; rdd =&gt; //统计人数 rdd.foreachPartition &#123; partition =&gt; //从分区所属executor的redis线程池获取一个连接. val redis = RedisUtil.getRedis partition.foreach &#123; case (date, userId) =&gt; //统计当前userId redis.pfadd(s"uv:$date", userId) &#125; redis.close() &#125;&#125; 关于Redis的连接,如果是用java或scala可以使用JedisPool,注意处理序列化即可.]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>streaming</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark API 全集(3):Spark RDD API全集]]></title>
    <url>%2F2018%2F03%2F26%2Fspark-rdd-RDD-api%2F</url>
    <content type="text"><![CDATA[RDD是啥Resilient Distributed Dataset (RDD)，弹性分布式数据集，是对不可修改，分区的数据集合的抽象。 RDD is characterized by five main properties: A list of partitions A function for computing each split A list of dependencies on other RDDs Optionally, a Partitioner for key-value RDDs (e.g. to say that the RDD is hash-partitioned) Optionally, a list of preferred locations to compute each split on (e.g. block locations for an HDFS file) org.spark.rdd.RDD类方法RDD是一个抽象类，定义如下abstract class RDD[T] extends Serializable with Logging RDD类的public方法大约有80多个（包括不同参数重载的）,均在下面列出。 值得注意的是，RDD类中并没有定义xxxByKey形式的方法，这类方法其实是在PairRDDFunctions中定义的，通过隐式转换，键值对形式的RDD（即RDD[(K, V)）可以调用PairRDDFunctions中定义的方法。相关的隐式转换定义在RDD的伴生对象中。 键值转换操作filter(f: (T) ⇒ Boolean): RDD[T]过滤数据，仅留下使得f返回true的元素。map[U](f: (T) ⇒ U)(implicit arg0: ClassTag[U]): RDD[U]将一个RDD中的每个数据项，通过map中的函数映射变为一个新的元素。输入分区与输出分区一对一，即：有多少个输入分区，就有多少个输出分区。flatMap[U](f: (T) ⇒ TraversableOnce[U])(implicit arg0: ClassTag[U]): RDD[U]第一步和map一样，最后将所有的输出分区合并成一个。使用flatMap时候需要注意：flatMap会将字符串看成是一个字符数组。mapPartitions[U](f: (Iterator[T]) ⇒ Iterator[U], preservesPartitioning: Boolean = false)(implicit arg0: ClassTag[U]): RDD[U]该函数和map函数类似，只不过映射函数的参数由RDD中的每一个元素变成了RDD中每一个分区的迭代器。如果在映射的过程中需要频繁创建额外的对象，使用mapPartitions要比map高效的过。比如，将RDD中的所有数据通过JDBC连接写入数据库，如果使用map函数，可能要为每一个元素都创建一个connection，这样开销很大，如果使用mapPartitions，那么只需要针对每一个分区建立一个connection。参数preservesPartitioning表示是否保留父RDD的partitioner分区信息。mapPartitionsWithIndex[U](f: (Int, Iterator[T]) =&gt; Iterator[U], preservesPartitioning: Boolean = false)(implicit arg0: ClassTag[U]): RDD[U]函数作用同mapPartitions，不过提供了两个参数，第一个参数为分区的索引。keyBy[K](f: (T) ⇒ K): RDD[(K, T)]通过f函数为每个元素生成一个KEYsortBy[K](f: (T) ⇒ K, ascending: Boolean = true, numPartitions: Int = this.partitions.length)(implicit ord: Ordering[K], ctag: ClassTag[K]): RDD[T]通过给定的函数对元素排序zip[U](other: RDD[U])(implicit arg0: ClassTag[U]): RDD[(T, U)]与另一个RDD组合成（k,v)对。zipPartitions[B, V](rdd2: RDD[B], preservesPartitioning: Boolean)(f: (Iterator[T], Iterator[B]) ⇒ Iterator[V])(implicit arg0: ClassTag[B], arg1: ClassTag[V]): RDD[V]zipWithIndex(): RDD[(T, Long)]zipWithUniqueId(): RDD[(T, Long)] 聚合相关aggregate[U](zeroValue: U)(seqOp: (U, T) ⇒ U, combOp: (U, U) ⇒ U)(implicit arg0: ClassTag[U]): Uaggregate用户聚合RDD中的元素，先使用seqOp将RDD中每个分区中的T类型元素聚合成U类型，再使用combOp将之前每个分区聚合后的U类型聚合成U类型，特别注意seqOp和combOp都会使用zeroValue的值，zeroValue的类型为U。treeAggregate[U](zeroValue: U)(seqOp: (U, T) ⇒ U, combOp: (U, U) ⇒ U, depth: Int = 2)(implicit arg0: ClassTag[U]): U多层级聚合reduce(f: (T, T) ⇒ T): T根据映射函数f，对RDD中的元素进行二元计算，返回计算结果。treeReduce(f: (T, T) ⇒ T, depth: Int = 2): T多级reduce归并聚合fold(zeroValue: T)(op: (T, T) ⇒ T): Tfold是aggregate的简化，将aggregate中的seqOp和combOp使用同一个函数op。count(): Longcount返回RDD中的元素数量。countApprox(timeout: Long, confidence: Double = 0.95): PartialResult[BoundedDouble]近似countcountApproxDistinct(relativeSD: Double = 0.05): LongcountApproxDistinct(p: Int, sp: Int): Long近似distinct countcountByValue()(implicit ord: Ordering[T] = null): Map[T, Long]计算每个值出现次数countByValueApprox(timeout: Long, confidence: Double = 0.95)(implicit ord: Ordering[T] = null):计算每个值出现次数近似值distinct(): RDD[T]distinct(numPartitions: Int)(implicit ord: Ordering[T] = null): RDD[T]返回元素去重后的RDDgroupBy[K](f: (T) ⇒ K)(implicit kt: ClassTag[K]): RDD[(K, Iterable[T])]groupBy[K](f: (T) ⇒ K, numPartitions: Int)(implicit kt: ClassTag[K]): RDD[(K, Iterable[T])]groupBy[K](f: (T) ⇒ K, p: Partitioner)(implicit kt: ClassTag[K], ord: Ordering[K] = null): RDD[(K, Iterable[T])]按指定函数生成key，并按key分组。注意：性能比较差，推荐用PairRDDFunctions.reduceByKey or PairRDDFunctions.aggregateByKey.因为reduceByKey会先在分区内做聚合，再进行数据交换(shuffle)。glom(): RDD[Array[T]]该函数是将RDD中每一个分区中类型为T的元素转换成Array[T]，这样每一个分区就只有一个数组元素。max()(implicit ord: Ordering[T]): T最大的元素min()(implicit ord: Ordering[T]): T最小的元素 遍历元素foreach(f: (T) ⇒ Unit): Unitforeach用于遍历RDD,将函数f应用于每一个元素。但要注意，如果对RDD执行foreach，只会在Executor端有效，而并不是Driver端。比如：rdd.foreach(println)，只会在Executor的stdout中打印出来，Driver端是看不到的。foreachPartition(f: (Iterator[T]) ⇒ Unit): UnitforeachPartition和foreach类似，只不过是对每一个分区使用f。 取元素相关collect(): Array[T]collect用于将一个RDD转换成数组。first(): Tfirst返回RDD中的第一个元素，不排序。take(num: Int): Array[T]take用于获取RDD中从0到num-1下标的元素，不排序。top(num: Int)(implicit ord: Ordering[T]): Array[T]top函数用于从RDD中，按照默认（降序）或者指定的排序规则，返回前num个元素。takeOrdered(num: Int)(implicit ord: Ordering[T]): Array[T]takeOrdered和top类似，只不过以和top相反的顺序返回元素takeSample(withReplacement: Boolean, num: Int, seed: Long = Utils.random.nextLong): Array[T]取样本元素 集合间运算++(other: RDD[T]): RDD[T]与另一个RDD union。intersection(other: RDD[T], partitioner: Partitioner)(implicit ord: Ordering[T] = null): RDD[T]intersection(other: RDD[T], numPartitions: Int): RDD[T]intersection(other: RDD[T]): RDD[T]取交集subtract(other: RDD[T], p: Partitioner)(implicit ord: Ordering[T] = null): RDD[T]subtract(other: RDD[T], numPartitions: Int): RDD[T]subtract(other: RDD[T]): RDD[T]求差集union(other: RDD[T]): RDD[T]与另一个RDD合并，类似union all,不会去重。 其他persist(): RDD.this.typepersist(newLevel: StorageLevel): RDD.this.type缓存数据，可设置缓存级别(如果尚未设置过，才可以设置，本地checkpoint除外)unpersist(blocking: Boolean = true): RDD.this.typeMark the RDD as non-persistent, and remove all blocks for it from memory and disk.cache(): RDD.this.typeMEMORY_ONLY级别缓存数据cartesian[U](other: RDD[U])(implicit arg0: ClassTag[U]): RDD[(T, U)]：计算两个RDD的迪卡尔积checkpoint(): Unit标记将该RDD进行checkpoint处理？coalesce(numPartitions: Int, shuffle: Boolean = false)(implicit ord: Ordering[T] = null): RDD[T]分区合并(只能减少分区)，使用HashPartitioner。第一个参数为重分区的数目，第二个为是否进行shuffle，默认为false;repartition(numPartitions: Int)(implicit ord: Ordering[T] = null): RDD[T]调整分区数，会导致shuffle，如果是减少分区，可以使用coalesce，避免shuffle。toDebugString: String返回RDD依赖树/血统图getCheckpointFile: Option[String]获取checkpoint文件夹名称localCheckpoint(): RDD.this.type标记为使用本地checkpointisEmpty(): Boolean是否含0个元素iterator(split: Partition, context: TaskContext): Iterator[T]返回迭代器，不应直接调用，而是给RDD的子类用的。toLocalIterator: Iterator[T]返回元素的本地迭代器pipe(command: String): RDD[String]pipe(command: String, env: Map[String, String]): RDD[String]pipe(command: Seq[String], env: Map[String, String] = Map(), printPipeContext: ((String) ⇒ Unit) ⇒ Unit = null, printRDDElement: (T, (String) ⇒ Unit) ⇒ Unit = null, separateWorkingDir: Boolean = false, bufferSize: Int = 8192, encoding: String = Codec.defaultCharsetCodec.name): RDD[String]调用外部进程处理RDD,如通过标准输入传给shell脚本。preferredLocations(split: Partition): Seq[String]Get the preferred locations of a partition, taking into account whether the RDD is checkpointed.randomSplit(weights: Array[Double], seed: Long = Utils.random.nextLong): Array[RDD[T]]按权随机将元素分组sample(withReplacement: Boolean, fraction: Double, seed: Long = Utils.random.nextLong): RDD[T]取样本/子集setName(_name: String): RDD.this.type设置RDD名字 保存saveAsObjectFile(path: String): Unit保存为SequenceFilesaveAsTextFile(path: String, codec: Class[_ &lt;: CompressionCodec]): UnitsaveAsTextFile(path: String): Unit保存为文本文件 变量context: SparkContext创建RDD的SparkContextsparkContext: SparkContext创建RDD的SparkContextdependencies: Seq[Dependency[_]]RDD的依赖列表getNumPartitions: Int获取RDD的分区数getStorageLevel: StorageLevel获取存储等级，如果设置为none,则返回StorageLevel.NONE 。id: Int该RDD的unique IDisCheckpointed: Boolean是否checkpointed and materialized, either reliably or locally.name: StringRDD的名字partitioner: Option[Partitioner]分区器partitions: Array[Partition]各个分区]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>API</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[[读书笔记] ES权威指南]]></title>
    <url>%2F2018%2F03%2F26%2Fes-guide-note%2F</url>
    <content type="text"><![CDATA[基础入门 轻量搜索GET /megacorp/employee/_search?q=last_name:Smith 使用查询表达式搜索GET /megacorp/employee/_search&#123; "query" : &#123; "bool": &#123; "must": &#123; "match" : &#123; "last_name" : "smith" &#125; &#125;, "filter": &#123; "range" : &#123; "age" : &#123; "gt" : 30 &#125; &#125; &#125; &#125; &#125;&#125; 全文搜索GET /megacorp/employee/_search&#123; "query" : &#123; "match" : &#123; "about" : "rock climbing" &#125; &#125;&#125; 短语搜索精确匹配一系列单词或者短语GET /megacorp/employee/_search&#123; "query" : &#123; "match_phrase" : &#123; "about" : "rock climbing" &#125; &#125;&#125; 高亮搜索GET /megacorp/employee/_search&#123; "query" : &#123; "match_phrase" : &#123; "about" : "rock climbing" &#125; &#125;, "highlight": &#123; "fields" : &#123; "about" : &#123;&#125; &#125; &#125;&#125; 聚合GET /megacorp/employee/_search&#123; "aggs" : &#123; "all_interests" : &#123; "terms" : &#123; "field" : "interests" &#125;, "aggs" : &#123; "avg_age" : &#123; "avg" : &#123; "field" : "age" &#125; &#125; &#125; &#125; &#125;&#125; 分布式特性Elasticsearch 尽可能地屏蔽了分布式系统的复杂性。这里列举了一些在后台自动执行的操作： 分配文档到不同的容器 或 分片 中，文档可以储存在一个或多个节点中按集群节点来均衡分配这些分片，从而对索引和搜索过程进行负载均衡复制每个分片以支持数据冗余，从而防止硬件故障导致的数据丢失将集群中任一节点的请求路由到存有相关数据的节点集群扩容时无缝整合新节点，重新分配分片以便从离群节点恢复 集群内的原理主分片的数目在索引创建时 就已经确定了下来。实际上，这个数目定义了这个索引能够 存储 的最大数据量。（实际大小取决于你的数据、硬件和使用场景。） 但是，读操作——搜索和返回数据——可以同时被主分片 或 副本分片所处理，所以当你拥有越多的副本分片时，也将拥有越高的吞吐量。 在运行中的集群上是可以动态调整副本分片数目的 ，我们可以按需伸缩集群。 当然，如果只是在相同节点数目的集群上增加更多的副本分片并不能提高性能，因为每个分片从节点上获得的资源会变少。 你需要增加更多的硬件资源来提升吞吐量。 但是更多的副本分片数提高了数据冗余量：按照上面的节点配置，我们可以在失去2个节点的情况下不丢失任何数据。 如果我们重新启动 Node 1 ，集群可以将缺失的副本分片再次进行分配，那么集群的状态也将如图 5 “将参数 number_of_replicas 调大到 2”所示。 如果 Node 1 依然拥有着之前的分片，它将尝试去重用它们，同时仅从主分片复制发生了修改的数据文件。 数据输入和输出 一个 键 可以是一个字段或字段的名称，一个 值 可以是一个字符串，一个数字，一个布尔值， 另一个对象，一些数组值，或一些其它特殊类型诸如表示日期的字符串，或代表一个地理位置的对象： 文档元数据一个文档不仅仅包含它的数据 ，也包含 元数据 —— 有关 文档的信息。 三个必须的元数据元素如下： _index文档在哪存放_type文档表示的对象类别_id文档唯一标识 _index一个 索引 应该是因共同的特性被分组到一起的文档集合。 例如，你可能存储所有的产品在索引 products 中，而存储所有销售的交易到索引 sales 中。 虽然也允许存储不相关的数据到一个索引中，但这通常看作是一个反模式的做法。 提示实际上，在 Elasticsearch 中，我们的数据是被存储和索引在 分片 中，而一个索引仅仅是逻辑上的命名空间， 这个命名空间由一个或者多个分片组合在一起。 然而，这是一个内部细节，我们的应用程序根本不应该关心分片，对于应用程序而言，只需知道文档位于一个 索引 内。 Elasticsearch 会处理所有的细节。 我们将在 索引管理 介绍如何自行创建和管理索引，但现在我们将让 Elasticsearch 帮我们创建索引。 所有需要我们做的就是选择一个索引名，这个名字必须小写，不能以下划线开头，不能包含逗号。我们用 website 作为索引名举例。 _type编辑数据可能在索引中只是松散的组合在一起，但是通常明确定义一些数据中的子分区是很有用的。 例如，所有的产品都放在一个索引中，但是你有许多不同的产品类别，比如 “electronics” 、 “kitchen” 和 “lawn-care”。 这些文档共享一种相同的（或非常相似）的模式：他们有一个标题、描述、产品代码和价格。他们只是正好属于“产品”下的一些子类。 Elasticsearch 公开了一个称为 types （类型）的特性，它允许您在索引中对数据进行逻辑分区。不同 types 的文档可能有不同的字段，但最好能够非常相似。 我们将在 类型和映射 中更多的讨论关于 types 的一些应用和限制。 一个 _type 命名可以是大写或者小写，但是不能以下划线或者句号开头，不应该包含逗号， 并且长度限制为256个字符. 我们使用 blog 作为类型名举例。 _id编辑ID 是一个字符串， 当它和 _index 以及 _type 组合就可以唯一确定 Elasticsearch 中的一个文档。 当你创建一个新的文档，要么提供自己的 _id ，要么让 Elasticsearch 帮你生成。 其他元数据编辑还有一些其他的元数据元素，他们在 类型和映射 进行了介绍。通过前面已经列出的元数据元素， 我们已经能存储文档到 Elasticsearch 中并通过 ID 检索它—换句话说，使用 Elasticsearch 作为文档的存储介质。 取回一个文档GET /website/blog/123?pretty #调用 Elasticsearch 的 pretty-print 功能，该功能 使得 JSON 响应体更加可读GET /website/blog/123?_source=title,text #该 _source 字段现在包含的只是我们请求的那些字段GET /website/blog/123/_source #只想得到 _source 字段，不需要任何元数据 检查文档是否存在curl -i -XHEAD http://localhost:9200/website/blog/123 更新整个文档PUT /website/blog/123&#123; "title": "My first blog entry", "text": "I am starting to get the hang of this...", "date": "2014/01/02"&#125; ps:不存在时，就是创建 仅创建文档PUT /website/blog/123/_create&#123; ... &#125; 删除文档DELETE /website/blog/123 冲突处理悲观并发控制这种方法被关系型数据库广泛使用，它假定有变更冲突可能发生，因此阻塞访问资源以防止冲突。 一个典型的例子是读取一行数据之前先将其锁住，确保只有放置锁的线程能够对这行数据进行修改。乐观并发控制Elasticsearch 中使用的这种方法假定冲突是不可能发生的，并且不会阻塞正在尝试的操作。 然而，如果源数据在读写当中被修改，更新将会失败。应用程序接下来将决定该如何解决冲突。 例如，可以重试更新、使用新的数据、或者将相关情况报告给用户。 乐观并发控制我们可以利用 _version 号来确保 应用中相互冲突的变更不会导致数据丢失。我们通过指定想要修改文档的 version 号来达到这个目的。 如果该版本不是当前版本号，我们的请求将会失败。PUT /website/blog/1?version=1&#123; "title": "My first blog entry", "text": "Starting to get the hang of this..."&#125; 我们想这个在我们索引中的文档只有现在的 _version 为 1 时，本次更新才能成功。 通过外部系统使用版本控制 如果你的主数据库已经有了版本号 — 或一个能作为版本号的字段值比如 timestamp — 那么你就可以在 Elasticsearch 中通过增加 version_type=external 到查询字符串的方式重用这些相同的版本号， 版本号必须是大于零的整数， 且小于 9.2E+18 — 一个 Java 中 long 类型的正值。 外部版本号的处理方式和我们之前讨论的内部版本号的处理方式有些不同， Elasticsearch 不是检查当前 _version 和请求中指定的版本号是否相同， 而是检查当前 _version 是否 小于 指定的版本号。 如果请求成功，外部的版本号作为文档的新 _version 进行存储。 文档是不可变的：他们不能被修改，只能被替换。 部分更新POST /website/blog/1/_update&#123; "doc" : &#123; "tags" : [ "testing" ], "views": 0 &#125;&#125; 对象被合并到一起，覆盖现有的字段，增加新的字段。 取回多个文档GET /_mget&#123; "docs" : [ &#123; "_index" : "website", "_type" : "blog", "_id" : 2 &#125;, &#123; "_index" : "website", "_type" : "pageviews", "_id" : 1, "_source": "views" &#125; ]&#125;GET /website/blog/_mget&#123; "ids" : [ "2", "1" ]&#125; 代价较小的批量操作POST /_bulk&#123; "delete": &#123; "_index": "website", "_type": "blog", "_id": "123" &#125;&#125;&#123; "create": &#123; "_index": "website", "_type": "blog", "_id": "123" &#125;&#125;&#123; "title": "My first blog post" &#125;&#123; "index": &#123; "_index": "website", "_type": "blog" &#125;&#125;&#123; "title": "My second blog post" &#125;&#123; "update": &#123; "_index": "website", "_type": "blog", "_id": "123", "_retry_on_conflict" : 3&#125; &#125;&#123; "doc" : &#123;"title" : "My updated blog post"&#125; &#125; 分布式文档存储分片选择公式 shard = hash(routing) % number_of_primary_shardsrouting 是一个可变值，默认是文档的 _id ，也可以设置成一个自定义的值。 相同分片的副本不会放在同一节点 我们可以发送请求到集群中的任一节点。 每个节点都有能力处理任意请求。 每个节点都知道集群中任一文档位置，所以可以直接将请求转发到需要的节点上。 新建、索引和删除 请求都是 写 操作， 必须在主分片上面完成之后才能被复制到相关的副本分片在客户端收到成功响应时，文档变更已经在主分片和所有副本分片执行完成，变更是安全的。 有一些可选的请求参数允许您影响这个过程，可能以数据安全为代价提升性能。这些选项很少使用，因为Elasticsearch已经很快，但是为了完整起见，在这里阐述如下：https://www.elastic.co/guide/cn/elasticsearch/guide/current/distrib-write.html 在处理读取请求时，协调结点在每次请求的时候都会通过轮询所有的副本分片来达到负载均衡。 搜索——最基本的工具映射（Mapping）描述数据在每个字段内如何存储分析（Analysis）全文是如何处理使之可以被搜索的领域特定查询语言（Query DSL）Elasticsearch 中强大灵活的查询语言 多索引，多类型然而，经常的情况下，你 想在一个或多个特殊的索引并且在一个或者多个特殊的类型中进行搜索。我们可以通过在URL中指定特殊的索引和类型达到这种效果，如下所示： /_search在所有的索引中搜索所有的类型/gb/_search在 gb 索引中搜索所有的类型/gb,us/_search在 gb 和 us 索引中搜索所有的文档/g,u/_search在任何以 g 或者 u 开头的索引中搜索所有的类型/gb/user/_search在 gb 索引中搜索 user 类型/gb,us/user,tweet/_search在 gb 和 us 索引中搜索 user 和 tweet 类型/_all/user,tweet/_search在所有的索引中搜索 user 和 tweet 类型 分页和 SQL 使用 LIMIT 关键字返回单个 page 结果的方法相同，Elasticsearch 接受 from 和 size 参数： size显示应该返回的结果数量，默认是 10from显示应该跳过的初始结果数量，默认是 0如果每页展示 5 条结果，可以用下面方式请求得到 1 到 3 页的结果： GET /_search?size=5GET /_search?size=5&amp;from=5GET /_search?size=5&amp;from=10 在分布式系统中深度分页理解为什么深度分页是有问题的，我们可以假设在一个有 5 个主分片的索引中搜索。 当我们请求结果的第一页（结果从 1 到 10 ），每一个分片产生前 10 的结果，并且返回给 协调节点 ，协调节点对 50 个结果排序得到全部结果的前 10 个。 现在假设我们请求第 1000 页—结果从 10001 到 10010 。所有都以相同的方式工作除了每个分片不得不产生前10010个结果以外。 然后协调节点对全部 50050 个结果排序最后丢弃掉这些结果中的 50040 个结果。 可以看到，在分布式系统中，对结果排序的成本随分页的深度成指数上升。这就是 web 搜索引擎对任何查询都不要返回超过 1000 个结果的原因。 当索引一个文档的时候，Elasticsearch 取出所有字段的值拼接成一个大的字符串，作为 _all 字段进行索引。除非设置特定字段，否则查询字符串就使用 _all 字段进行搜索。 在刚开始开发一个应用时，_all 字段是一个很实用的特性。之后，你会发现如果搜索时用指定字段来代替 _all 字段，将会更好控制搜索结果。当 _all 字段不再有用的时候，可以将它置为失效，正如在 元数据: _all 字段 中所解释的。 下面的查询针对tweents类型，并使用以下的条件： name 字段中包含 mary 或者 johndate 值大于 2014-09-10all 字段包含 aggregations 或者 geo+name:(mary john) +date:&gt;2014-09-10 +(aggregations geo) 不推荐直接向用户暴露查询字符串搜索功能，除非对于集群和数据来说非常信任他们。 相反，我们经常在生产环境中更多地使用功能全面的 request body 查询API，除了能完成以上所有功能，还有一些附加功能。 映射和分析Elasticsearch 中的数据可以概括的分为两类：精确值和全文。 你只能搜索在索引中出现的词条，所以索引文本和查询字符串必须标准化为相同的格式。 分词和标准化的过程称为 分析（analyze） 分析器 实际上是将三个功能封装到了一个包里： 字符过滤器首先，字符串按顺序通过每个 字符过滤器 。他们的任务是在分词前整理字符串。一个字符过滤器可以用来去掉HTML，或者将 &amp; 转化成 and。分词器其次，字符串被 分词器 分为单个的词条。一个简单的分词器遇到空格和标点的时候，可能会将文本拆分成词条。Token 过滤器最后，词条按顺序通过每个 token 过滤器 。这个过程可能会改变词条（例如，小写化 Quick ），删除词条（例如， 像 a，and，the 等无用词），或者增加词条（例如，像 jump 和 leap 这种同义词）。 当你查询一个 全文 域时， 会对查询字符串应用相同的分析器，以产生正确的搜索词条列表。当你查询一个 精确值 域时，不会分析查询字符串， 而是搜索你指定的精确值。 测试分析器，测试分词结果GET /_analyze&#123; "analyzer": "standard", "text": "Text to analyze"&#125; 当Elasticsearch在你的文档中检测到一个新的字符串域 ，它会自动设置其为一个全文 字符串 域，使用 标准 分析器对它进行分析。 Elasticsearch 支持 如下简单域类型： 字符串: string整数 : byte, short, integer, long浮点数: float, double布尔型: boolean日期: date 当你索引一个包含新域的文档—之前未曾出现— Elasticsearch 会使用 动态映射 ，通过JSON中基本数据类型，尝试猜测域类型， 查看映射GET /gb/_mapping/tweet 域最重要的属性是 type 。对于不是 string 的域，你一般只需要设置 type ： 默认， string 类型域会被认为包含全文。就是说，它们的值在索引前，会通过 一个分析器，针对于这个域的查询在搜索前也会经过一个分析器。 string 域映射的两个最重要 属性是 index 和 analyzer 。 index 属性控制怎样索引字符串。它可以是下面三个值： analyzed首先分析字符串，然后索引它。换句话说，以全文索引这个域。not_analyzed 索引这个域，所以它能够被搜索，但索引的是精确值。不会对它进行分析。no不索引这个域。这个域不会被搜索到。 analyzer对于 analyzed 字符串域，用 analyzer 属性指定在搜索和索引时使用的分析器。默认， Elasticsearch 使用 standard 分析器， 但你可以指定一个内置的分析器替代它，例如 whitespace 、 simple 和 english： 当你首次 创建一个索引的时候，可以指定类型的映射。你也可以使用 /mapping 为新类型（或者为存在的类型更新映射）增加映射。不能 修改 存在的域映射。 创建索引的时候指定映射PUT /gb&#123; "mappings": &#123; "tweet" : &#123; "properties" : &#123; "tweet" : &#123; "type" : "string", "analyzer": "english" &#125;, "date" : &#123; "type" : "date" &#125;, "name" : &#123; "type" : "string" &#125;, "user_id" : &#123; "type" : "long" &#125; &#125; &#125; &#125;&#125; 测试映射结果GET /gb/_analyze&#123; "field": "tweet", "text": "Black-cats"&#125; ps:仅支持fulltext字段 复杂核心域类型事实上， type 映射只是一种特殊的 对象 映射，我们称之为 根对象 。除了它有一些文档元数据的特殊顶级域，例如 _source 和 _all 域，它和其他对象一样。 Lucene 不理解内部对象。 Lucene 文档是由一组键值对列表组成的。为了能让 Elasticsearch 有效地索引内部类，它把我们的文档转化成这样：&#123; "tweet": [elasticsearch, flexible, very], "user.id": [@johnsmith], "user.gender": [male], "user.age": [26], "user.name.full": [john, smith], "user.name.first": [john], "user.name.last": [smith]&#125; 内部域 可以通过名称引用（例如， first ）。为了区分同名的两个域，我们可以使用全 路径 （例如， user.name.first ） 或 type 名加路径（ tweet.user.name.first ）。 内部对象数组最后，考虑包含 内部对象的数组是如何被索引的。 假设我们有个 followers 数组：&#123; "followers": [ &#123; "age": 35, "name": "Mary White"&#125;, &#123; "age": 26, "name": "Alex Jones"&#125;, &#123; "age": 19, "name": "Lisa Smith"&#125; ]&#125; 这个文档会像我们之前描述的那样被扁平化处理，结果如下所示：&#123; "followers.age": [19, 26, 35], "followers.name": [alex, jones, lisa, smith, mary, white]&#125; {age: 35} 和 {name: Mary White} 之间的相关性已经丢失了，因为每个多值域只是一包无序的值，而不是有序数组。这足以让我们问，“有一个26岁的追随者？” 但是我们不能得到一个准确的答案：“是否有一个26岁 名字叫 Alex Jones 的追随者？” 相关内部对象被称为 nested 对象，可以回答上面的查询，我们稍后会在嵌套对象中介绍它。 请求体查询对于一个查询请求，Elasticsearch 的工程师偏向于使用 GET 方式，因为他们觉得它比 POST 能更好的描述信息检索（retrieving information）的行为。然而，因为带请求体的 GET 请求并不被广泛支持，所以 search API 同时支持 POST 请求： 过滤情况（filtering context）和查询情况（query context）。当使用于 过滤情况 时，查询被设置成一个“不评分”或者“过滤”查询。当使用于 查询情况 时，查询就变成了一个“评分”的查询.过滤查询（Filtering queries）只是简单的检查包含或者排除，这就使得计算起来非常快。通常的规则是，使用 查询（query）语句来进行 全文 搜索或者其它任何需要影响 相关性得分 的搜索。除此以外的情况都使用过滤（filters)。 查询表达式(Query DSL)是一种非常灵活又富有表现力的 查询语言。GET /_search&#123; "query": YOUR_QUERY_HERE&#125;一个查询语句 的典型结构：&#123; QUERY_NAME: &#123; ARGUMENT: VALUE, ARGUMENT: VALUE,... &#125;&#125;如果是针对某个字段，那么它的结构如下：&#123; QUERY_NAME: &#123; FIELD_NAME: &#123; ARGUMENT: VALUE, ARGUMENT: VALUE,... &#125; &#125;&#125; 无论你在任何字段上进行的是全文搜索还是精确查询，match 查询是你可用的标准查询。如果你在一个全文字段上使用 match 查询，在执行查询前，它将用正确的分析器去分析查询字符串：{ “match”: { “tweet”: “About Search” }}如果在一个精确值的字段上使用它， 例如数字、日期、布尔或者一个 not_analyzed 字符串字段，那么它将会精确匹配给定的值：{ “match”: { “age”: 26 }} multi_match 查询可以在多个字段上执行相同的 match 查询：&#123; "multi_match": &#123; "query": "full text search", "fields": [ "title", "body" ] &#125;&#125; range 查询找出那些落在指定区间内的数字或者时间：&#123; "range": &#123; "age": &#123; "gte": 20, "lt": 30 &#125; &#125;&#125; term 查询对于输入的文本不 分析 ，所以它将给定的值进行精确查询。 terms 查询和 term 查询一样，但它允许你指定多值进行匹配。如果这个字段包含了指定值中的任何一个值，那么这个文档满足条件： { “terms”: { “tag”: [ “search”, “full_text”, “nosql” ] }} exists 查询和 missing 查询被用于查找那些指定字段中有值 (exists) 或无值 (missing) 的文档。这与SQL中的 IS_NULL (missing) 和 NOT IS_NULL (exists) 在本质上具有共性：&#123; "exists": &#123; "field": "title" &#125;&#125; 你可以用 bool 查询来实现你的需求。这种查询将多查询组合在一起，成为用户自己想要的布尔查询。它接收以下参数： must文档 必须 匹配这些条件才能被包含进来。must_not文档 必须不 匹配这些条件才能被包含进来。should如果满足这些语句中的任意语句，将增加 _score ，否则，无任何影响。它们主要用于修正每个文档的相关性得分。filter必须 匹配，但它以不评分、过滤模式来进行。这些语句对评分没有贡献，只是根据过滤标准来排除或包含文档。由于这是我们看到的第一个包含多个查询的查询，所以有必要讨论一下相关性得分是如何组合的。每一个子查询都独自地计算文档的相关性得分。一旦他们的得分被计算出来， bool 查询就将这些得分进行合并并且返回一个代表整个布尔操作的得分。 下面的查询用于查找 title 字段匹配 how to make millions 并且不被标识为 spam 的文档。那些被标识为 starred 或在2014之后的文档，将比另外那些文档拥有更高的排名。如果 两者 都满足，那么它排名将更高：&#123; "bool": &#123; "must": &#123; "match": &#123; "title": "how to make millions" &#125;&#125;, "must_not": &#123; "match": &#123; "tag": "spam" &#125;&#125;, "should": [ &#123; "match": &#123; "tag": "starred" &#125;&#125;, &#123; "range": &#123; "date": &#123; "gte": "2014-01-01" &#125;&#125;&#125; ] &#125;&#125; 如果没有 must 语句，那么至少需要能够匹配其中的一条 should 语句。但，如果存在至少一条 must 语句，则对 should 语句的匹配没有要求。&#123; "bool": &#123; "must": &#123; "match": &#123; "title": "how to make millions" &#125;&#125;, "must_not": &#123; "match": &#123; "tag": "spam" &#125;&#125;, "should": [ &#123; "match": &#123; "tag": "starred" &#125;&#125; ], "filter": &#123; "range": &#123; "date": &#123; "gte": "2014-01-01" &#125;&#125; &#125; &#125;&#125; 过将 range 查询移到 filter 语句中，我们将它转成不评分的查询，将不再影响文档的相关性排名。由于它现在是一个不评分的查询，可以使用各种对 filter 查询有效的优化手段来提升性能。 对于合法查询，使用 explain 参数将返回可读的描述，这对准确理解 Elasticsearch 是如何解析你的 query 是非常有用的：GET /_validate/query?explain&#123; "query": &#123; "match" : &#123; "tweet" : "really powerful" &#125; &#125;&#125; 排序与相关性GET /_search&#123; "query" : &#123; "bool" : &#123; "must": &#123; "match": &#123; "tweet": "manage text search" &#125;&#125;, "filter" : &#123; "term" : &#123; "user_id" : 2 &#125;&#125; &#125; &#125;, "sort": [ &#123; "date": &#123; "order": "desc" &#125;&#125;, &#123; "_score": &#123; "order": "desc" &#125;&#125; ]&#125; 对于数字或日期，你可以将多值字段减为单值，这可以通过使用 min 、 max 、 avg 或是 sum 排序模式 。 例如你可以按照每个 date 字段中的最早日期进行排序，通过以下方法："sort": &#123; "dates": &#123; "order": "asc", "mode": "min" &#125;&#125; 所有的 _core_field 类型 (strings, numbers, Booleans, dates) 接收一个 fields 参数 该参数允许你转化一个简单的映射如："tweet": &#123; "type": "string", "analyzer": "english"&#125; 为一个多字段映射如："tweet": &#123; "type": "string", "analyzer": "english", "fields": &#123; "raw": &#123; "type": "string", "index": "not_analyzed" &#125; &#125;&#125; 现在，至少只要我们重新索引了我们的数据，使用 tweet 字段用于搜索，tweet.raw 字段用于排序：GET /_search&#123; "query": &#123; "match": &#123; "tweet": "elasticsearch" &#125; &#125;, "sort": "tweet.raw"&#125; 以全文 analyzed 字段排序会消耗大量的内存 相关性查询语句会为每个文档生成一个 _score 字段。评分的计算方式取决于查询类型 不同的查询语句用于不同的目的： fuzzy 查询会计算与关键词的拼写相似程度，terms 查询会计算 找到的内容与关键词组成部分匹配的百分比，但是通常我们说的 relevance 是我们用来计算全文本字段的值相对于全文本检索词相似程度的算法。 Elasticsearch 的相似度算法 被定义为检索词频率/反向文档频率， TF/IDF. 解释为什么匹配或不匹配GET /us/tweet/12/_explain&#123; "query" : &#123; "bool" : &#123; "filter" : &#123; "term" : &#123; "user_id" : 2 &#125;&#125;, "must" : &#123; "match" : &#123; "tweet" : "honeymoon" &#125;&#125; &#125; &#125;&#125; 在 Elasticsearch 中，doc values 就是一种列式存储结构，默认情况下每个字段的 doc values 都是激活的，doc values 是在索引时创建的，当字段索引时，Elasticsearch 为了能够快速检索，会把字段的值加入倒排索引中，同时它也会存储该字段的 doc values。 执行分布式检索搜索被执行成一个两阶段过程，我们称之为 query then fetchps:查询阶段+取回阶段 查询阶段查询阶段包含以下三个步骤: 1.客户端发送一个 search 请求到 Node 3 ， Node 3 会创建一个大小为 from + size 的空优先队列。2.Node 3 将查询请求转发到索引的每个主分片或副本分片中。每个分片在本地执行查询并添加结果到大小为 from + size 的本地有序优先队列中。3.每个分片返回各自优先队列中所有文档的 ID 和排序值给协调节点，也就是 Node 3 ，它合并这些值到自己的优先队列中来产生一个全局排序后的结果列表。 每个分片在本地执行查询请求并且创建一个长度为 from + size 的优先队列—也就是说，每个分片创建的结果集足够大，均可以满足全局的搜索请求。 分片返回一个轻量级的结果列表到协调节点，它仅包含文档 ID 集合以及任何排序需要用到的值，例如 _score 。 协调节点将这些分片级的结果合并到自己的有序优先队列里，它代表了全局排序结果集合。至此查询过程结束。ps:from 90 size 10 ，需要100长度的队列。 取回阶段分布式阶段由以下步骤构成： 1.协调节点辨别出哪些文档需要被取回并向相关的分片提交多个 GET 请求。2.每个分片加载并 丰富 文档，如果有需要的话，接着返回文档给协调节点。3.一旦所有的文档都被取回了，协调节点返回结果给客户端。 深分页（Deep Pagination） 给 10,000 到 50,000 的结果文档深分页（ 1,000 到 5,000 页）是完全可行的。但是使用足够大的 from 值，排序过程可能会变得非常沉重，使用大量的CPU、内存和带宽。因为这个原因，我们强烈建议你不要使用深分页。 实际上， “深分页” 很少符合人的行为。当2到3页过去以后，人会停止翻页，并且改变搜索标准。会不知疲倦地一页一页的获取网页直到你的服务崩溃的罪魁祸首一般是机器人或者web spider。 如果你 确实 需要从你的集群取回大量的文档，你可以通过用 scroll 查询禁用排序使这个取回行为更有效率，我们会在 later in this chapter 进行讨论。 超时问题通常分片处理完它所有的数据后再把结果返回给协同节点，协同节点把收到的所有结果合并为最终结果。 这意味着花费的时间是最慢分片的处理时间加结果合并的时间。如果有一个节点有问题，就会导致所有的响应缓慢。 参数 timeout 告诉 分片允许处理数据的最大时间。如果没有足够的时间处理所有数据，这个分片的结果可以是部分的，甚至是空数据。 搜索的返回结果会用属性 timed_out 标明分片是否返回的是部分结果： ... &quot;timed_out&quot;: true, ... 游标查询 Scrollscroll 查询 可以用来对 Elasticsearch 有效地执行大批量的文档查询，而又不用付出深度分页那种代价。 游标查询允许我们 先做查询初始化，然后再批量地拉取结果。 这有点儿像传统数据库中的 cursor 。 GET /old_index/_search?scroll=1m&#123; "query": &#123; "match_all": &#123;&#125;&#125;, "sort" : ["_doc"], "size": 1000&#125; 保持游标查询窗口一分钟。 关键字 _doc 是最有效的排序顺序。 这个查询的返回结果包括一个字段 _scroll_id， 它是一个base64编码的长字符串 (((&quot;scroll_id&quot;))) 。 现在我们能传递字段_scroll_id 到 _search/scroll 查询接口获取下一批结果：GET /_search/scroll&#123; "scroll": "1m", "scroll_id" : "cXVlcnlUaGVuRmV0Y2g7NTsxMDk5NDpkUmpiR2FjOFNhNnlCM1ZDMWpWYnRROzEwOTk1OmRSamJHYWM4U2E2eUIzVkMxalZidFE7MTA5OTM6ZFJqYkdhYzhTYTZ5QjNWQzFqVmJ0UTsxMTE5MDpBVUtwN2lxc1FLZV8yRGVjWlI2QUVBOzEwOTk2OmRSamJHYWM4U2E2eUIzVkMxalZidFE7MDs="&#125; 注意再次设置游标查询过期时间为一分钟。 这个游标查询返回的下一批结果。 尽管我们指定字段 size 的值为1000，我们有可能取到超过这个值数量的文档。 当查询的时候， 字段 size 作用于单个分片，所以每个批次实际返回的文档数量最大为 size * number_of_primary_shards 。 索引管理创建索引PUT /my_index&#123; "settings": &#123; ... any settings ... &#125;, "mappings": &#123; "type_one": &#123; ... any mappings ... &#125;, "type_two": &#123; ... any mappings ... &#125;, ... &#125;&#125; 删除索引用以下的请求来 删除索引: DELETE /my_index你也可以这样删除多个索引： DELETE /indexone,index_twoDELETE /index*你甚至可以这样删除 全部 索引： DELETE /_allDELETE /* 设置索引 下面是两个 最重要的设置： number_of_shards每个索引的主分片数，默认值是 5 。这个配置在索引创建后不能修改。number_of_replicas每个主分片的副本数，默认值是 1 。对于活动的索引库，这个配置可以随时修改。 可以用 update-index-settings API 动态修改副本数：PUT /my_temp_index/_settings&#123; "number_of_replicas": 1&#125; 创建自定义分析器PUT /my_index&#123; "settings": &#123; "analysis": &#123; "char_filter": &#123; ... custom character filters ... &#125;, "tokenizer": &#123; ... custom tokenizers ... &#125;, "filter": &#123; ... custom token filters ... &#125;, "analyzer": &#123; ... custom analyzers ... &#125; &#125; &#125;&#125; 分析器应用在一个 string 字段上：PUT /my_index/_mapping/my_type&#123; "properties": &#123; "title": &#123; "type": "string", "analyzer": "my_analyzer" &#125; &#125;&#125; 类型和映射如果有两个不同的类型，每个类型都有同名的字段，但映射不同（例如：一个是字符串一个是数字），将会出现什么情况？ 简单回答是，Elasticsearch 不会允许你定义这个映射。当你配置这个映射时，将会出现异常。 Lucene 没有文档类型的概念，每个文档的类型名被存储在一个叫 _type 的元数据字段上。 当我们要检索某个类型的文档时, Elasticsearch 通过在 _type 字段上使用过滤器限制只返回这个类型的文档。 Lucene 也没有映射的概念。 映射是 Elasticsearch 将复杂 JSON 文档 映射 成 Lucene 需要的扁平化数据的方式。 重要的一点是: 类型可以很好的区分同一个集合中的不同细分。在不同的细分中数据的整体模式是相同的（或相似的）。 类型不适合 完全不同类型的数据 。如果两个类型的字段集是互不相同的，这就意味着索引中将有一半的数据是空的（字段将是 稀疏的 ），最终将导致性能问题。在这种情况下，最好是使用两个单独的索引。 根对象_source 字段在被写入磁盘之前先会被压缩。这个字段的存储几乎总是我们想要的，因为它意味着下面的这些： 搜索结果包括了整个可用的文档——不需要额外的从另一个的数据仓库来取文档。 如果没有 _source 字段，部分 update 请求不会生效。 当你的映射改变时，你需要重新索引你的数据，有了_source字段你可以直接从Elasticsearch这样做，而不必从另一个（通常是速度更慢的）数据仓库取回你的所有文档。 当你不需要看到整个文档时，单个字段可以从 _source 字段提取和通过 get 或者 search 请求返回。 调试查询语句更加简单，因为你可以直接看到每个文档包括什么，而不是从一列id猜测它们的内容。然而，存储 _source 字段的确要使用磁盘空间。如果上面的原因对你来说没有一个是重要的，你可以用下面的映射禁用 _source 字段：PUT /my_index&#123; "mappings": &#123; "my_type": &#123; "_source": &#123; "enabled": false &#125; &#125; &#125;&#125; 在一个搜索请求里，你可以通过在请求体中指定 _source 参数，来达到只获取特定的字段的效果：GET /_search&#123; "query": &#123; "match_all": &#123;&#125;&#125;, "_source": [ "title", "created" ]&#125; _all 字段：一个把其它字段值 当作一个大字符串来索引的特殊字段。 query_string 查询子句(搜索 ?q=john )在没有指定字段时默认使用 _all 字段。 relevance algorithm 考虑的一个最重要的原则是字段的长度：字段越短越重要。 在较短的 title 字段中出现的短语可能比在较长的 content 字段中出现的短语更加重要。字段长度的区别在 _all 字段中不会出现。 如果你不再需要 _all 字段，你可以通过下面的映射来禁用：PUT /my_index/_mapping/my_type&#123; "my_type": &#123; "_all": &#123; "enabled": false &#125; &#125;&#125; 你可以为所有字段默认禁用 include_in_all 选项，仅在你选择的字段上启用：PUT /my_index/my_type/_mapping&#123; "my_type": &#123; "include_in_all": false, "properties": &#123; "title": &#123; "type": "string", "include_in_all": true &#125;, ... &#125; &#125;&#125; 记住，_all 字段仅仅是一个 经过分词的 string 字段。它使用默认分词器来分析它的值，不管这个值原本所在字段指定的分词器。就像所有 string 字段，你可以配置 _all 字段使用的分词器：PUT /my_index/my_type/_mapping&#123; "my_type": &#123; "_all": &#123; "analyzer": "whitespace" &#125; &#125;&#125; 文档标识与四个元数据字段 相关： _id文档的 ID 字符串 _type文档的类型名 _index文档所在的索引 _uid_type 和 _id 连接在一起构造成 type#id 默认情况下， _uid 字段是被存储（可取回）和索引（可搜索）的。 _type 字段被索引但是没有存储， _id 和 _index 字段则既没有被索引也没有被存储，这意味着它们并不是真实存在的。 动态映射幸运的是可以用 dynamic 配置来控制这种行为 ，可接受的选项如下： true动态添加新的字段—缺省false忽略新的字段strict如果遇到新字段抛出异常 把 dynamic 设置为 false 一点儿也不会改变 _source 的字段内容。 _source 仍然包含被索引的整个JSON文档。只是新的字段不会被加到映射中也不可搜索。 日期检测可以通过在根对象上设置 date_detection 为 false 来关闭： PUT /my_index{ “mappings”: { “my_type”: { “date_detection”: false } }} 使用 dynamic_templates ，你可以完全控制 新检测生成字段的映射。你甚至可以通过字段名称或数据类型来应用不同的映射。 一个索引中的所有类型共享相同的字段和设置。 default 映射更加方便地指定通用设置，而不是每次创建新类型时都要重复设置。 default 映射是新类型的模板。我们可以使用 default 映射为所有的类型禁用 _all 字段， 而只在 blog 类型启用： PUT /myindex{ “mappings”: { “_default“: { “_all”: { “enabled”: false } }, “blog”: { “_all”: { “enabled”: true } } }} 重新索引数据从旧索引复制到新索引POST _reindex&#123; "source": &#123; "index": "twitter" &#125;, "dest": &#123; "index": "new_twitter" &#125;&#125; ps:需要先准备好目标索引ps:https://www.elastic.co/guide/en/elasticsearch/reference/current/docs-reindex.html 索引 别名 就像一个快捷方式或软连接，可以指向一个或多个索引，也可以给任何一个需要索引名的API来使用。别名 带给我们极大的灵活性，允许我们做下面这些： 在运行的集群中可以无缝的从一个索引切换到另一个索引给多个索引分组 (例如， last_three_months)给索引的一个子集创建 视图 首先，创建索引 my_index_v1 ，然后将别名 my_index 指向它： PUT /my_index_v1PUT /my_index_v1/_alias/my_index 你可以检测这个别名指向哪一个索引： GET /*/_alias/my_index或哪些别名指向这个索引： GET /my_index_v1/_alias/* 在你的应用中使用别名而不是索引名。然后你就可以在任何时候重建索引。别名的开销很小，应该广泛使用。 分片内部原理倒排索引被写入磁盘后是 不可改变 的:它永远不会修改。 文档更新也是类似的操作方式：当一个文档被更新时，旧版本文档被标记删除，文档的新版本被索引到一个新的段中。 可能两个版本的文档都会被一个查询匹配到，但被删除的那个旧版本文档在结果集返回前就已经被移除。 在 Elasticsearch 中，写入和打开一个新段的轻量的过程叫做 refresh 。 默认情况下每个分片会每秒自动刷新一次。这就是为什么我们说 Elasticsearch 是 近 实时搜索: 文档的变化并不是立即对搜索可见，但会在一秒之内变为可见。 不要在生产环境下每次索引一个文档都去手动刷新。 相反，你的应用需要意识到 Elasticsearch 的近实时的性质，并接受它的不足。 在生产环境中，当你正在建立一个大的新索引时，可以先关闭自动刷新，待开始使用该索引时，再把它们调回来： PUT /my_logs/_settings{ “refresh_interval”: -1 } PUT /my_logs/_settings{ “refresh_interval”: “1s” } refresh_interval 需要一个 持续时间 值， 例如 1s （1 秒） 或 2m （2 分钟）。 一个绝对值 1 表示的是 1毫秒 —无疑会使你的集群陷入瘫痪。 这个执行一个提交并且截断 translog 的行为在 Elasticsearch 被称作一次 flush 。 分片每30分钟被自动刷新（flush），或者在 translog 太大的时候也会刷新。 在文件被 fsync 到磁盘前，被写入的文件在重启之后就会丢失。默认 translog 是每 5 秒被 fsync 刷新到硬盘， 或者在每次写请求完成之后执行(e.g. index, delete, update, bulk)。 段合并的时候会将那些旧的已删除文档 从文件系统中清除。 被删除的文档（或被更新文档的旧版本）不会被拷贝到新的大段中。 optimize API大可看做是 强制合并 API 。它会将一个分片强制合并到 max_num_segments 参数指定大小的段数目。 这样做的意图是减少段的数量（通常减少到一个），来提升搜索性能。 使用optimize优化老的索引，将每一个分片合并为一个单独的段就很有用了；这样既可以节省资源，也可以使搜索更加快速： POST /logstash-2014-10/_optimize?max_num_segments=1 请注意，使用 optimize API 触发段合并的操作一点也不会受到任何资源上的限制。这可能会消耗掉你节点上全部的I/O资源, 使其没有余裕来处理搜索请求，从而有可能使集群失去响应。 如果你想要对索引执行 optimize，你需要先使用分片分配（查看 迁移旧索引）把索引移到一个安全的节点，再执行。 深入搜索结构化搜索通常当查找一个精确值的时候，我们不希望对查询进行评分计算。只希望对文档进行包括或排除的计算，所以我们会使用 constant_score 查询以非评分模式来执行 term 查询并以一作为统一评分。 最终组合的结果是一个 constant_score 查询，它包含一个 term 查询： GET /my_store/products/_search{ “query” : { “constant_score” : { “filter” : { “term” : { “price” : 20 } } } }}从概念上记住非评分计算是首先执行的，这将有助于写出高效又快速的搜索请求。 一个 bool 过滤器由三部分组成：&#123; "bool" : &#123; "must" : [], "should" : [], "must_not" : [], &#125;&#125; must所有的语句都 必须（must） 匹配，与 AND 等价。must_not所有的语句都 不能（must not） 匹配，与 NOT 等价。should至少有一个语句要匹配，与 OR 等价。GET /my_store/products/_search&#123; "query" : &#123; "filtered" : &#123; "filter" : &#123; "bool" : &#123; "should" : [ &#123; "term" : &#123;"productID" : "KDKE-B-9947-#kL5"&#125;&#125;, &#123; "bool" : &#123; "must" : [ &#123; "term" : &#123;"productID" : "JODL-X-1937-#pV7"&#125;&#125;, &#123; "term" : &#123;"price" : 30&#125;&#125; ] &#125;&#125; ] &#125; &#125; &#125; &#125;&#125; term 和 terms 是 包含（contains） 操作，而非 等值（equals） （判断）。 如何理解这句话呢？ 如果我们有一个 term（词项）过滤器 { “term” : { “tags” : “search” } } ，它会与以下两个文档 同时 匹配： { “tags” : [“search”] }{ “tags” : [“search”, “open_source”] } 尽管第二个文档包含除 search 以外的其他词，它还是被匹配并作为结果返回。 如果一定期望得到我们前面说的那种行为（即整个字段完全相等），最好的方式是增加并索引另一个字段， 这个字段用以存储该字段包含词项的数量，同样以上面提到的两个文档为例，现在我们包括了一个维护标签数的新字段： { “tags” : [“search”], “tag_count” : 1 }{ “tags” : [“search”, “open_source”], “tag_count” : 2 } 范围查询range 支持数值、日期、字符串 “range” : { “timestamp” : { “gt” : “2014-01-01 00:00:00”, “lt” : “2014-01-07 00:00:00” }} 日期计算“range” : { “timestamp” : { “gt” : “now-1h” # 过去一个小时 }}“range” : { “timestamp” : { “gt” : “2014-01-01 00:00:00”, “lt” : “2014-01-01 00:00:00||+1M” #加一个月 }}时间格式参考文档 字符串范围可采用 字典顺序（lexicographically） 或字母顺序（alphabetically）。例如，下面这些字符串是采用字典序（lexicographically）排序的： 5, 50, 6, B, C, a, ab, abb, abc, b “range” : { “title” : { “gte” : “a”, “lt” : “b” }}字符串范围在过滤 低基数（low cardinality） 字段（即只有少量唯一词项）时可以正常工作，但是唯一词项越多，字符串范围的计算会越慢。 那么我们如何用 exists 或 missing 查询 name 字段呢？ name 字段并不真实存在于倒排索引中。 原因是当我们执行下面这个过滤的时候： { “exists” : { “field” : “name” }}实际执行的是： { “bool”: { “should”: [ { “exists”: { “field”: “name.first” }}, { “exists”: { “field”: “name.last” }} ] }}这也就意味着，如果 first 和 last 都是空，那么 name 这个命名空间才会被认为不存在。 Elasticsearch 会基于使用频次自动缓存查询。如果一个非评分查询在最近的 256 词查询中被使用过（次数取决于查询类型），那么这个查询就会作为缓存的候选。但是，并不是所有的片段都能保证缓存 bitset 。只有那些文档数量超过 10,000 （或超过总文档数量的 3% )才会缓存 bitset 。因为小的片段可以很快的进行搜索和合并，这里缓存的意义不大。 全文搜索基于词项与基于全文文本查询可以划分成两大家族： 基于词项的查询如 term 或 fuzzy 这样的底层查询不需要分析阶段，它们对单个词项进行操作。用 term 查询词项 Foo 只要在倒排索引中查找 准确词项 ，并且用 TF/IDF 算法为每个包含该词项的文档计算相关度评分 _score 。记住 term 查询只对倒排索引的词项精确匹配，这点很重要，它不会对词的多样性进行处理（如， foo 或 FOO ）。这里，无须考虑词项是如何存入索引的。如果是将 [“Foo”,”Bar”] 索引存入一个不分析的（ not_analyzed ）包含精确值的字段，或者将 Foo Bar 索引到一个带有 whitespace 空格分析器的字段，两者的结果都会是在倒排索引中有 Foo 和 Bar 这两个词。 基于全文的查询像 match 或 query_string 这样的查询是高层查询，它们了解字段映射的信息：如果查询 日期（date） 或 整数（integer）字段，它们会将查询字符串分别作为日期或整数对待。如果查询一个（ not_analyzed ）未分析的精确值字符串字段， 它们会将整个查询字符串作为单个词项对待。但如果要查询一个（ analyzed ）已分析的全文字段， 它们会先将查询字符串传递到一个合适的分析器，然后生成一个供查询的词项列表。一旦组成了词项列表，这个查询会对每个词项逐一执行底层的查询，再将结果合并，然后为每个文档生成一个最终的相关度评分。 Elasticsearch 执行上面这个 match 查询的步骤是： 1.检查字段类型 。 2.分析查询字符串 。 3.查找匹配文档 。 4.为每个文档评分 。 多词查询因为 match 查询必须查找两个词（ [“brown”,”dog”] ），它在内部实际上先执行两次 term 查询，然后将两次查询的结果合并作为最终结果输出。 match 查询还可以接受 operator 操作符作为输入参数，默认情况下该操作符是 or 。我们可以将它修改成 and 让所有指定词项都必须匹配： GET /my_index/my_type/_search&#123; "query": &#123; "match": &#123; "title": &#123; "query": "BROWN DOG!", "operator": "and" &#125; &#125; &#125;&#125; match 查询支持 minimum_should_match 最小匹配参数， 这让我们可以指定必须匹配的词项数用来表示一个文档是否相关。我们可以将其设置为某个具体数字，更常用的做法是将其设置为一个百分数，因为我们无法控制用户搜索时输入的单词数量： GET /my_index/my_type/_search&#123; "query": &#123; "match": &#123; "title": &#123; "query": "quick brown dog", "minimum_should_match": "75%" &#125; &#125; &#125;&#125; 组合查询GET /my_index/my_type/_search&#123; "query": &#123; "bool": &#123; "must": &#123; "match": &#123; "title": "quick" &#125;&#125;, "must_not": &#123; "match": &#123; "title": "lazy" &#125;&#125;, "should": [ &#123; "match": &#123; "title": "brown" &#125;&#125;, &#123; "match": &#123; "title": "dog" &#125;&#125; ] &#125; &#125;&#125; 拷贝为 CURL在 SENSE 中查看以上的查询结果返回 title 字段包含词项 quick 但不包含 lazy 的任意文档。目前为止，这与 bool 过滤器的工作方式非常相似。 ==区别就在于两个 should 语句，也就是说：一个文档不必包含 brown 或 dog 这两个词项，但如果一旦包含，我们就认为它们 更相关== bool 查询会为每个文档计算相关度评分 _score ， 再将所有匹配的 must 和 should 语句的分数 _score 求和，最后除以 must 和 should 语句的总数。 must_not 语句不会影响评分； 它的作用只是将不相关的文档排除。默认情况下，没有 should 语句是必须匹配的，只有一个例外：那就是当没有 must 语句的时候，至少有一个 should 语句必须匹配。 如何使用布尔匹配以下两个查询是等价的：&#123; "match": &#123; "title": "brown fox"&#125;&#125;&#123; "bool": &#123; "should": [ &#123; "term": &#123; "title": "brown" &#125;&#125;, &#123; "term": &#123; "title": "fox" &#125;&#125; ] &#125;&#125; 查询语句提升权重我们可以通过指定 boost 来控制任何查询语句的相对的权重， boost 的默认值为 1 ，大于 1 会提升一个语句的相对权重。所以下面重写之前的查询：GET /_search&#123; "query": &#123; "bool": &#123; "must": &#123; "match": &#123; "content": &#123; "query": "full text search", "operator": "and" &#125; &#125; &#125;, "should": [ &#123; "match": &#123; "content": &#123; "query": "Elasticsearch", "boost": 3 &#125; &#125;&#125;, &#123; "match": &#123; "content": &#123; "query": "Lucene", "boost": 2 &#125; &#125;&#125; ] &#125; &#125;&#125; 控制分析分析器可以从三个层面进行定义：按字段（per-field）、按索引（per-index）或全局缺省（global default）。Elasticsearch 会按照以下顺序依次处理，直到它找到能够使用的分析器。 索引时的顺序如下：字段映射里定义的 analyzer ，否则索引设置中名为 default 的分析器，默认为standard 标准分析器 在搜索时，顺序有些许不同：查询自己定义的 analyzer ，否则字段映射里定义的 analyzer ，否则索引设置中名为 default 的分析器，默认为standard 标准分析器 持一个可选的 search_analyzer 映射，它仅会应用于搜索时（ analyzer 还用于索引时）。还有一个等价的 default_search 映射，用以指定索引层的默认配置。 如果考虑到这些额外参数，一个搜索时的 完整 顺序会是下面这样： 查询自己定义的 analyzer ，否则字段映射里定义的 search_analyzer ，否则字段映射里定义的 analyzer ，否则索引设置中名为 default_search 的分析器，默认为索引设置中名为 default 的分析器，默认为standard 标准分析器 ==最简单的途径就是在创建索引或者增加类型映射时，为每个全文字段设置分析器。这种方式尽管有点麻烦，但是它让我们可以清楚的看到每个字段每个分析器是如何设置的。可以在索引级别设置中，为绝大部分的字段设置你想指定的 default 默认分析器。然后在字段级别设置中，对某一两个字段配置需要指定的分析器。== 被破坏的相关度！由于性能原因， Elasticsearch 不会计算索引内所有文档的 IDF 。 相反，每个分片会根据 该分片 内的所有文档计算一个本地 IDF 。 在实际应用中，这并不是一个问题，本地和全局的 IDF 的差异会随着索引里文档数的增多渐渐消失，在真实世界的数据量下，局部的 IDF 会被迅速均化，所以上述问题并不是相关度被破坏所导致的，而是由于数据太少。 在搜索请求后添加 ?search_type=dfs_query_then_fetch ， dfs 是指 分布式频率搜索（Distributed Frequency Search） ， 它告诉 Elasticsearch ，先分别获得每个分片本地的 IDF ，然后根据结果再计算整个索引的全局 IDF 。不要在生产环境上使用 dfs_query_then_fetch 。完全没有必要。只要有足够的数据就能保证词频是均匀分布的。没有理由给每个查询额外加上 DFS 这步。 多字段搜索多字符串查询GET /_search&#123; "query": &#123; "bool": &#123; "should": [ &#123; "match": &#123; "title": "War and Peace" &#125;&#125;, &#123; "match": &#123; "author": "Leo Tolstoy" &#125;&#125;, &#123; "bool": &#123; "should": [ &#123; "match": &#123; "translator": "Constance Garnett" &#125;&#125;, &#123; "match": &#123; "translator": "Louise Maude" &#125;&#125; ] &#125;&#125; ] &#125; &#125;&#125; 答案在于评分的计算方式。 bool 查询运行每个 match 查询，再把评分加在一起，然后将结果与所有匹配的语句数量相乘，最后除以所有的语句数量。处于同一层的每条语句具有相同的权重。在前面这个例子中，包含 translator 语句的 bool 查询，只占总评分的三分之一。如果将 translator 语句与 title 和 author 两条语句放入同一层，那么 title 和 author 语句只贡献四分之一评分。 单字符串查询最佳字段dis_max 查询不使用 bool 查询，可以使用 dis_max 即分离 最大化查询（Disjunction Max Query） 。分离（Disjunction）的意思是 或（or） ，这与可以把结合（conjunction）理解成 与（and） 相对应。分离最大化查询（Disjunction Max Query）指的是： 将任何与任一查询匹配的文档作为结果返回，但==只将最佳匹配的评分作为查询的评分结果返回== ： &#123; "query": &#123; "dis_max": &#123; "queries": [ &#123; "match": &#123; "title": "Brown fox" &#125;&#125;, &#123; "match": &#123; "body": "Brown fox" &#125;&#125; ] &#125; &#125;&#125; 最佳字段查询调优tie_breaker 参数可以通过指定 tie_breaker 这个参数将其他匹配语句的评分也考虑其中：&#123; "query": &#123; "dis_max": &#123; "queries": [ &#123; "match": &#123; "title": "Quick pets" &#125;&#125;, &#123; "match": &#123; "body": "Quick pets" &#125;&#125; ], "tie_breaker": 0.3 &#125; &#125;&#125; tie_breaker 参数提供了一种 dis_max 和 bool 之间的折中选择，它的评分方式如下： 1.获得最佳匹配语句的评分 _score 。 2.将其他匹配语句的评分结果与 tie_breaker 相乘。 3.对以上评分求和并规范化。 有了 tie_breaker ，会考虑所有匹配语句，但最佳匹配语句依然占最终结果里的很大一部分。 注意 tie_breaker 可以是 0 到 1 之间的浮点数，其中 0 代表使用 dis_max 最佳匹配语句的普通逻辑， 1 表示所有匹配语句同等重要。最佳的精确值需要根据数据与查询调试得出，但是合理值应该与零接近（处于 0.1 - 0.4 之间），这样就不会颠覆 dis_max 最佳匹配性质的根本。 multi_match 查询multi_match 查询为能在多个字段上反复执行相同查询提供了一种便捷方式。 注意multi_match 多匹配查询的类型有多种，其中的三种恰巧与 了解我们的数据 中介绍的三个场景对应，即： best_fields 、 most_fields 和 cross_fields （最佳字段、多数字段、跨字段）。 &#123; "multi_match": &#123; "query": "Quick brown fox", "type": "best_fields", "fields": [ "title", "body" ], "tie_breaker": 0.3, "minimum_should_match": "30%" &#125;&#125; 查询字段名称的模糊匹配字段名称可以用模糊匹配的方式给出：任何与模糊模式正则匹配的字段都会被包括在搜索条件中， 例如可以使用以下方式同时匹配 book_title 、 chapter_title 和 section_title （书名、章名、节名）这三个字段：&#123; "multi_match": &#123; "query": "Quick brown fox", "fields": "*_title" &#125;&#125; 提升单个字段的权重编辑可以使用 ^ 字符语法为单个字段提升权重，在字段名称的末尾添加 ^boost ， 其中 boost 是一个浮点数：&#123; "multi_match": &#123; "query": "Quick brown fox", "fields": [ "*_title", "chapter_title^2" ] &#125;&#125; chapter_title 这个字段的 boost 值为 2 ，而其他两个字段 book_title 和 section_title 字段的默认 boost 值为 1 。 多数字段多字段映射编辑首先要做的事情就是对我们的字段索引两次： 一次使用词干模式以及一次非词干模式。为了做到这点，采用 multifields 来实现，已经在 multifields 有所介绍：DELETE /my_indexPUT /my_index&#123; "settings": &#123; "number_of_shards": 1 &#125;, "mappings": &#123; "my_type": &#123; "properties": &#123; "title": &#123; "type": "string", "analyzer": "english", "fields": &#123; "std": &#123; "type": "string", "analyzer": "standard" &#125; &#125; &#125; &#125; &#125; &#125;&#125; 跨字段实体搜索&#123; "query": &#123; "multi_match": &#123; "query": "Poland Street W1V", "type": "most_fields", "fields": [ "street", "city", "country", "postcode" ] &#125; &#125;&#125; most_fields 方式的问题用 most_fields 这种方式搜索也存在某些问题，这些问题并不会马上显现： 它是为多数字段匹配 任意 词设计的，而不是在 所有字段 中找到最匹配的。 它不能使用 operator 或 minimum_should_match 参数来降低次相关结果造成的长尾效应。 词频对于每个字段是不一样的，而且它们之间的相互影响会导致不好的排序结果。 字段中心式查询解决方案存在这些问题仅仅是因为我们在处理着多个字段，如果将所有这些字段组合成单个字段，问题就会消失。可以为 person 文档添加 full_name 字段来解决这个问题：&#123; "first_name": "Peter", "last_name": "Smith", "full_name": "Peter Smith"&#125; 当查询 full_name 字段时： 具有更多匹配词的文档会比只有一个重复匹配词的文档更重要。minimum_should_match 和 operator 参数会像期望那样工作。姓和名的逆向文档频率被合并，所以 Smith 到底是作为姓还是作为名出现，都会变得无关紧要。这么做当然是可行的，但我们并不太喜欢存储冗余数据。取而代之的是 Elasticsearch 可以提供两个解决方案——一个在索引时，而另一个是在搜索时——随后会讨论它们。 自定义 _all 字段Elasticsearch 在字段映射中为我们提供 copy_to 参数来实现这个功能：PUT /my_index&#123; "mappings": &#123; "person": &#123; "properties": &#123; "first_name": &#123; "type": "string", "copy_to": "full_name" &#125;, "last_name": &#123; "type": "string", "copy_to": "full_name" &#125;, "full_name": &#123; "type": "string" &#125; &#125; &#125; &#125;&#125; first_name 和 last_name 字段中的值会被复制到 full_name 字段。 cross-fields 跨字段查询cross_fields 使用词中心式（term-centric）的查询方式，这与 best_fields 和 most_fields 使用字段中心式（field-centric）的查询方式非常不同，它将所有字段当成一个大字段，并在 每个字段 中查找 每个词 。GET /_validate/query?explain&#123; "query": &#123; "multi_match": &#123; "query": "peter smith", "type": "cross_fields", "operator": "and", "fields": [ "first_name", "last_name" ] &#125; &#125;&#125; 用 cross_fields 词中心式匹配。 它通过 混合 不同字段逆向索引文档频率的方式解决了词频的问题： +blended(&quot;peter&quot;, fields: [first_name, last_name])+blended(&quot;smith&quot;, fields: [first_name, last_name]) ==自定义单字段查询是否能够优于多字段查询，取决于在多字段查询与单字段自定义 _all 之间代价的权衡，即哪种解决方案会带来更大的性能优化就选择哪一种。== Exact-Value 精确值字段在 multi_match 查询中避免使用 not_analyzed 字段。 近似匹配短语匹配match_phrase 查询首先将查询字符串解析成一个词项列表，然后对这些词项进行搜索，但只保留那些包含 全部 搜索词项，且 位置 与搜索词项相同的文档。GET /my_index/my_type/_search&#123; "query": &#123; "match_phrase": &#123; "title": "quick brown fox" &#125; &#125;&#125; 混合起来slop 参数告诉 match_phrase 查询词条相隔多远时仍然能将文档视为匹配 。 相隔多远的意思是为了让查询和文档匹配你需要移动词条多少次？GET /my_index/my_type/_search&#123; "query": &#123; "match_phrase": &#123; "title": &#123; "query": "quick fox", "slop": 1 &#125; &#125; &#125;&#125; 为了使查询 fox quick 匹配我们的文档， 我们需要 slop 的值为 3: Pos 1 Pos 2 Pos 3 ----------------------------------------------- Doc: quick brown fox ----------------------------------------------- Query: fox quick Slop 1: fox|quick ↵ &lt;1&gt; Slop 2: quick ↳ fox Slop 3: quick ↳ fox 注意 fox 和 quick 在这步中占据同样的位置。 因此将 fox quick 转换顺序成 quick fox 需要两步， 或者值为 2 的 slop 。 PS:是将查询词进行移动，不是移动文档中的词。 多值字段数组相关，略。 越近越好通过设置一个像 50 或者 100 这样的高 slop 值, 你能够排除单词距离太远的文档， 但是也给予了那些单词临近的的文档更高的分数。 使用邻近度提高相关度略 性能优化短语查询和邻近查询都比简单的 query 查询代价更高 。 一个 match 查询仅仅是看词条是否存在于倒排索引中，而一个 match_phrase 查询是必须计算并比较多个可能重复词项的位置。 Lucene nightly benchmarks 表明一个简单的 term 查询比一个短语查询大约快 10 倍，比邻近查询(有 slop 的短语 查询)大约快 20 倍。当然，这个代价指的是在搜索时而不是索引时。 重新评分阶段支持一个代价更高的评分算法—比如 phrase 查询—只是为了从每个分片中获得前 K 个结果。 然后会根据它们的最新评分 重新排序。 该请求如下所示：GET /my_index/my_type/_search&#123; "query": &#123; "match": &#123; "title": &#123; "query": "quick brown fox", "minimum_should_match": "30%" &#125; &#125; &#125;, "rescore": &#123; "window_size": 50, "query": &#123; "rescore_query": &#123; "match_phrase": &#123; "title": &#123; "query": "quick brown fox", "slop": 50 &#125; &#125; &#125; &#125; &#125;&#125; match 查询决定哪些文档将包含在最终结果集中，并通过 TF/IDF 排序。window_size 是每一分片进行重新评分的顶部文档数量。目前唯一支持的重新打分算法就是另一个查询，但是以后会有计划增加更多的算法。 寻找相关词要将每个单词 以及它的邻近词 作为单个词项索引： [“sue ate”, “ate the”, “the alligator”]这些单词对（或者 bigrams ）被称为 shingles 。ps:索引三个单词（ trigrams ） Trigrams 提供了更高的精度，但是也大大增加了索引中唯一词项的数量。在大多数情况下，Bigrams 就够了。 生成 ShinglesPUT /my_index&#123; "settings": &#123; "number_of_shards": 1, "analysis": &#123; "filter": &#123; "my_shingle_filter": &#123; "type": "shingle", "min_shingle_size": 2, "max_shingle_size": 2, "output_unigrams": false &#125; &#125;, "analyzer": &#123; "my_shingle_analyzer": &#123; "type": "custom", "tokenizer": "standard", "filter": [ "lowercase", "my_shingle_filter" ] &#125; &#125; &#125; &#125;&#125;PUT /my_index/_mapping/my_type&#123; "my_type": &#123; "properties": &#123; "title": &#123; "type": "string", "fields": &#123; "shingles": &#123; "type": "string", "analyzer": "my_shingle_analyzer" &#125; &#125; &#125; &#125; &#125;&#125; 现在在查询里添加 shingles 字段。不要忘了在 shingles 字段上的匹配是充当一 种信号—为了提高相关度评分—所以我们仍然需要将基本 title 字段包含到查询中：GET /my_index/my_type/_search&#123; "query": &#123; "bool": &#123; "must": &#123; "match": &#123; "title": "the hungry alligator ate sue" &#125; &#125;, "should": &#123; "match": &#123; "title.shingles": "the hungry alligator ate sue" &#125; &#125; &#125; &#125;&#125; shingles 不仅比短语查询更灵活， 而且性能也更好。 shingles 查询跟一个简单的 match 查询一样高效，而不用每次搜索花费短语查询的代价。只是在索引期间因为更多词项需要被索引会付出一些小的代价， 这也意味着有 shingles 的字段会占用更多的磁盘空间。 然而，大多数应用写入一次而读取多次，所以在索引期间优化我们的查询速度是有意义的。 部分匹配prefix 前缀查询GET /my_index/address/_search&#123; "query": &#123; "prefix": &#123; "postcode": "W1" &#125; &#125;&#125; prefix 查询或过滤对于一些特定的匹配是有效的，但使用方式还是应当注意。 当字段中词的集合很小时，可以放心使用，但是它的伸缩性并不好，会对我们的集群带来很多压力。可以使用较长的前缀来限制这种影响，减少需要访问的量。 通配符与正则表达式查询wildcard 通配符查询: ? 匹配任意字符， * 匹配 0 或多个字符. GET /my_index/address/_search&#123; "query": &#123; "wildcard": &#123; "postcode": "W?F*HW" &#125; &#125;&#125;GET /my_index/address/_search&#123; "query": &#123; "regexp": &#123; "postcode": "W[0-9].+" &#125; &#125;&#125; wildcard 和 regexp 查询的工作方式与 prefix 查询完全一样，它们也需要扫描倒排索引中的词列表才能找到所有匹配的词，然后依次获取每个词相关的文档 ID ，与 prefix 查询的唯一不同是：它们能支持更为复杂的匹配模式。 prefix 、 wildcard 和 regexp 查询是基于词操作的，如果用它们来查询 analyzed 字段，它们会检查字段里面的每个词，而不是将字段作为整体来处理。 查询时输入即搜索&#123; "match_phrase_prefix" : &#123; "brand" : &#123; "query": "johnnie walker bl", "max_expansions": 50, "slop": 10 &#125; &#125;&#125; 与 match_phrase 一样，它也可以接受 slop 参数（参照 slop ）让相对词序位置不那么严格：参数 max_expansions 控制着可以与前缀匹配的词的数量，它会先查找第一个与前缀 bl 匹配的词，然后依次查找搜集与之匹配的词（按字母顺序），直到没有更多可匹配的词或当数量超过 max_expansions 时结束。 索引时输入即搜索自定义分析器 autocompletePUT /my_index&#123; "settings": &#123; "number_of_shards": 1, "analysis": &#123; "filter": &#123; "autocomplete_filter": &#123; "type": "edge_ngram", "min_gram": 1, "max_gram": 20 &#125; &#125;, "analyzer": &#123; "autocomplete": &#123; "type": "custom", "tokenizer": "standard", "filter": [ "lowercase", "autocomplete_filter" ] &#125; &#125; &#125; &#125;&#125; 应用在字段PUT /my_index/my_type/_mapping&#123; "my_type": &#123; "properties": &#123; "name": &#123; "type": "string", "index_analyzer": "autocomplete", "search_analyzer": "standard" &#125; &#125; &#125;&#125; 因为大多数工作是在索引时完成的，所有的查询只要查找 brown 和 fo 这两个词，这比使用 match_phrase_prefix 查找所有以 fo 开始的词的方式要高效许多。 控制相关度相关度评分背后的理论以下三个因素——词频（term frequency）、逆向文档频率（inverse document frequency）和字段长度归一值（field-length norm）——是在索引时计算并存储的。 最后将它们结合在一起计算单个词在特定文档中的 权重 。 Lucene 的实用评分函数实用评分函数（practical scoring function）…………………………..score(q,d) = queryNorm(q) · coord(q,d) · ∑ ( tf(t in d) · idf(t)² · t.getBoost() · norm(t,d) ) (t in q) ………………………….. score(q,d) 是文档 d 与查询 q 的相关度评分。 queryNorm(q) 是 &lt;&gt; （新）。 coord(q,d) 是 &lt;&gt; （新）。 查询 q 中每个词 t 对于文档 d 的权重和。 tf(t in d) 是词 t 在文档 d 中的 &lt;&gt; 。 idf(t) 是词 t 的 &lt;&gt; 。 t.getBoost() 是查询中使用的 &lt;&gt;（新）。 norm(t,d) 是 &lt;&gt; ，与 &lt;&gt; （如果存在）的和（新）。 我们不建议在建立索引时对字段提升权重，有以下原因：将提升值与字段长度归一值合在单个字节中存储会丢失字段长度归一值的精度，这样会导致 Elasticsearch 不知如何区分包含三个词的字段和包含五个词的字段。要想改变索引时的提升值，就必须重新为所有文档建立索引，与此不同的是，查询时的提升值可以随着每次查询的不同而更改。如果一个索引时权重提升的字段有多个值，提升值会按照每个值来自乘，这会导致该字段的权重急剧上升。查询时赋予权重 是更为简单、清楚、灵活的选择。 查询时权重提升在实际应用中，无法通过简单的公式得出某个特定查询语句的 “正确” 权重提升值，只能通过不断尝试获得。需要记住的是 boost 只是影响相关度评分的其中一个因子；它还需要与其他因子相互竞争。在前例中， title 字段相对 content 字段可能已经有一个 “缺省的” 权重提升值，这因为在 字段长度归一值 中，标题往往比相关内容要短，所以不要想当然的去盲目提升一些字段的权重。选择权重，检查结果，如此反复。 当在多个索引中搜索时， 可以使用参数 indices_boost 来提升整个索引的权重 GET /docs_2014_*/_search&#123; "indices_boost": &#123; "docs_2014_10": 3, "docs_2014_09": 2 &#125;, "query": &#123; "match": &#123; "text": "quick brown fox" &#125; &#125;&#125; 使用查询结构修改相关度略 function_score 查询function_score 查询 是用来控制评分过程的终极武器，它允许为每个与主查询匹配的文档应用一个函数， 以达到改变甚至完全替换原始查询评分 _score 的目的。实际上，也能用过滤器对结果的 子集 应用不同的函数，这样一箭双雕：既能高效评分，又能利用过滤器缓存。 Elasticsearch 预定义了一些函数： weight为每个文档应用一个简单而不被规范化的权重提升值：当 weight 为 2 时，最终结果为 2 * _score 。 field_value_factor使用这个值来修改 _score ，如将 popularity 或 votes （受欢迎或赞）作为考虑因素。 random_score为每个用户都使用一个不同的随机评分对结果排序，但对某一具体用户来说，看到的顺序始终是一致的。 衰减函数 —— linear 、 exp 、 gauss将浮动值结合到评分 _score 中，例如结合 publish_date 获得最近发布的文档，结合 geo_location 获得更接近某个具体经纬度（lat/lon）地点的文档，结合 price 获得更接近某个特定价格的文档。 script_score如果需求超出以上范围时，用自定义脚本可以完全控制评分计算，实现所需逻辑。如果没有 function_score 查询，就不能将全文查询与最新发生这种因子结合在一起评分，而不得不根据评分 _score 或时间 date 进行排序；这会相互影响抵消两种排序各自的效果。这个查询可以使两个效果融合：可以仍然根据全文相关度进行排序，但也会同时考虑最新发布文档、流行文档、或接近用户希望价格的产品。 按受欢迎度提升权重GET /blogposts/post/_search&#123; "query": &#123; "function_score": &#123; "query": &#123; "multi_match": &#123; "query": "popularity", "fields": [ "title", "content" ] &#125; &#125;, "field_value_factor": &#123; "field": "votes", "modifier": "log1p" &#125; &#125; &#125;&#125; 修饰语 modifier 的值可以为： none （默认状态）、 log 、 log1p 、 log2p 、 ln 、 ln1p 、 ln2p 、 square 、 sqrt 以及 reciprocal 。想要了解更多信息请参照： field_value_factor 文档. boost_mode或许将全文评分与 field_value_factor 函数值乘积的效果仍然可能太大， 我们可以通过参数 boost_mode 来控制函数与查询评分 _score 合并后的结果，参数接受的值为： multiply评分 _score 与函数值的积（默认）sum评分 _score 与函数值的和min评分 _score 与函数值间的较小值max评分 _score 与函数值间的较大值replace函数值替代评分 _score 可以使用 max_boost 参数限制一个函数的最大效果 GET /blogposts/post/_search&#123; "query": &#123; "function_score": &#123; "query": &#123; "multi_match": &#123; "query": "popularity", "fields": [ "title", "content" ] &#125; &#125;, "field_value_factor": &#123; "field": "votes", "modifier": "log1p", "factor": 0.1 &#125;, "boost_mode": "sum", "max_boost": 1.5 &#125; &#125;&#125; 过滤集提升权重用过滤器将结果划分为多个子集（每个特性一个过滤器），并为每个子集使用不同的函数。GET /_search&#123; "query": &#123; "function_score": &#123; "filter": &#123; "term": &#123; "city": "Barcelona" &#125; &#125;, "functions": [ &#123; "filter": &#123; "term": &#123; "features": "wifi" &#125;&#125;, "weight": 1 &#125;, &#123; "filter": &#123; "term": &#123; "features": "garden" &#125;&#125;, "weight": 1 &#125;, &#123; "filter": &#123; "term": &#123; "features": "pool" &#125;&#125;, "weight": 2 &#125; ], "score_mode": "sum", &#125; &#125;&#125; 随机评分略 越近越好（衰减函数）function_score 查询会提供一组 衰减函数（decay functions） ， 让我们有能力在两个滑动标准，如地点和价格，之间权衡。 有三种衰减函数—— linear 、 exp 和 gauss （线性、指数和高斯函数），它们可以操作数值、时间以及经纬度地理坐标点这样的字段。所有三个函数都能接受以下参数： origin中心点 或字段可能的最佳值，落在原点 origin 上的文档评分 _score 为满分 1.0 。scale衰减率，即一个文档从原点 origin 下落时，评分 _score 改变的速度。（例如，每 £10 欧元或每 100 米）。decay从原点 origin 衰减到 scale 所得的评分 _score ，默认值为 0.5 。offset以原点 origin 为中心点，为其设置一个非零的偏移量 offset 覆盖一个范围，而不只是单个原点。在范围 -offset &lt;= origin &lt;= +offset 内的所有评分 _score 都是 1.0 。 理解 price 价格语句略 脚本评分https://www.elastic.co/guide/cn/elasticsearch/guide/current/script-score.html 可插拔的相似度算法https://www.elastic.co/guide/cn/elasticsearch/guide/current/pluggable-similarites.html 更改相似度https://www.elastic.co/guide/cn/elasticsearch/guide/current/changing-similarities.html 调试相关度是最后 10% 要做的事情相关度的调试就有如兔子洞，一旦跳进去就很难再出来。 最相关 这个概念是一个难以触及的模糊目标，通常不同人对文档排序又有着不同的想法，这很容易使人陷入持续反复调整而没有明显进展的怪圈。 我们强烈建议不要陷入这种怪圈，而要监控测量搜索结果。监控用户点击最顶端结果的频次，这可以是前 10 个文档，也可以是第一页的；用户不查看首次搜索的结果而直接执行第二次查询的频次；用户来回点击并查看搜索结果的频次，等等诸如此类的信息。 处理人类语言开始处理各种语言略，混合不同语言的处理。 词汇识别官方分词插件 归一化词元略 将单词还原为词根略 停用词: 性能与精度停用词的优缺点在此基础上，从索引里将这些词移除会使我们降低某种类型的搜索能力。将前面这些所列单词移除会让我们难以完成以下事情： 区分 happy 和 not happy。搜索乐队名称 The The。查找莎士比亚的名句 “To be, or not to be” （生存还是毁灭)。使用挪威的国家代码: no。移除停用词的最主要好处是性能，假设我们在个具有上百万文档的索引中搜索单词 fox。或许fox 只在其中 20 个文档中出现，也就是说 Elasticsearch 需要计算 20 个文档的相关度评分 _score从而排出前十。现在我们把搜索条件改为 the OR fox，几乎所有的文件都包含 the 这个词，也就是说 Elasticsearch 需要为所有一百万文档计算评分_score`。 由此可见第二个查询肯定没有第一个的结果好。 幸运的是，我们可以用来保持常用词搜索，同时还可以保持良好的性能。 停用词与性能保留停用词最大的缺点就影响搜索性能。我们想要减少待评分文档的数量，最简单的方式就是在and 操作符 match 查询时使用 and 操作符， 这样可以让所有词都是必须的。 以下是 match 查询：&#123; "match": &#123; "text": &#123; "query": "the quick brown fox", "operator": "and" &#125; &#125;&#125; 最少匹配数(minimum_should_match)在精度匹配控制精度的章节里面，我们讨论过使用 minimum_should_match 配置去掉结果中次相关的长尾。 虽然它只对这个目的奏效，但是也为我们从侧面带来一个好处，它提供 and 操作符相似的性能。&#123; "match": &#123; "text": &#123; "query": "the quick brown fox", "minimum_should_match": "75%" &#125; &#125;&#125; 在上面这个示例中，四分之三的词都必须匹配，这意味着我们只需考虑那些包含最低频或次低频词的文档。 相比默认使用 or 操作符的简单查询，这为我们带来了巨大的性能提升。不过我们有办法可以做得更好…… 词项的分别管理match 查询接受一个参数 cutoff_frequency ，从而可以让它将查询字符串里的词项分为低频和高频两组。 低频组（更重要的词项）组成 bulk 大量查询条件，而高频组（次重要的词项）只会用来评分，而不参与匹配过程。通过对这两组词的区分处理，我们可以在之前慢查询的基础上获得巨大的速度提升。cutoff_frequency 会查看索引里词项的具体频率，这些词会被自动归类为 高频词汇 。&#123; "match": &#123; "text": &#123; "query": "Quick and the dead", "cutoff_frequency": 0.01 &#125;&#125; 任何词项出现在文档中超过1%，被认为是高频词。cutoff_frequency 配置可以指定为一个分数（ 0.01 ）或者一个正整数（ 5 ）。 此查询通过 cutoff_frequency 配置，将查询条件划分为低频组（ quick , dead ）和高频组（ and , the ）。然后，此查询会被重写为以下的 bool 查询：&#123; "bool": &#123; "must": &#123; "bool": &#123; "should": [ &#123; "term": &#123; "text": "quick" &#125;&#125;, &#123; "term": &#123; "text": "dead" &#125;&#125; ] &#125; &#125;, "should": &#123; "bool": &#123; "should": [ &#123; "term": &#123; "text": "and" &#125;&#125;, &#123; "term": &#123; "text": "the" &#125;&#125; ] &#125; &#125; &#125;&#125; 停用词与短语查询一个典型的索引会可能包含部分或所有以下数据： 词项字典（Terms dictionary）索引中所有文档内所有词项的有序列表，以及包含该词的文档数量。倒排表（Postings list）包含每个词项的文档（ID）列表。词频（Term frequency）每个词项在每个文档里出现的频率。位置（Positions）每个词项在每个文档里出现的位置，供短语查询或近似查询使用。偏移（Offsets）每个词项在每个文档里开始与结束字符的偏移，供词语高亮使用，默认是禁用的。规范因子（Norms）用来对字段长度进行规范化处理的因子，给较短字段予以更多权重。 我们首先应该问自己：是否真的需要使用短语查询 或 近似查询 ？ 答案通常是：不需要。在很多应用场景下，比如说日志，我们需要知道一个词 是否 在文档中（这个信息由倒排表提供）而不是关心词的位置在哪里。或许我们要对一两个字段使用短语查询，但是我们完全可以在其他 analyzed 字符串字段上禁用位置信息。 index_options 参数 允许我们控制索引里为每个字段存储的信息。 可选值如下: docs只存储文档及其包含词项的信息。这对 not_analyzed 字符串字段是默认的。freqs存储 docs 信息，以及每个词在每个文档里出现的频次。词频是完成TF/IDF 相关度计算的必要条件，但如果只想知道一个文档是否包含某个特定词项，则无需使用它。positions存储 docs 、 freqs 、 analyzed ，以及每个词项在每个文档里出现的位置。 这对 analyzed 字符串字段是默认的，但当不需使用短语或近似匹配时，可以将其禁用。offsets存储 docs,freqs,positions, 以及每个词在原始字符串中开始与结束字符的偏移信息( postings highlighter )。这个信息被用以高亮搜索结果，但它默认是禁用的。PUT /my_index&#123; "mappings": &#123; "my_type": &#123; "properties": &#123; "title": &#123; "type": "string" &#125;, "content": &#123; "type": "string", "index_options": "freqs" &#125; &#125; &#125;&#125; common_grams 过滤器common_grams 过滤器是针对短语查询能更高效的使用停用词而设计的。 停用词与相关性在索引中保留停用词会降低相关度计算的准确性，特别是当我们的文档非常长时。基于逆文档频率的影响，非常常用的词可能只有很低的权重，但是在长文档中，单个文档出现的绝对数量很大的停用词会导致这些词被不自然的加权。 同义词//TODO 拼写错误(模糊查询)//TODO 聚合高阶概念要掌握聚合，你只需要明白两个主要的概念： 桶（Buckets）满足特定条件的文档的集合指标（Metrics）对桶内的文档进行统计计算 尝试聚合GET /cars/transactions/_search&#123; "size" : 0, "aggs": &#123; "colors": &#123; "terms": &#123; "field": "color" &#125;, "aggs": &#123; "avg_price": &#123; "avg": &#123; "field": "price" &#125; &#125; &#125; &#125; &#125;&#125; 正如所见，我们用前面的例子加入了新的 aggs 层。这个新的聚合层让我们可以将 avg 度量嵌套置于 terms 桶内。实际上，这就为每个颜色生成了平均价格。 正如 颜色 的例子，我们需要给度量起一个名字（ avg_price ）这样可以稍后根据名字获取它的值。最后，我们指定度量本身（ avg ）以及我们想要计算平均值的字段（ price ） 嵌套桶 GET /cars/transactions/_search&#123; "size" : 0, "aggs": &#123; "colors": &#123; "terms": &#123; "field": "color" &#125;, "aggs": &#123; "avg_price": &#123; "avg": &#123; "field": "price" &#125; &#125;, "make" : &#123; "terms" : &#123; "field" : "make" &#125;, "aggs" : &#123; "min_price" : &#123; "min": &#123; "field": "price"&#125; &#125;, "max_price" : &#123; "max": &#123; "field": "price"&#125; &#125; &#125; &#125; &#125; &#125; &#125;&#125; 新增的这个 make 聚合，它是一个 terms 桶（嵌套在 colors 、 terms 桶内）。这意味着它 会为数据集中的每个唯一组合生成（ color 、 make ）元组。 条形图GET /cars/transactions/_search&#123; "size" : 0, "aggs":&#123; "price":&#123; "histogram":&#123; "field": "price", "interval": 20000 &#125;, "aggs":&#123; "revenue": &#123; "sum": &#123; "field" : "price" &#125; &#125; &#125; &#125; &#125;&#125; 如我们所见，查询是围绕 price 聚合构建的，它包含一个 histogram 桶。它要求字段的类型必须是数值型的同时需要设定分组的间隔范围。 间隔设置为 20,000 意味着我们将会得到如 [0-19999, 20000-39999, …] 这样的区间。 Doc Values and FielddataDoc values 可以使聚合更快、更高效并且内存友好。文档值是在索引时与倒排索引同时产生的。也就是说文档值是按段来产生的并且是不可变的，正如用于搜索的倒排索引一样。 同样，和倒排索引一样，文档值也序列化到磁盘。这些对于性能和伸缩性很重要。 ==文档值默认对所有字段启用，除了分析字符类型字段。也就是说所有的数字、地理坐标、日期、IP 和不分析（ not_analyzed ）字符类型。== 字段 “session_id” 禁用了文档值：PUT my_index&#123; "mappings": &#123; "my_type": &#123; "properties": &#123; "session_id": &#123; "type": "string", "index": "not_analyzed", "doc_values": false &#125; &#125; &#125; &#125;&#125; 地理位置//TODO Blockquote 数据建模关联关系处理 以下四种常用的方法，用来在 Elasticsearch 中进行关系型数据的管理： Application-side joinsData denormalizationNested objectsParent/child relationships 当非规范化成为很多项目的一个很好的选择，采用锁方案的需求会带来复杂的实现逻辑。 作为替代方案，Elasticsearch 提供两个模型帮助我们处理相关联的实体： 嵌套的对象 和 父子关系 。 嵌套对象嵌套对象 就是来解决这个问题的。将 comments 字段类型设置为 nested 而不是 object 后,每一个嵌套对象都会被索引为一个 隐藏的独立文档 ,举例如下: { “comments.name”: [ john, smith ], “comments.comment”: [ article, great ], “comments.age”: [ 28 ], “comments.stars”: [ 4 ], “comments.date”: [ 2014-09-01 ]}{ “comments.name”: [ alice, white ], “comments.comment”: [ like, more, please, this ], “comments.age”: [ 31 ], “comments.stars”: [ 5 ], “comments.date”: [ 2014-10-22 ]}{ “title”: [ eggs, nest ], “body”: [ making, money, work, your ], “tags”: [ cash, shares ]} 设置一个字段为 nested 很简单 — 你只需要将字段类型 object 替换为 nested 即可：PUT /my_index&#123; "mappings": &#123; "blogpost": &#123; "properties": &#123; "comments": &#123; "type": "nested", "properties": &#123; "name": &#123; "type": "string" &#125;, "comment": &#123; "type": "string" &#125;, "age": &#123; "type": "short" &#125;, "stars": &#123; "type": "short" &#125;, "date": &#123; "type": "date" &#125; &#125; &#125; &#125; &#125; &#125;&#125; 由于嵌套对象 被索引在独立隐藏的文档中，我们无法直接查询它们。 相应地，我们必须使用 nested 查询 去获取它们：默认情况下，根文档的分数是这些嵌套文档分数的平均值。可以通过设置 score_mode 参数来控制这个得分策略，相关策略有 avg (平均值), max (最大值), sum (加和) 和 none (直接返回 1.0 常数值分数)。GET /my_index/blogpost/_search&#123; "query": &#123; "bool": &#123; "must": [ &#123; "match": &#123; "title": "eggs" &#125; &#125;, &#123; "nested": &#123; "path": "comments", "score_mode": "max", "query": &#123; "bool": &#123; "must": [ &#123; "match": &#123; "comments.name": "john" &#125; &#125;, &#123; "match": &#123; "comments.age": 28 &#125; &#125; ] &#125; &#125; &#125; &#125; ] &#125; &#125;&#125; 使用嵌套字段排序假如我们想要查询在10月份收到评论的博客文章，并且按照 stars 数的最小值来由小到大排序，那么查询语句如下：GET /_search&#123; "query": &#123; "nested": &#123; "path": "comments", "filter": &#123; "range": &#123; "comments.date": &#123; "gte": "2014-10-01", "lt": "2014-11-01" &#125; &#125; &#125; &#125; &#125;, "sort": &#123; "comments.stars": &#123; "order": "asc", "mode": "min", "nested_path": "comments", "nested_filter": &#123; "range": &#123; "comments.date": &#123; "gte": "2014-10-01", "lt": "2014-11-01" &#125; &#125; &#125; &#125; &#125;&#125; 我们为什么要用 nested_path 和 nested_filter 重复查询条件呢？原因在于，排序发生在查询执行之后。 查询条件限定了只在10月份收到评论的博客文档，但返回整个博客文档。如果我们不在排序子句中加入 nested_filter ， 那么我们对博客文档的排序将基于博客文档的所有评论，而不是仅仅在10月份接收到的评论。 父子关系文档与 nested objects 相比，父-子关系的主要优势有： 更新父文档时，不会重新索引子文档。创建，修改或删除子文档时，不会影响父文档或其他子文档。这一点在这种场景下尤其有用：子文档数量较多，并且子文档创建和修改的频率高时。子文档可以作为搜索结果独立返回。Elasticsearch 维护了一个父文档和子文档的映射关系，得益于这个映射，父-子文档关联查询操作非常快。但是这个映射也对父-子文档关系有个限制条件：父文档和其所有子文档，都必须要存储在同一个分片中。 ==当文档索引性能远比查询性能重要 的时候，父子关系是非常有用的，但是它也是有巨大代价的。其查询速度会比同等的嵌套查询慢5到10倍!== 当你考虑父子关系是否适合你现有关系模型时，请考虑下面这些建议 ： 尽量少地使用父子关系，仅在子文档远多于父文档时使用。避免在一个查询中使用多个父子联合语句。在 has_child 查询中使用 filter 上下文，或者设置 score_mode 为 none 来避免计算文档得分。保证父 IDs 尽量短，以便在 doc values 中更好地压缩，被临时载入时占用更少的内存。最重要的是: 先考虑下我们之前讨论过的其他方式来达到父子关系的效果。 扩容设计副本分片与主分片做着相同的工作；它们只是扮演着略微不同的角色。没有必要确保主分片均匀地分布在所有节点中。PS:主分片数&gt;=节点数，大于是预留给节点数的扩充。 使用索引别名来指向当前版本的索引。 举例来说，给你的索引命名为 tweets_v1 而不是 tweets 。你的应用程序会与 tweets 进行交互，但事实上它是一个指向 tweets_v1 的别名。 这允许你将别名切换至一个更新版本的索引而保持服务运转。 一个搜索请求可以以多个索引为目标，所以将搜索别名指向 tweets_1 以及 tweets_2 是完全有效的。 然而，索引写入请求只能以单个索引为目标。因此，我们必须将索引写入的别名只指向新的索引。PUT /tweets_1/_alias/tweets_searchPUT /tweets_1/_alias/tweets_indexPOST /_aliases{ “actions”: [ { “add”: { “index”: “tweets_2”, “alias”: “tweets_search” }}, { “remove”: { “index”: “tweets_1”, “alias”: “tweets_index” }}, { “add”: { “index”: “tweets_2”, “alias”: “tweets_index” }} ]}一个文档 GET 请求，像一个索引写入请求那样，只能以单个索引为目标。 这导致在通过ID获取文档这样的场景下有一点复杂。作为代替，你可以对 tweets_1 以及 tweets_2 运行一个 ids 查询 搜索请求， 或者 multi-get 请求。 按时间范围索引 POST /_aliases{ “actions”: [ { “add”: { “alias”: “logs_current”, “index”: “logs_2014-10” }}, { “remove”: { “alias”: “logs_current”, “index”: “logs_2014-09” }}, { “add”: { “alias”: “last_3_months”, “index”: “logs_2014-10” }}, { “remove”: { “alias”: “last_3_months”, “index”: “logs_2014-07” }} ]} 索引模板PUT /_template/my_logs&#123; "template": "logstash-*", "order": 1, "settings": &#123; "number_of_shards": 1 &#125;, "mappings": &#123; "_default_": &#123; "_all": &#123; "enabled": false &#125; &#125; &#125;, "aliases": &#123; "last_3_months": &#123;&#125; &#125;&#125; 这个模板指定了所有名字以 logstash- 为起始的索引的默认设置，不论它是手动还是自动创建的。 如果我们认为明天的索引需要比今天更大的容量，我们可以更新这个索引以使用更多的分片。 这个模板还将新建索引添加至了 last_3_months 别名中，然而从那个别名中删除旧的索引则需要手动执行。 数据过期删除整个索引比删除单个文档要更加高效：Elasticsearch 只需要删除整个文件夹。 这些索引可以被关闭。它们还会存在于集群中，但它们不会消耗磁盘空间以外的资源。重新打开一个索引要比从备份中恢复快得多。在关闭之前，值得我们去刷写索引来确保没有事务残留在事务日志中。一个空白的事务日志会使得索引在重新打开时恢复得更快：POST /logs_2014-01-/_flushPOST /logs_2014-01-/_closePOST /logs_2014-01-*/_open 扩容并不是无限的需要记住的是相同的数据结构需要在每个节点的内存中保存，并且当它发生更改时必须发布到每一个节点。 集群状态的数据量越大，这个操作就会越久。 管理、监控和部署监控X-Pack 部署Transport Client 与 Node Client如果你使用的是 Java，你可能想知道何时使用传输客户端（注：Transport Client，下同）与节点客户端（注：Node Client，下同）。 在书的开头所述， 传输客户端作为一个集群和应用程序之间的通信层。它知道 API 并能自动帮你在节点之间轮询，帮你嗅探集群等等。但它是集群 外部的 ，和 REST 客户端类似。 另一方面，节点客户端，实际上是一个集群中的节点（但不保存数据，不能成为主节点）。因为它是一个节点，它知道整个集群状态（所有节点驻留，分片分布在哪些节点，等等）。 这意味着它可以执行 APIs 但少了一个网络跃点。 这里有两个客户端案例的使用情况： 如果要将应用程序和 Elasticsearch 集群进行解耦，传输客户端是一个理想的选择。例如，如果您的应用程序需要快速的创建和销毁到集群的连接，传输客户端比节点客户端”轻”，因为它不是一个集群的一部分。 类似地，如果您需要创建成千上万的连接，你不想有成千上万节点加入集群。传输客户端（ TC ）将是一个更好的选择。 另一方面，如果你只需要有少数的、长期持久的对象连接到集群，客户端节点可以更高效，因为它知道集群的布局。但是它会使你的应用程序和集群耦合在一起，所以从防火墙的角度，它可能会构成问题。 参考文献Elasticsearch: 权威指南]]></content>
      <tags>
        <tag>book</tag>
        <tag>ElasticSearch</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark API 全集(2):Spark SQL 函数全集]]></title>
    <url>%2F2018%2F03%2F23%2Fspark-sql-functions-api%2F</url>
    <content type="text"><![CDATA[Summaryorg.apache.spark.sql.functions是一个Object，提供了约两百多个函数。 大部分函数与Hive的差不多。 除UDF函数，均可在spark-sql中直接使用。 经过import org.apache.spark.sql.functions._ ，也可以用于Dataframe，Dataset。 version2.3.0 大部分支持Column的函数也支持String类型的列名。这些函数的返回类型基本都是Column。 函数很多，都在下面了。 聚合函数approx_count_distinctcount_distinct近似值avg平均值collect_list聚合指定字段的值到listcollect_set聚合指定字段的值到setcorr计算两列的Pearson相关系数count计数countDistinct去重计数 SQL中用法select count(distinct class)covar_pop总体协方差（population covariance）covar_samp样本协方差（sample covariance）first分组第一个元素last分组最后一个元素groupinggrouping_idkurtosis 计算峰态(kurtosis)值skewness 计算偏度(skewness)max最大值min最小值mean平均值stddev 即stddev_sampstddev_samp 样本标准偏差（sample standard deviation）stddev_pop总体标准偏差（population standard deviation）sum求和sumDistinct非重复值求和 SQL中用法select sum(distinct class)var_pop总体方差（population variance）var_samp样本无偏方差（unbiased variance）variance即var_samp 集合函数array_contains(column,value)检查array类型字段是否包含指定元素explode 展开array或map为多行explode_outer同explode，但当array或map为空或null时，会展开为null。posexplode同explode，带位置索引。posexplode_outer同explode_outer，带位置索引。from_json解析JSON字符串为StructType or ArrayType，有多种参数形式，详见文档。to_json转为json字符串，支持StructType, ArrayType of StructTypes, a MapType or ArrayType of MapTypes。get_json_object(column,path)获取指定json路径的json对象字符串。select get_json_object('&#123;"a"1,"b":2&#125;','$.a');[JSON Path介绍](http://blog.csdn.net/koflance/article/details/63262484)json_tuple(column,fields)获取json中指定字段值。select json_tuple('&#123;"a":1,"b":2&#125;','a','b');map_keys返回map的键组成的arraymap_values返回map的值组成的arraysizearray or map的长度sort_array(e: Column, asc: Boolean)将array中元素排序（自然排序），默认asc。 时间函数add_months(startDate: Column, numMonths: Int)指定日期添加n月date_add(start: Column, days: Int)指定日期之后n天 e.g. select date_add('2018-01-01',3)date_sub(start: Column, days: Int)指定日期之前n天datediff(end: Column, start: Column)两日期间隔天数current_date()当前日期current_timestamp()当前时间戳，TimestampType类型date_format(dateExpr: Column, format: String)日期格式化dayofmonth(e: Column)日期在一月中的天数，支持 date/timestamp/stringdayofyear(e: Column)日期在一年中的天数， 支持 date/timestamp/stringweekofyear(e: Column)日期在一年中的周数， 支持 date/timestamp/stringfrom_unixtime(ut: Column, f: String)时间戳转字符串格式from_utc_timestamp(ts: Column, tz: String)时间戳转指定时区时间戳to_utc_timestamp(ts: Column, tz: String)指定时区时间戳转UTF时间戳hour(e: Column)提取小时值minute(e: Column)提取分钟值month(e: Column)提取月份值quarter(e: Column)提取季度second(e: Column)提取秒year(e: Column):提取年last_day(e: Column)指定日期的月末日期months_between(date1: Column, date2: Column)计算两日期差几个月next_day(date: Column, dayOfWeek: String)计算指定日期之后的下一个周一、二...，dayOfWeek区分大小写，只接受 "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"。to_date(e: Column)字段类型转为DateTypetrunc(date: Column, format: String)日期截断unix_timestamp(s: Column, p: String)指定格式的时间字符串转时间戳unix_timestamp(s: Column)同上，默认格式为 yyyy-MM-dd HH:mm:ssunix_timestamp():当前时间戳(秒),底层实现为unix_timestamp(current_timestamp(), yyyy-MM-dd HH:mm:ss)window(timeColumn: Column, windowDuration: String, slideDuration: String, startTime: String)时间窗口函数，将指定时间(TimestampType)划分到窗口 数学函数cos,sin,tan计算角度的余弦，正弦。。。sinh,tanh,cosh计算双曲正弦，正切，。。acos,asin,atan,atan2计算余弦/正弦值对应的角度bin将long类型转为对应二进制数值的字符串For example, bin("12") returns "1100".bround舍入，使用Decimal的HALF_EVEN模式，v&gt;0.5向上舍入，v&lt; 0.5向下舍入，v0.5向最近的偶数舍入。round(e: Column, scale: Int)HALF_UP模式舍入到scale为小数点。v&gt;=0.5向上舍入，v&lt; 0.5向下舍入,即四舍五入。ceil向上舍入floor向下舍入cbrtComputes the cube-root of the given value.conv(num:Column, fromBase: Int, toBase: Int) 转换数值（字符串）的进制log(base: Double, a: Column):$log_&#123;base&#125;(a)$log(a: Column):$log_e(a)$log10(a: Column):$log_&#123;10&#125;(a)$log2(a: Column):$log_&#123;2&#125;(a)$log1p(a: Column):$log_&#123;e&#125;(a+1)$pmod(dividend: Column, divisor: Column):Returns the positive value of dividend mod divisor.pow(l: Double, r: Column):$r^l$ 注意r是列pow(l: Column, r: Double):$r^l$ 注意l是列pow(l: Column, r: Column):$r^l$ 注意r,l都是列radians(e: Column):角度转弧度rint(e: Column):Returns the double value that is closest in value to the argument and is equal to a mathematical integer.shiftLeft(e: Column, numBits: Int):向左位移shiftRight(e: Column, numBits: Int):向右位移shiftRightUnsigned(e: Column, numBits: Int):向右位移（无符号位）signum(e: Column):返回数值正负符号sqrt(e: Column):平方根hex(column: Column):转十六进制unhex(column: Column):逆转十六进制 混杂(misc)函数crc32(e: Column):计算CRC32,返回biginthash(cols: Column*):计算 hash code，返回intmd5(e: Column):计算MD5摘要，返回32位，16进制字符串sha1(e: Column):计算SHA-1摘要，返回40位，16进制字符串sha2(e: Column, numBits: Int):计算SHA-1摘要，返回numBits位，16进制字符串。numBits支持224, 256, 384, or 512. 其他非聚合函数abs(e: Column)绝对值array(cols: Column*)多列合并为array，cols必须为同类型map(cols: Column*):将多列组织为map，输入列必须为（key,value)形式，各列的key/value分别为同一类型。bitwiseNOT(e: Column):Computes bitwise NOT.broadcast[T](df: Dataset[T]): Dataset[T]:将df变量广播，用于实现broadcast join。如left.join(broadcast(right), "joinKey")coalesce(e: Column*):返回第一个非空值col(colName: String):返回colName对应的Columncolumn(colName: String):col函数的别名expr(expr: String):解析expr表达式，将返回值存于Column，并返回这个Column。greatest(exprs: Column*):返回多列中的最大值，跳过Nullleast(exprs: Column*):返回多列中的最小值，跳过Nullinput_file_name():返回当前任务的文件名 ？？isnan(e: Column):检查是否NaN（非数值）isnull(e: Column):检查是否为Nulllit(literal: Any):将字面量(literal)创建一个ColumntypedLit[T](literal: T)(implicit arg0: scala.reflect.api.JavaUniverse.TypeTag[T]):将字面量(literal)创建一个Column，literal支持 scala types e.g.: List, Seq and Map.monotonically_increasing_id():返回单调递增唯一ID，但不同分区的ID不连续。ID为64位整型。nanvl(col1: Column, col2: Column):col1为NaN则返回col2negate(e: Column):负数，同df.select( -df("amount") )not(e: Column):取反，同df.filter( !df("isActive") )rand():随机数[0.0, 1.0]rand(seed: Long):随机数[0.0, 1.0]，使用seed种子randn():随机数，从正态分布取randn(seed: Long):同上spark_partition_id():返回partition IDstruct(cols: Column*):多列组合成新的struct column ？？when(condition: Column, value: Any):当condition为true返回value，如people.select(when(people("gender") === "male", 0) .when(people("gender") === "female", 1) .otherwise(2))如果没有otherwise且condition全部没命中，则返回null. 排序函数asc(columnName: String):正序asc_nulls_first(columnName: String):正序，null排最前asc_nulls_last(columnName: String):正序，null排最后e.g.df.sort(asc("dept"), desc("age"))对应有desc函数 desc,desc_nulls_first,desc_nulls_last 字符串函数ascii(e: Column): 计算第一个字符的ascii码base64(e: Column): base64转码unbase64(e: Column): base64解码concat(exprs: Column*):连接多列字符串concat_ws(sep: String, exprs: Column*):使用sep作为分隔符连接多列字符串decode(value: Column, charset: String): 解码encode(value: Column, charset: String): 转码，charset支持 'US-ASCII', 'ISO-8859-1', 'UTF-8', 'UTF-16BE', 'UTF-16LE', 'UTF-16'。format_number(x: Column, d: Int):格式化'#,###,###.##'形式的字符串format_string(format: String, arguments: Column*): 将arguments按format格式化，格式为printf-style。initcap(e: Column): 单词首字母大写lower(e: Column): 转小写upper(e: Column): 转大写instr(str: Column, substring: String): substring在str中第一次出现的位置length(e: Column): 字符串长度levenshtein(l: Column, r: Column): 计算两个字符串之间的编辑距离（Levenshtein distance）locate(substr: String, str: Column): substring在str中第一次出现的位置，位置编号从1开始，0表示未找到。locate(substr: String, str: Column, pos: Int): 同上，但从pos位置后查找。lpad(str: Column, len: Int, pad: String):字符串左填充。用pad字符填充str的字符串至len长度。有对应的rpad，右填充。ltrim(e: Column):剪掉左边的空格、空白字符，对应有rtrim.ltrim(e: Column, trimString: String):剪掉左边的指定字符,对应有rtrim.trim(e: Column, trimString: String):剪掉左右两边的指定字符trim(e: Column):剪掉左右两边的空格、空白字符regexp_extract(e: Column, exp: String, groupIdx: Int): 正则提取匹配的组regexp_replace(e: Column, pattern: Column, replacement: Column): 正则替换匹配的部分，这里参数为列。regexp_replace(e: Column, pattern: String, replacement: String): 正则替换匹配的部分repeat(str: Column, n: Int):将str重复n次返回reverse(str: Column): 将str反转soundex(e: Column): 计算桑迪克斯代码（soundex code）PS:用于按英语发音来索引姓名,发音相同但拼写不同的单词，会映射成同一个码。split(str: Column, pattern: String): 用pattern分割strsubstring(str: Column, pos: Int, len: Int): 在str上截取从pos位置开始长度为len的子字符串。substring_index(str: Column, delim: String, count: Int):Returns the substring from string str before count occurrences of the delimiter delim. If count is positive, everything the left of the final delimiter (counting from left) is returned. If count is negative, every to the right of the final delimiter (counting from the right) is returned. substring_index performs a case-sensitive match when searching for delim.translate(src: Column, matchingString: String, replaceString: String):把src中的matchingString全换成replaceString。 UDF函数user-defined function.callUDF(udfName: String, cols: Column*): 调用UDFimport org.apache.spark.sql._val df = Seq(("id1", 1), ("id2", 4), ("id3", 5)).toDF("id", "value")val spark = df.sparkSessionspark.udf.register("simpleUDF", (v: Int) =&gt; v * v)df.select($"id", callUDF("simpleUDF", $"value"))udf: 定义UDF 窗口函数cume_dist(): cumulative distribution of values within a window partitioncurrentRow(): returns the special frame boundary that represents the current row in the window partition.rank():排名，返回数据项在分组中的排名，排名相等会在名次中留下空位 1,2,2,4。dense_rank(): 排名，返回数据项在分组中的排名，排名相等会在名次中不会留下空位 1,2,2,3。row_number():行号，为每条记录返回一个数字 1,2,3,4percent_rank():returns the relative rank (i.e. percentile) of rows within a window partition.lag(e: Column, offset: Int, defaultValue: Any): offset rows before the current rowlead(e: Column, offset: Int, defaultValue: Any): returns the value that is offset rows after the current rowntile(n: Int): returns the ntile group id (from 1 to n inclusive) in an ordered window partition.unboundedFollowing():returns the special frame boundary that represents the last row in the window partition. 参考 Spark API]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>API</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark API 全集(1):Spark SQL Dataset & DataFrame API]]></title>
    <url>%2F2018%2F03%2F22%2Fspark-sql-dataset-api%2F</url>
    <content type="text"><![CDATA[简介org.apache.spark.sql.Dataset是Spark SQL中核心的类，定义如下：class Dataset[T] extends Serializable DataFrame是Dataset[Row]的别名。 本文基于spark2.3.0. 下面是类方法简介。 类方法Actionscollect(): Array[T]返回一个数组，包含Dataset所有行的数据。注意：所有数据会被加载进driver进程的内存。collectAsList(): List[T]同上，但是返回Java list。count(): Long数据行数describe(cols: String*): DataFrame计算指定列的统计指标，包括count, mean, stddev, min, and max.head(): T返回第一行head(n: Int): Array[T]返回前N行first(): T返回第一行，是head()的别名。foreach(f: (T) ⇒ Unit): Unit所有元素上应用f函数foreachPartition(f: (Iterator[T]) ⇒ Unit): Unit所有元素分区上应用f函数reduce(func: (T, T) ⇒ T): T根据映射函数func，对RDD中的元素进行二元计算，返回计算结果。注意：提供的函数应满足交换律及结合律，否则计算结果将是非确定的。show(numRows: Int, truncate: Int, vertical: Boolean): Unit表格形式打印出数据。numRows：显示的行数，truncate：裁剪字符串类型值到指定长度，vertical：垂直打印。show(numRows: Int, truncate: Int): Unitshow(numRows: Int, truncate: Boolean): Unitshow(truncate: Boolean): UnitnumRows=20 truncate=20show(numRows: Int): Unittruncate=20show(): UnitnumRows=20 truncate=20summary(statistics: String*): DataFrame计算数据集statistics指定的指标，可指定 count, mean, stddev, min, approximate quartiles (percentiles at 25%, 50%, and 75%), and max.如未指定则会计算全部。take(n: Int): Array[T]获取前n行takeAsList(n: Int): List[T]获取前n行保存为listtoLocalIterator(): Iterator[T]返回一个所有行的迭代器The iterator will consume as much memory as the largest partition in this Dataset. 基本函数（Basic Dataset functions)as[U](implicit arg0: Encoder[U]): Dataset[U]将数据映射成指定类型U，返回新的Datasetpersist(newLevel: StorageLevel): Dataset.this.type缓存数据，可设置缓存级别。persist(): Dataset.this.type同cache方法cache(): Dataset.this.type缓存数据,MEMORY_AND_DISK模式。注意：RDD的cache函数默认是MEMORY_ONLY。checkpoint(eager: Boolean): Dataset[T]返回一个checkpointed的Dataset，Dataset的逻辑执行计划将被截断。checkpoint(): Dataset[T]同上，eager=true.columns: Array[String]数组形式返回所有列名。dtypes: Array[(String, String)]数组形式返回所有列名及类型。createGlobalTempView(viewName: String): Unit创建全局临时视图(view)，生命周期与Spark应用一致。可以跨session访问。e.g. SELECT * FROM global_temp.view1.createOrReplaceGlobalTempView(viewName: String): Unit同上，已存在则替换。createTempView(viewName: String): Unit创建本地临时视图(view)，仅当前SparkSession可访问。注意：不跟任何库绑定，不能用db1.view1这样的形式访问。createOrReplaceTempView(viewName: String): Unit同上，已存在则替换。explain(): Unit打印物理执行计划另有：queryExecution变量，完整执行计划。explain(extended: Boolean): Unit打印物理+逻辑执行计划hint(name: String, parameters: Any*): Dataset[T]当前dataset指定hint。//todoe.g. df1.join(df2.hint("broadcast"))inputFiles: Array[String]返回组成Dataset的输入文件（Returns a best-effort snapshot of the files that compose this Dataset）isLocal: Booleancollect和take是否可以本地执行，不需要executor.localCheckpoint(eager: Boolean): Dataset[T]执行本地Checkpoint，返回新dataset。localCheckpoint(): Dataset[T]eager=trueprintSchema(): Unit打印schema结构rdd: RDD[T]dataset内部的RDDschema: StructTypeschemastorageLevel: StorageLevel当前存储等级，没有被persist则是StorageLevel.NONEtoDF(): DataFrametoDF(colNames: String*): DataFrame转为DataFrame，也可以将RDD转为DataFrame。unpersist(): Dataset.this.typeunpersist(blocking: Boolean): Dataset.this.type删除缓存，blocking表示是否等所有blocks删除后才返回,删除期间阻塞。write: DataFrameWriter[T]DataFrameWriter，非流式数据写接口。writeStream: DataStreamWriter[T]DataStreamWriter，流式数据写接口。 流式函数（streaming）isStreaming: Boolean是否流式数据withWatermark(eventTime: String, delayThreshold: String): Dataset[T]Defines an event time watermark for this Dataset.//TODO 强类型转换（Typed transformations）alias(alias: Symbol): Dataset[T]alias(alias: String): Dataset[T]as(alias: Symbol): Dataset[T]as(alias: String): Dataset[T]给Dataset一个别名coalesce(numPartitions: Int): Dataset[T]分区合并(只能减少分区)distinct(): Dataset[T]dropDuplicates的别名dropDuplicates(col1: String, cols: String*): Dataset[T]dropDuplicates(colNames: Array[String]): Dataset[T]dropDuplicates(colNames: Seq[String]): Dataset[T]dropDuplicates(): Dataset[T]根据指定字段，对数据去重。except(other: Dataset[T]): Dataset[T]去除other中也有的行。同EXCEPT DISTINCT in SQL。//TODOfilter(func: (T) ⇒ Boolean): Dataset[T]filter(conditionExpr: String): Dataset[T]filter(condition: Column): Dataset[T]根据条件过滤行e.g.peopleDs.filter("age &gt; 15")peopleDs.filter($"age" &gt; 15)flatMap[U](func: (T) ⇒ TraversableOnce[U])(implicit arg0: Encoder[U]): Dataset[U]第一步和map一样，最后将所有的输出合并。groupByKey[K](func: (T) ⇒ K)(implicit arg0: Encoder[K]): KeyValueGroupedDataset[K, T]现根据func函数生成key，然后按key分组。intersect(other: Dataset[T]): Dataset[T]求两个dataset的交集，等同于INTERSECT in SQL.joinWith[U](other: Dataset[U], condition: Column): Dataset[(T, U)]inner equi-join两个datasetjoinWith[U](other: Dataset[U], condition: Column, joinType: String): Dataset[(T, U)]joinType可选：inner, cross, outer, full, full_outer, left, left_outer, right, right_outerlimit(n: Int): Dataset[T]返回前n行，与head的区别是，head是一个action，会马上返回结果数组。map[U](func: (T) ⇒ U)(implicit arg0: Encoder[U]): Dataset[U]在每一个元素应用func函数，返回包含结果集的dataset。mapPartitions[U](func: (Iterator[T]) ⇒ Iterator[U])(implicit arg0: Encoder[U]): Dataset[U]在每一个分区应用func函数，返回包含结果集的dataset。orderBy(sortExprs: Column*): Dataset[T]orderBy(sortCol: String, sortCols: String*): Dataset[T]sort的别名sort(sortExprs: Column*): Dataset[T]sort(sortCol: String, sortCols: String*): Dataset[T]按指定列排序，默认asc。e.g. ds.sort($"col1", $"col2".desc)sortWithinPartitions(sortExprs: Column*): Dataset[T]sortWithinPartitions(sortCol: String, sortCols: String*): Dataset[T]分区内排序，同"SORT BY" in SQL (Hive QL).randomSplit(weights: Array[Double]): Array[Dataset[T]]randomSplit(weights: Array[Double], seed: Long): Array[Dataset[T]]按权重随机分割数据repartition(partitionExprs: Column*): Dataset[T]repartition(numPartitions: Int, partitionExprs: Column*): Dataset[T]repartition(numPartitions: Int): Dataset[T]按指定表达式，分区数，重新分区（hash），同"DISTRIBUTE BY" in SQL。默认分区数为spark.sql.shuffle.partitionsrepartitionByRange(partitionExprs: Column*): Dataset[T]repartitionByRange(numPartitions: Int, partitionExprs: Column*): Dataset[T]按指定表达式，分区数，重新分区，采用Range partition方式，按键范围分区。分区默认排序方式为ascending nulls first，分区内数据未排序。sample(withReplacement: Boolean, fraction: Double): Dataset[T]sample(withReplacement: Boolean, fraction: Double, seed: Long): Dataset[T]sample(fraction: Double): Dataset[T]sample(fraction: Double, seed: Long): Dataset[T]随机取样本数据withReplacement：Sample with replacement or not.fraction：Fraction of rows to generate, range [0.0, 1.0].seed：Seed for sampling.select[U1](c1: TypedColumn[T, U1]): Dataset[U1]根据列/表达式获取列数据transform[U](t: (Dataset[T]) ⇒ Dataset[U]): Dataset[U]应用t函数转换Dataset。union(other: Dataset[T]): Dataset[T]等于UNION ALL in SQL。注意是按列位置合并：val df1 = Seq((1, 2, 3)).toDF("col0", "col1", "col2")val df2 = Seq((4, 5, 6)).toDF("col1", "col2", "col0")df1.union(df2).show// output:// +----+----+----+// |col0|col1|col2|// +----+----+----+// | 1| 2| 3|// | 4| 5| 6|// +----+----+----+unionByName(other: Dataset[T]): Dataset[T]同union方法，但是按列名合并：val df1 = Seq((1, 2, 3)).toDF("col0", "col1", "col2")val df2 = Seq((4, 5, 6)).toDF("col1", "col2", "col0")df1.unionByName(df2).show// output:// +----+----+----+// |col0|col1|col2|// +----+----+----+// | 1| 2| 3|// | 6| 4| 5|// +----+----+----+where(conditionExpr: String): Dataset[T]where(condition: Column): Dataset[T]filter的别名 弱类型转换（Untyped transformations）返回类型为DataFrame而不是Dataset。agg(expr: Column, exprs: Column*): DataFrameagg(exprs: Map[String, String]): DataFrameagg(aggExpr: (String, String), aggExprs: (String, String)*): DataFrame在整个dataset进行聚合。ds.agg(...) 是 ds.groupBy().agg(...) 的简写。e.g.ds.agg(max($"age"), avg($"salary"))ds.agg(Map("age" -&gt; "max", "salary" -&gt; "avg"))ds.agg("age" -&gt; "max", "salary" -&gt; "avg")apply(colName: String): Columncol(colName: String): ColumncolRegex(colName: String): Column返回指定列。crossJoin(right: Dataset[_]): DataFramecross join。cube(col1: String, cols: String*): RelationalGroupedDatasetcube(cols: Column*): RelationalGroupedDataset使用指定列创建多维cube。//TODOdrop(col: Column): DataFramedrop(colNames: String*): DataFramedrop(colName: String): DataFrame剪掉指定字段。groupBy(col1: String, cols: String*): RelationalGroupedDatasetgroupBy(cols: Column*): RelationalGroupedDataset按指定列分组join(right: Dataset[_], joinExprs: Column, joinType: String): DataFramejoin(right: Dataset[_], joinExprs: Column): DataFramejoin(right: Dataset[_], usingColumns: Seq[String], joinType: String): DataFramejoin(right: Dataset[_], usingColumns: Seq[String]): DataFramejoin(right: Dataset[_], usingColumn: String): DataFramejoin(right: Dataset[_]): DataFrame与另一个DataFrame join。joinExprs：$"df1Key" === $"df2Key"usingColumn：Seq("user_id", "user_name")joinType：Default inner. Must be one of: inner, cross, outer, full, full_outer, left, left_outer, right, right_outer, left_semi, left_anti.na: DataFrameNaFunctions见DataFrameNaFunctionsstat: DataFrameStatFunctions见DataFrameStatFunctionsrollup(col1: String, cols: String*): RelationalGroupedDatasetrollup(cols: Column*): RelationalGroupedDataset使用指定列进行rollup聚合。//TODOselect(col: String, cols: String*): DataFrameselect(cols: Column*): DataFrameselectExpr(exprs: String*): DataFrame选取指定列、SQL表达式。withColumn(colName: String, col: Column): DataFrame新增或替换一列。withColumnRenamed(existingName: String, newName: String): DataFrame将指定列更名。 未分组（Ungrouped）queryExecution: QueryExecution执行计划sparkSession: SparkSession创建该dataset的SparkSessionsqlContext: SQLContextdataset的SQLContexttoJSON: Dataset[String]每行数据转成JSON字符串。toString(): StringAny的toString 参考 Spark API]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>API</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Linux配置SSH免密登陆(公私钥登陆)]]></title>
    <url>%2F2018%2F03%2F05%2Fssh-without-password%2F</url>
    <content type="text"><![CDATA[环境说明客户机:Mac OS X 服务器:CentOS 6.5 客户端:OpenSSH,OS X及大多数Linux都内置了OpenSSH.’ssh -v’命令可以查看版本. 大致流程 1.在客户机创建一对密钥文件,包括公钥文件(~/.ssh/id_rsa.pub),私钥文件(~/.ssh/id_rsa). 2.把公钥放到服务器上（~/.ssh/authorized_keys）,在使用ssh登录时，ssh程序会发送私钥去和服务器上的公钥做匹配。如果匹配成功就可以自动登录了。 客户机配置 1.查看~/.ssh文件夹,若已经存在有公钥文件(id_rsa.pub),私钥文件(id_rsa),则可以跳过客户端配置. 2.生成密钥文件. $ ssh-keygen 然后一路回车.然后~/.ssh下会生成id_rsa.pub和id_rsa, 其中id_rsa文件起到唯一标识你的客户机的作用. 注意:不要改这两个文件的文件名,ssh登陆时会读取id_rsa文件. 服务器配置1.修改sshd配置文件(/etc/ssh/sshd_config).找到以下内容，并去掉注释符”#“ RSAAuthentication yes PubkeyAuthentication yes AuthorizedKeysFile .ssh/authorized_keys 2.配置authorized_keys文件.若’~/.ssh/authorized_keys’不存在,则建立.ssh文件夹和authorized_keys文件. 将上文中客户机id_rsa.pub的内容拷贝到authorized_keys中. PS:可以在客户机中执行命令来拷贝:cat ~/.ssh/id_rsa.pub | ssh user@host “cat - &gt;&gt; ~/.ssh/authorized_keys” 注意: 1 .ssh目录的权限必须是700 2 .ssh/authorized_keys文件权限必须是600 3.重启sshd.$ /etc/init.d/sshd restart 测试客户机执行:ssh -v user@host (-v 调试模式) 会显示一些登陆信息.若登陆失败,或者仍然要输入密码,可以在服务器查看日志文件:/var/log/secure. 若登陆成功,则以后就可以用ssh user@host 直接登陆了,不用输入密码.]]></content>
      <tags>
        <tag>SSH</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[自定义开发Spark ML机器学习类 - 1]]></title>
    <url>%2F2018%2F02%2F04%2Fdiy-spark-ml-1%2F</url>
    <content type="text"><![CDATA[初窥门径Spark的MLlib组件内置实现了很多常见的机器学习算法,包括数据抽取,分类,聚类,关联分析,协同过滤等等.然鹅,内置的算法并不能满足我们所有的需求,所以我们还是经常需要自定义ML算法. MLlib提供的API分为两类: 1.基于DataFrame的API,属于spark.ml包. 2.基于RDD的API, 属于spark.mllib包. 从Spark 2.0开始,Spark的API全面从RDD转向DataFrame,MLlib也是如此,官网原话如下: Announcement: DataFrame-based API is primary API The MLlib RDD-based API is now in maintenance mode. 所以本文将介绍基于DataFrame的自定义ml类编写方法.不涉及具体算法,只讲扩展ml类的方法. 略知一二官方文档并没有介绍如何自定义ml类,所以只有从源码入手,看看源码里面是怎么实现的. 找一个最简单的内置算法入手,这个算法就是内置的分词器,Tokenizer. Tokenizer只是简单的将文本以空白部分进行分割,只适合给英文进行分词,所以它的实现及其简短,源码如下: package org.apache.spark.ml.featureimport org.apache.spark.annotation.Sinceimport org.apache.spark.ml.UnaryTransformerimport org.apache.spark.ml.param._import org.apache.spark.ml.util._import org.apache.spark.sql.types.&#123;ArrayType, DataType, StringType&#125;/** * A tokenizer that converts the input string to lowercase and then splits it by white spaces. * * @see [[RegexTokenizer]] */@Since("1.2.0")class Tokenizer @Since("1.4.0") (@Since("1.4.0") override val uid: String) extends UnaryTransformer[String, Seq[String], Tokenizer] with DefaultParamsWritable &#123; @Since("1.2.0") def this() = this(Identifiable.randomUID("tok")) override protected def createTransformFunc: String =&gt; Seq[String] = &#123; _.toLowerCase.split("\\s") &#125; override protected def validateInputType(inputType: DataType): Unit = &#123; require(inputType == StringType, s"Input type must be string type but got $inputType.") &#125; override protected def outputDataType: DataType = new ArrayType(StringType, true) @Since("1.4.1") override def copy(extra: ParamMap): Tokenizer = defaultCopy(extra)&#125;@Since("1.6.0")object Tokenizer extends DefaultParamsReadable[Tokenizer] &#123; @Since("1.6.0") override def load(path: String): Tokenizer = super.load(path)&#125; 简单分析下源码: Tokenizer继承了UnaryTransformer类.unary是’一元’的意思,也是说这个类实现的是类似一元函数的功能,一个输入变量,一个输出.直接看UnaryTransformer的源码注释: /*** :: DeveloperApi ::* Abstract class for transformers that take one input column, apply transformation, and output the* result as a new column.*/ DeveloperApi表明这是一个开发级API,开发者可以用,不会有权限问题(源码中有很多private[spark]的类,是不允许外部调用的).注释的大意就是:这是一个为实现transformers准备的抽象类,以一个字段(列)为输入,输出一个新字段(列). 所以实际上就是实现一个Transformer,只是这个Transformer有指定的输入字段和输出字段. UnaryTransformer类中只有两个抽象方法.一个是createTransformFunc,是最核心的方法,这个方法需要返回一个函数,这个函数的参数即Transformer的输入字段的值,返回值为Transformer的输出字段的值.看看Tokenizer中的实现,就明白了. 另一个是outputDataType,这个方法用来返回输出字段的类型. validateInputType方法是用来检查输入字段类型的,看需要实现. Tokenizer混入了DefaultParamsWritable特质,使得自己可以被保存. 对应的object Tokenizer伴生对象,用来读取已保存的Tokenizer. 值得注意的是,Transformer类是PipelineStage类的子类,所以Transformer的子类,包括我们自定义的,是可以直接用在ML Pipelines中的.这就厉害了,说明自定义的算法类,可以无缝与内置机器学习算法打配合,还能利用Pipeline的调优工具(model selection,Cross-Validation等). 初出茅庐看完源码,基本套路已经明了,不如动手抄一个,不,敲一个.依葫芦画瓢,实现一个正则提取的Transformer. import util.matching.Regeximport org.apache.spark.ml.UnaryTransformerimport org.apache.spark.ml.param.Paramimport org.apache.spark.ml.util.Identifiableimport org.apache.spark.sql.types._/** * 正则提取器 * 将匹配指定正则表达式的全部子字符串，提取到array[string]中. */class RegexExtractor(override val uid: String) extends UnaryTransformer[String, Seq[String], RegexExtractor] &#123; def this() = this(Identifiable.randomUID("RegexExtractor")) /** * 参数:正则表达式 * * @group param */ final val regex = new Param[Regex](this, "RegexExpr", "正则表达式") /** @group setParam */ def setRegexExpr(value: String): this.type = set(regex, new Regex(value)) override protected def outputDataType: DataType = new ArrayType(StringType, true) override protected def validateInputType(inputType: DataType): Unit = &#123; require(inputType == DataTypes.StringType, s"Input type must be string type but got $inputType." ) &#125; override protected def createTransformFunc: String =&gt; Seq[String] = &#123; parseContent &#125; /** * 数据处理 */ private def parseContent(text: String): Seq[String] = &#123; if (text == null || text.isEmpty) &#123; return Seq.empty[String] &#125; $(regex).findAllIn(text).toSeq &#125;&#125; 这个类结构与Tokenizer源码基本差不多,多用到的Param类,是一个参数的包装类.作用是self-contained documentation and optionally default value.其实就是把参数的值,文档,默认值等属性组合成一个类,方便调用. 比如上面定义的regex参数,就可以用$(regex)这样的方式直接调用. 另外在org.apache.spark.ml.param中有很多内置的Param类,可以直接使用. 同时org.apache.spark.ml.param.shared中有很多辅助引入参数的特质,比如HasInputCols特质,你的自定义Transformer只要混入这个特质就拥有了inputCols参数.不过目前shared中特质的作用域是private[ml],也就是说不能直接引用,而是要copy一份代码到自己的项目,并修改作用域才行.关于这个作用域的问题,有人在spark的jira上提到,提议将其作为DeveloperApi开放出来,我也投了一票表示支持.后来在2017年11月终于resolved,该问题将在Spark2.3.0中解决.详情戳我 粗懂皮毛自定义的类写好了,该怎么用呢? 当然是跟内置的一样啦.上栗子:val regex="nidezhengze"val tranTitle = new RegexExtractor() .setInputCol("title") .setOutputCol("title_price_texts") .setRegexExpr(regex)val pipeline = new Pipeline().setStages(Array( tranTitle))val matched = pipeline.fit(data).transform(data) 打完收功到这里,开发简单Transform的套路已经清楚了,不过这里实现的功能比较类似于一个UDF,只能对dataset的一个字段进行处理,而且是逐行处理,并不能根据多行数据进行处理,实现窗口函数类似的功能,而且也没有涉及模型的输出.如果要开发更复杂的算法,甚至进行模型训练,就需要更深入的了解MLlib了,阅读源码是个好途径. 有机会再说.👋]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>ML</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[一条通往服务器所有端口的隧道]]></title>
    <url>%2F2017%2F06%2F13%2Fssh-tunnel-with-windows%2F</url>
    <content type="text"><![CDATA[应用场景通常为了安全,服务器需要通过跳板机访问，服务器对外网暴露的端口也严格限制。这种情况下若要在本地访问服务器上的服务或系统就会比较蛋疼。有一个简单的解决方案，就是在本地和跳板机之间建立SSH隧道。SSH隧道提供了一个网络代理服务，通过该代理服务可以直接访问跳板机所在的局域网，即服务器上的任意端口，服务都可以直接访问。 本文介绍的方法比一般的端口映射更方便，不需要为每个端口配置一条SSH隧道，包括Windows、Mac、Linux上的操作方法。 SSH隧道建立Mac &amp; Linux 版非常简单，只需一条命令即可建立SSH隧道。ssh user@host -ND 127.0.0.1:1080 其实就是在常规的SSH命令加上-D参数，开启动态端口转发，使SSH成为了SOCKS server，在后台提供网络服务。 而-N参数是让ssh不要返回命令行终端，因为我们不需要发送命令，只是做转发。 1080是绑定的本地端口，也就是SOCKS server提供服务的端口，可以换成其他端口号。 127.0.0.1表示只能有你本机访问这个服务，去掉IP只留下端口号的话，就没有这个限制了。 PS:SSH隧道相当于在服务器的防火墙上打了个洞，可能有安全隐患，所以建议加上仅限本机访问的限制。 Windows版这里使用Windows上常用的XShell做说明。 1.配置常规的SSH连接配置用户名，密码，主机地址(通常是跳板机)等。 2.添加隧道 3.其他 SSH隧道使用让浏览器访问内网服务这里介绍Chrome浏览器+SwitchyOmega插件的方法。 SwitchyOmega是一个Chrome插件，下载安装地址：Chrome应用商店或者 官网下载 安装好后，找到插件图标（一个圆圈），单击图标-&gt;选项，打开配置页面。点击侧边栏的“新建情景模式（New Profile）”，添加一个代理服务器（Proxy Profile），配置如下图。注意端口要填写前面SSH隧道服务的端口号。 如果侧边栏中已经有一个默认的’proxy‘模式，也可以直接拿来修改。 配置好后单击SwitchyOmega的图标，切换到新建的情景模式，然后所有的浏览器请求都会以SSH隧道作为代理了，这时你应该已经可以访问服务器上的任何web服务了。 但是！我们并不需要所有的浏览器流量都走代理哇。SwitchyOmega其实可以根据URL的规则，自动选择走不走代理。默认已经存在的一个情景模式’auto switch‘，就是一个可以根据规则来自动选择代理服务的模式。比如在’auto switch‘模式中配置一条规则，填写10.1.*，并选择proxy模式。这就代表将10.1.开头的所有请求都用proxy模式转发，而其他不满足规则的请求，将命中最后一条’默认‘规则，进行’直接访问‘，即不使用任何代理服务。 与Proxifier配合使用可以让任意程序访问服务器上的服务及端口,实现如下操作： 使用客户端（如Navicat）访问服务上的数据库(如没有对公网暴露的MySQL)； 让本地运行的代码访问服务器内网服务，比如HDFS,Hive,Hbase,ES等等。（对于大数据开发非常实用，可以愉快的打断点调试Spark应用） 配置方法1 配置代理服务器 2 配置规则 3 使用说明配置好规则后，本地应用就可以直接访问服务器的内网IP了。 比如连接数据库：]]></content>
      <tags>
        <tag>SSH</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Kafka 0.10 常用运维命令]]></title>
    <url>%2F2017%2F03%2F02%2Fkafka-operation-command%2F</url>
    <content type="text"><![CDATA[引言Kafka是由LinkedIn开发的一个分布式的消息系统，它以可水平扩展和高吞吐率而被广泛使用，现在已经是Apache的项目。 Kafka系统自带了丰富的运维管理工具，都是基于命令行的，本文主要介绍一些常用的命令。 读者需要对Kafka已经有入门级的了解。 常用命令以下命令都是在Kafka的主目录下执行的。 启动Kafka启动命令需要指定配置文件 bin/kafka-server-start.sh config/server.properties 默认的启动方式并不是守护进程，可以添加’nohup’和’&amp;’让进程保持在后台运行，即使断开SSH终端连接。nohup bin/kafka-server-start.sh config/server.properties &gt; ~/kafka-server-start.out 2&gt;&amp;1 &amp; topic 相关列出 topicbin/kafka-topics.sh --list --zookeeper localhost:2181 —zookeeper：指定zookeeper地址。 创建 topicbin/kafka-topics.sh --create --zookeeper localhost:2181 --replication-factor 3 --partitions 4 --topic test_topic —replication-factor：备份数，就是数据保存几份，这里设置为3，表示该topic的数据会在3个节点上各保存一份。 —partitions：分区数 topic 详情bin/kafka-topics.sh --describe --zookeeper localhost:2181 --topic test_topic 删除 topicbin/kafka-topics.sh --zookeeper localhost:2181 --delete --topic test_topic 需要开启一个配置项才可以删除topic。 配置项在server.properties中：ps: 有个特殊的topic叫 __consumer_offsets，是Kafka内部使用的，不允许删除的。## 生产者消费者相关### 生产者 bin/kafka-console-producer.sh —broker-list localhost:9092 —topic test_topic--broker-list:kafka broker地址### 消费者 bin/kafka-console-consumer.sh —zookeeper localhost:2181 —topic test_topic## 分区相关### 增加分区 bin/kafka-topics.sh —zookeeper localhost:2181 —alter —topic test_topic —partitions 4--partitions:指明将分区增加至多少个。分区不能减少只能增加。### 改变分区分布和副本数 bin/kafka-reassign-partitions.sh —zookeeper localhost:2181 —reassignment-json-file test_topic.json —execute--reassignment-json-file:指定一个分区方案文件。分区方案文件是一个json格式的文本文件，需要自己编写。举个例子，test_topic.json内容如下：```json&#123; &quot;version&quot;: 1, &quot;partitions&quot;: [ &#123; &quot;topic&quot;: &quot;test_topic&quot;, &quot;partition&quot;: 0, &quot;replicas&quot;: [1,2] &#125;, &#123; &quot;topic&quot;: &quot;test_topic&quot;, &quot;partition&quot;: 1, &quot;replicas&quot;: [2,3] &#125;, &#123; &quot;topic&quot;: &quot;test_topic&quot;, &quot;partition&quot;: 2, &quot;replicas&quot;: [3,1] &#125;, &#123; &quot;topic&quot;: &quot;test_topic&quot;, &quot;partition&quot;: 3, &quot;replicas&quot;: [1,2] &#125; ]&#125; 解释其中一部分，剩下的自然也会明白。 &quot;topic&quot;: &quot;test_topic&quot;, //topic名称&quot;partition&quot;: 0, //分区编号，从0开始&quot;replicas&quot;: [1,2] //指定保存在哪个broker,这里是填写broker id。写两个id，意思就是有两份备份啦。 PS:这个方法不能用来增加分区数。第一个replicas就是默认leader。 改变分区和备份数的操作并不能立即完成，而是需要一段时间，内部操作会在后台运行。所以需要… 检查进度bin/kafka-reassign-partitions.sh --zookeeper localhost:2181 --reassignment-json-file test_topic.json --verify 以上。]]></content>
      <tags>
        <tag>Kafka</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark work with MySQL]]></title>
    <url>%2F2016%2F12%2F27%2Fspark-work-with-mysql%2F</url>
    <content type="text"><![CDATA[With spark 2.0.x,we can use DataFrameReader and DataFrameWriter.Use SparkSession.read to access DataFrameReader and use Dataset.write to access DataFrameWriter. Suppose using spark-shell. read exampleval prop=new java.util.Properties()prop.put("user","username")prop.put("password","yourpassword")val url="jdbc:mysql://host:3306/db_name"val df=spark.read.jdbc(url,"table_name",prop)df.show() read example2val jdbcDF = spark.read .format("jdbc") .option("url", "jdbc:mysql:dbserver") .option("dbtable", "schema.tablename") .option("user", "username") .option("password", "password") .load() read example3If you want to read data from a query result rather than a table. val sql="""select * from db.your_table where id&gt;1"""val jdbcDF = spark.read .format("jdbc") .option("url", "jdbc:mysql:dbserver") .option("dbtable", s"( $sql ) t") .option("user", "username") .option("password", "password") .load() write exampleimport org.apache.spark.sql.SaveModeval prop=new java.util.Properties()prop.put("user","username")prop.put("password","yourpassword")val url="jdbc:mysql://host:3306/db_name"//df is a dataframe contains the data which you want to write.df.write.mode(SaveMode.Append).jdbc(url,"table_name",prop)]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hello World, Hello Blog]]></title>
    <url>%2F2016%2F12%2F25%2Fhello-blog%2F</url>
    <content type="text"><![CDATA[Hello!Today is this blog’s first day ! Merry Christmas !]]></content>
      <tags>
        <tag>Life</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hive添加自定义UDF函数]]></title>
    <url>%2F2016%2F04%2F11%2Fadd-udf-to-hive%2F</url>
    <content type="text"><![CDATA[1 编写UDF类以简单的处理单个字段的UDF函数为例,开发自定义UDF函数需要继承’org.apache.hadoop.hive.ql.exec.UDF’类.可以通过Maven添加,pom文件中加入(版本号跟Hive版本一致即可): &lt;dependency&gt; &lt;groupId&gt;org.apache.hive&lt;/groupId&gt; &lt;artifactId&gt;hive-exec&lt;/artifactId&gt; &lt;version&gt;0.13.1&lt;/version&gt;&lt;/dependency&gt; 最简单的实现只需继承UDF类,并实现evaluate函数.如下UDF函数用来将IP(v4)地址转换为整数.package ml.liam8.hive;import org.apache.hadoop.hive.ql.exec.Description;import org.apache.hadoop.hive.ql.exec.UDF;/*** Convert IPv4 to a num which type is Long in java.* Created by Liam on 2016/4/11.*/@Description(name = "IpToNum", value = "_FUNC_(ip) - Convert IPv4 to a num(long).")public class IpToNum extends UDF &#123; public long evaluate(String ip) &#123; String[] nums = ip.split("\\."); return Long.parseLong(nums[3]) + Long.parseLong(nums[2]) * 256 + Long.parseLong(nums[1]) * 65536 + Long.parseLong(nums[0]) * 16777216; &#125;&#125; evaluate方法的输入输出即是UDF函数的输入输出.Description注解部分提供函数的帮助信息. 执行:function test.iptonum```输出:```test.iptonum(ip) - Convert IPv4 to a num(long). 源码已上传 Github 2 部署及创建UDF函数ps:Hive0.13及以后版本适用 部署jar包将jar包复制到HDFS.hdfs -dfs -put udfs-0.1.jar &apos;hdfs:///user/hadoop/hiveUDF&apos; 创建永久函数需在Hive中执行sql语句,格式如下: CREATE FUNCTION [db_name.]function_name AS class_name [USING JAR|FILE|ARCHIVE &#39;file_uri&#39; [, JAR|FILE|ARCHIVE &#39;file_uri&#39;] ]; 如: create function test.iptonum as &#39;com.liam8.hive.IpToNum&#39; using jar &#39;hdfs:///user/hadoop/hiveUDF/udfs-0.1.jar&#39; 函数需要属于某个库,如这里是’test’,当其他库调用时,需要加上库名,如’test.iptonum’. 调用方式: select test.iptonum(&#39;127.0.0.1&#39;); 创建临时函数临时函数只在当前session中有效,临时函数不能指定库. create temporary function iptonum as &#39;com.liam8.hive.IpToNum&#39; using jar &#39;hdfs:///user/hadoop/hiveUDF/udfs-0.1.jar&#39; 调用方式: select iptonum(&#39;127.0.0.1&#39;); 3 参考资料LanguageManualDDL-PermanentFunctions HivePlugins]]></content>
      <tags>
        <tag>Hive</tag>
        <tag>UDF</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Spark on YARN 部署]]></title>
    <url>%2F2016%2F03%2F29%2Fspark-on-yarn%2F</url>
    <content type="text"><![CDATA[1.说明: 首先要部署好Hadoop集群包括HDFS和YARN,这里不再赘述. Spark on YARN模式,不需要启动spark的master和worker,master的工作由YARN完成. 相关路径根据实际情况修改. 1.4和1.6版本部署过程类似. 2.部署下载从官网(http://spark.apache.org/)下载对应Hadoop版本的包,如spark-1.6.1-bin-hadoop2.4.tgz.解压至某个目录,如”/usr/local/hadoop/spark-1.6.1-bin-hadoop2.4”.解压后得到如下文件 配置配置文件修改配置文件在conf目录中.配置环境变量cp spark-env.sh.template spark-env.sh在spark-env.sh中添加如下:export HADOOP_HOME=/usr/local/hadoop/hadoop-2.4.1export YARN_CONF_DIR=$HADOOP_HOME/etc/hadoop/ 配置spark选项cp spark-defaults.conf.template spark-defaults.conf添加如下:spark.master yarn #指定使用yarnspark.driver.memory 1g #指定driver使用的内存spark.executor.memory 512m #指定executor使用的内存 选填:spark.executor.extraClassPath /data/hadoop/hadoop/hadoop-2.4.1/share/hadoop/common/lib/hadoop-lzo-0.4.20.jarspark.driver.extraClassPath /data/hadoop/hadoop/hadoop-2.4.1/share/hadoop/common/lib/*spark.kryoserializer.buffer.max 256m 另外也可以设置一下Spark的log4j配置文件，使得屏幕中不打印额外的INFO信息:cp log4j.properties.template log4j.properties修改一行log4j.rootCategory=WARN, console 启动一个spark应用执行 ./bin/spark-shell 输出将包括如下所示 浏览器访问当前机器的8088端口,将可以看到当前启动的driver的web界面,如下: 点击’Environment’标签,将可以看到相关环境变量和属性的值. 浏览器访问YARN的管理页面,可以看到有一个SPARK应用正在运行(如下),至此spark就可以跑在YARN上了. 3.运行模式Spark on YARN有两种运行模式cluster和client,spark-shell和spark-sql交互式的应用只支持client模式.两种模式区别如下: 从广义上讲，yarn-cluster适用于生产环境；而yarn-client适用于交互和调试，也就是希望快速地看到application的输出。 可以参考这篇博客:Spark:Yarn-cluster和Yarn-client区别与联系(http://www.iteblog.com/archives/1223). 4.参考文献:Running Spark on YARNhttp://spark.apache.org/docs/latest/running-on-yarn.html]]></content>
      <tags>
        <tag>Spark</tag>
        <tag>YARN</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[tags]]></title>
    <url>%2Ftags%2Findex.html</url>
    <content type="text"></content>
  </entry>
</search>
